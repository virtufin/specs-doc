{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Virtufin Platform Specifications","text":"<p>This site contains the formal specifications for the Virtufin microservices platform \u2014 a real-time financial market data pipeline built on .NET 10.0, Dapr, and Kubernetes.</p>"},{"location":"#architecture","title":"Architecture","text":"<pre><code>graph TB\n    subgraph External[\"External\"]\n        BINANCE[\"Binance\"]\n        ALPACA[\"Alpaca\"]\n        NATS[\"NATS\"]\n        WS[\"WebSocket&lt;br/&gt;endpoints\"]\n    end\n\n    subgraph Platform[\"Virtufin Platform\"]\n        subgraph Services[\"Core Services\"]\n            GW[\"API Gateway&lt;br/&gt;(virtufin-api)\"]\n            WM[\"WorkManager&lt;br/&gt;(worker-management)\"]\n            WSM[\"WebSocket Manager&lt;br/&gt;(websocket-proxy)\"]\n        end\n\n        subgraph Infra[\"Infrastructure\"]\n            DAPR[\"Dapr Sidecars\"]\n            REDIS[\"Valkey / Redis\"]\n            K8S[\"Kubernetes\"]\n        end\n    end\n\n    subgraph Consumers[\"Consumers\"]\n        CSHARP[\"C# Client\"]\n        PYTHON[\"Python Client\"]\n        TS[\"TypeScript Client\"]\n    end\n\n    GW -- \"gRPC (h2c)\" --&gt; WM\n    GW -- \"gRPC (h2c)\" --&gt; WSM\n    WM --&gt; DAPR\n    WSM --&gt; DAPR\n    GW --&gt; DAPR\n    DAPR --&gt; REDIS\n    WSM -- \"WebSocket\" --&gt; BINANCE\n    WSM -- \"WebSocket\" --&gt; ALPACA\n    BINANCE --&gt; WSM\n    ALPACA --&gt; WSM\n    CSHARP -- \"gRPC / gRPC-Web\" --&gt; GW\n    PYTHON -- \"gRPC\" --&gt; GW\n    TS -- \"gRPC-Web\" --&gt; GW\n    K8S --- Services</code></pre>"},{"location":"#dependency-map","title":"Dependency Map","text":"<pre><code>graph TD\n    CC[\"cross-cutting\"]\n    PT[\"pubsub-topics\"]\n    SC[\"scenarios\"]\n    CDT[\"core-data-types\"]\n    AG[\"api-gateway\"]\n    WM[\"worker-management\"]\n    WSP[\"websocket-proxy\"]\n    SDK[\"multi-sdk\"]\n    TUI[\"terminal-ui\"]\n    DEP[\"deployment\"]\n    CICD[\"ci-cd-tooling\"]\n\n    CC --&gt; AG\n    CC --&gt; WM\n    CC --&gt; WSP\n    CC --&gt; DEP\n    CC --&gt; CICD\n    PT --&gt; SC\n    AG --&gt; SC\n    AG --&gt; SDK\n    WM --&gt; SDK\n    WSP --&gt; SDK\n    AG --&gt; TUI\n    WM --&gt; TUI\n    WSP --&gt; TUI\n    DEP --&gt; AG\n    DEP --&gt; WM\n    DEP --&gt; WSP\n    CICD --&gt; SDK</code></pre> <p>Note: NATS in the diagram above refers to <code>NatsObservable&lt;T&gt;</code> in <code>Virtufin.Data</code> \u2014 a library-level reactive data source, not a platform infrastructure component. Workers running under the WorkManager use the API gateway's <code>Pubsub.Subscribe</code> RPC for pub/sub, not a direct NATS connection.</p>"},{"location":"#specifications","title":"Specifications","text":"# Specification Requirements Description 1 Cross-cutting 11 Shared infrastructure, API-mediated pub/sub, observability, resilience, security 2 Pub/Sub Topics 19 Topic naming taxonomy, market/scenario worlds, CloudEvents envelope, state-key conventions 3 Scenarios 9 Scenario registry triplets, named hypothetical worlds, lifecycle statuses 4 Core Data Types 5 EAV framework, Variant, FlatBuffers, reactive observables 5 API Gateway 11 Dynamic gRPC discovery, JSON transcoding, distributed state, pub/sub streaming 6 Worker Management 23 Polyglot worker lifecycle, event-driven processing, competing consumers, worker engines 7 WebSocket Proxy 12 External WS connections, pub/sub routing, distributed ownership, auto-reconnect 8 Multi-SDK 8 C#/Python/TypeScript client SDKs from proto contracts 9 Client Testing \u2014 Cross-language client test parity matrix 10 Terminal UI 16 Operator dashboard: worker/connection/event management, multi-context, live streaming, AI assistant 11 Deployment 8 Helm chart, Dapr components, per-service versioning 12 CI/CD Tooling 6 Doc generation, gRPC client publishing, reusable workflows <p>Total: 128 requirements across 11 normative specifications (plus the client-testing parity matrix).</p>"},{"location":"specs/api-gateway/","title":"API Gateway","text":""},{"location":"specs/api-gateway/#purpose","title":"Purpose","text":"<p>Unified API Gateway providing dynamic gRPC service discovery, JSON-to-Protobuf invocation, REST API generation from proto annotations, distributed state management, and pub/sub event streaming for all backend Virtufin services.</p>"},{"location":"specs/api-gateway/#requirements","title":"Requirements","text":""},{"location":"specs/api-gateway/#requirement-dynamic-service-discovery","title":"Requirement: Dynamic Service Discovery","text":"<p>The gateway SHALL discover available backend gRPC services and their methods via gRPC reflection, without requiring pre-compiled proto stubs for each backend.</p>"},{"location":"specs/api-gateway/#scenario-list-known-services","title":"Scenario: List known services","text":"<ul> <li>WHEN a client calls <code>ListServices</code></li> <li>THEN the gateway SHALL return all services defined in its configuration</li> </ul>"},{"location":"specs/api-gateway/#scenario-list-methods-for-a-service","title":"Scenario: List methods for a service","text":"<ul> <li>WHEN a client calls <code>ListMethods</code> for a configured service</li> <li>THEN the gateway SHALL return all gRPC methods discovered via reflection from that backend</li> </ul>"},{"location":"specs/api-gateway/#scenario-schema-introspection","title":"Scenario: Schema introspection","text":"<ul> <li>WHEN a client requests a method's input/output schema</li> <li>THEN the gateway SHALL return the full Protobuf message descriptor including field names, types, and oneof groups</li> </ul>"},{"location":"specs/api-gateway/#scenario-reflection-cache-invalidation","title":"Scenario: Reflection cache invalidation","text":"<ul> <li>WHEN a backend service is redeployed with new methods</li> <li>THEN the gateway SHALL support invalidating its reflection cache for that service</li> </ul>"},{"location":"specs/api-gateway/#requirement-dynamic-grpc-invocation","title":"Requirement: Dynamic gRPC Invocation","text":"<p>The gateway SHALL invoke gRPC methods on backend services without pre-compiled client stubs, using the method name and JSON payload provided at runtime.</p>"},{"location":"specs/api-gateway/#scenario-json-to-protobuf-invocation","title":"Scenario: JSON-to-Protobuf invocation","text":"<ul> <li>WHEN a client sends a JSON payload with a service name and method name</li> <li>THEN the gateway SHALL dynamically marshal the JSON into the method's Protobuf input type, invoke the backend, and return the Protobuf output as JSON</li> </ul>"},{"location":"specs/api-gateway/#scenario-raw-bytes-invocation","title":"Scenario: Raw bytes invocation","text":"<ul> <li>WHEN a client sends a raw Protobuf-encoded byte payload</li> <li>THEN the gateway SHALL forward it directly to the backend method and return the raw response bytes</li> </ul>"},{"location":"specs/api-gateway/#requirement-rest-api-from-proto-annotations","title":"Requirement: REST API from Proto Annotations","text":"<p>The gateway SHALL expose REST/HTTP endpoints equivalent to each gRPC method that has <code>google.api.http</code> annotations. gRPC-Web SHALL be enabled on all endpoints.</p>"},{"location":"specs/api-gateway/#scenario-get-endpoint","title":"Scenario: GET endpoint","text":"<ul> <li>WHEN a gRPC method defines a <code>google.api.http.get</code> annotation</li> <li>THEN the gateway SHALL serve that endpoint as an HTTP GET on the annotated path</li> </ul>"},{"location":"specs/api-gateway/#scenario-post-endpoint","title":"Scenario: POST endpoint","text":"<ul> <li>WHEN a gRPC method defines a <code>google.api.http.post</code> annotation with a body field</li> <li>THEN the gateway SHALL serve that endpoint as an HTTP POST, mapping the JSON body to the Protobuf field</li> </ul>"},{"location":"specs/api-gateway/#scenario-grpc-web-browser-access","title":"Scenario: gRPC-Web browser access","text":"<ul> <li>WHEN a browser client connects via gRPC-Web</li> <li>THEN the gateway SHALL upgrade the request and forward it as native gRPC to the backend</li> </ul>"},{"location":"specs/api-gateway/#requirement-distributed-state-management","title":"Requirement: Distributed State Management","text":"<p>The gateway SHALL provide CRUD operations on the Dapr state store with optimistic concurrency control via ETags. State changes SHALL emit events.</p>"},{"location":"specs/api-gateway/#scenario-save-state-with-etag","title":"Scenario: Save state with ETag","text":"<ul> <li>WHEN a client saves state with an ETag that matches the stored value</li> <li>THEN the save SHALL succeed and the new ETag SHALL be returned</li> </ul>"},{"location":"specs/api-gateway/#scenario-save-state-with-stale-etag","title":"Scenario: Save state with stale ETag","text":"<ul> <li>WHEN a client saves state with an ETag that does not match the stored value</li> <li>THEN the save SHALL fail with a concurrency conflict error</li> </ul>"},{"location":"specs/api-gateway/#scenario-state-change-events","title":"Scenario: State change events","text":"<ul> <li>WHEN state is saved or deleted for a registered key</li> <li>THEN a state change event SHALL be published to Dapr pub/sub on a well-known topic</li> </ul>"},{"location":"specs/api-gateway/#scenario-bulk-state-retrieval","title":"Scenario: Bulk state retrieval","text":"<ul> <li>WHEN a client requests all state for a registered service</li> <li>THEN the gateway SHALL return all stored key-value pairs</li> </ul>"},{"location":"specs/api-gateway/#requirement-pubsub-and-event-streaming","title":"Requirement: Pub/Sub and Event Streaming","text":"<p>The gateway SHALL support publishing events to Dapr pub/sub topics and subscribing clients to topic events via server-side streaming. Dead subscriptions SHALL be cleaned up.</p>"},{"location":"specs/api-gateway/#scenario-publish-event","title":"Scenario: Publish event","text":"<ul> <li>WHEN a client publishes an event to a topic</li> <li>THEN the gateway SHALL forward it to Dapr pub/sub on that topic</li> </ul>"},{"location":"specs/api-gateway/#scenario-subscribe-to-events","title":"Scenario: Subscribe to events","text":"<ul> <li>WHEN a client opens a server-side streaming subscription to a topic</li> <li>THEN all events published to that topic SHALL be streamed to the client until the subscription is cancelled</li> </ul>"},{"location":"specs/api-gateway/#scenario-dead-subscription-cleanup","title":"Scenario: Dead subscription cleanup","text":"<ul> <li>WHEN a subscriber's connection drops without explicit unsubscribe</li> <li>THEN the gateway SHALL detect the dead subscription and remove it within one minute</li> </ul>"},{"location":"specs/api-gateway/#requirement-service-configuration","title":"Requirement: Service Configuration","text":"<p>The gateway SHALL load per-service configuration from a JSON or YAML file, specifying each backend's gRPC host/port, Dapr app ID, pubsub component, state store, and optional cron jobs.</p>"},{"location":"specs/api-gateway/#scenario-configuration-hot-reload","title":"Scenario: Configuration hot-reload","text":"<ul> <li>WHEN the services configuration file is updated</li> <li>THEN the gateway SHOULD reload the configuration without restarting</li> </ul>"},{"location":"specs/api-gateway/#requirement-api-documentation","title":"Requirement: API Documentation","text":"<p>The gateway SHALL expose an OpenAPI/Swagger specification describing all REST endpoints.</p>"},{"location":"specs/api-gateway/#scenario-swagger-ui","title":"Scenario: Swagger UI","text":"<ul> <li>WHEN a developer navigates to <code>/swagger</code></li> <li>THEN the interactive API documentation SHALL be served</li> </ul>"},{"location":"specs/api-gateway/#requirement-pubsub-cloudevent-propagation","title":"Requirement: PubSub CloudEvent Propagation","text":"<p>PublishEvent SHALL receive a complete <code>io.cloudevents.v1.CloudEvent</code> proto from the caller and publish it as-is to Dapr. The caller is responsible for constructing the CloudEvent with all desired attributes (<code>id</code>, <code>source</code>, <code>type</code>, <code>time</code>, etc.). The gateway SHALL NOT add, remove, or modify CloudEvent attributes \u2014 it is a pure passthrough. Request-reply clients (PublishWithResult) SHALL set <code>correlationid</code> and <code>replytopic</code> as CloudEvent attributes before calling PublishEvent.</p>"},{"location":"specs/api-gateway/#requirement-cloudevent-time-attribute","title":"Requirement: CloudEvent Time Attribute","text":"<p>The CloudEvent <code>time</code> attribute (RFC 3339) is a standard CloudEvent attribute carried in the proto's <code>attributes</code> map with type <code>ce_timestamp</code>. The caller sets it directly on the <code>io.cloudevents.v1.CloudEvent</code> proto. No metadata indirection \u2014 the <code>time</code> attribute flows from publisher through Dapr to subscriber as part of the standard CloudEvent payload.</p>"},{"location":"specs/api-gateway/#scenario-caller-supplied-time","title":"Scenario: Caller-supplied time","text":"<ul> <li>WHEN a client publishes a CloudEvent with <code>attributes[\"time\"]</code> set to a <code>ce_timestamp</code> value</li> <li>THEN the CloudEvent <code>time</code> attribute SHALL be preserved in the published event</li> </ul>"},{"location":"specs/api-gateway/#scenario-default-time","title":"Scenario: Default time","text":"<ul> <li>WHEN a client publishes a CloudEvent without a <code>time</code> attribute</li> <li>THEN the gateway SHALL NOT add a default \u2014 the caller owns the CloudEvent entirely</li> </ul>"},{"location":"specs/api-gateway/#scenario-publish-with-correlation-id-request-reply","title":"Scenario: Publish with correlation ID (request-reply)","text":"<ul> <li>WHEN a client calls PublishWithResult with a CloudEvent</li> <li>THEN the client library SHALL set <code>correlationid</code> and <code>replytopic</code> as CloudEvent string attributes before passing to PublishEvent</li> <li>AND the Subscribe handler SHALL extract the correlation ID from <code>SubscribeResponse.Cloudevent.Attributes[\"correlationid\"]</code></li> </ul>"},{"location":"specs/api-gateway/#scenario-publish-without-correlation-id","title":"Scenario: Publish without correlation ID","text":"<ul> <li>WHEN a client publishes an event without <code>correlationid</code> in metadata</li> <li>THEN the gateway SHALL publish the event normally without the correlation ID key</li> </ul>"},{"location":"specs/api-gateway/#requirement-pubsub-metadata-extraction","title":"Requirement: PubSub Metadata Extraction","text":"<p>The Subscribe handler SHALL extract correlation IDs from Dapr <code>TopicMessage.Extensions</code> using <code>Value.StringValue</code> (not <code>Value.ToString()</code>) to avoid JSON-quoted string artifacts. Extracted metadata SHALL be forwarded to gRPC subscribers via <code>BroadcastToTopicAsync</code>.</p>"},{"location":"specs/api-gateway/#scenario-extract-stringvalue-metadata","title":"Scenario: Extract StringValue metadata","text":"<ul> <li>WHEN a Dapr message arrives with a <code>correlationid</code> extension of kind <code>StringValue</code></li> <li>THEN the handler SHALL extract the raw string value without surrounding JSON quotes and include it in the gRPC subscriber broadcast metadata</li> </ul>"},{"location":"specs/api-gateway/#scenario-non-string-metadata-kind","title":"Scenario: Non-string metadata kind","text":"<ul> <li>WHEN a Dapr message arrives with a <code>correlationid</code> extension of a non-StringValue protobuf kind</li> <li>THEN the handler SHALL skip that extension rather than producing a JSON-encoded string</li> </ul>"},{"location":"specs/api-gateway/#requirement-per-service-pubsub-and-state","title":"Requirement: Per-service Pubsub and State","text":"<p>The gateway exposes the regular <code>Pubsub</code> (PublishEvent, Subscribe, Unsubscribe) and <code>State</code> (SaveState, GetState, GetAllState, RegisterKeys, DeleteState) RPCs. Each service is responsible for using these RPCs against its own topic (defined in its <code>Configuration/Topics.cs</code>) and its own state service entry (matching its service name in <code>services.json</code>).</p>"},{"location":"specs/ci-cd-tooling/","title":"CI/CD Tooling","text":""},{"location":"specs/ci-cd-tooling/#purpose","title":"Purpose","text":"<p>Shared CI/CD tooling and developer utilities for the Virtufin ecosystem, including multi-language documentation generation, gRPC client stub generation and publishing, reusable GitHub Actions workflows, centralized versioning, and local development utilities.</p>"},{"location":"specs/ci-cd-tooling/#requirements","title":"Requirements","text":""},{"location":"specs/ci-cd-tooling/#requirement-multi-language-documentation-generation","title":"Requirement: Multi-Language Documentation Generation","text":"<p>The tooling SHALL generate API documentation for C# (via DocFX), Python (via pdoc), and TypeScript (via TypeDoc) from a single configuration. Generated docs SHALL include git metadata.</p>"},{"location":"specs/ci-cd-tooling/#scenario-c-doc-generation","title":"Scenario: C# doc generation","text":"<ul> <li>WHEN <code>DocsGenerator</code> is invoked for a .NET solution</li> <li>THEN DocFX SHALL extract XML comments from source and produce a static HTML site</li> </ul>"},{"location":"specs/ci-cd-tooling/#scenario-python-doc-generation","title":"Scenario: Python doc generation","text":"<ul> <li>WHEN <code>DocsGenerator</code> is invoked for a Python package</li> <li>THEN pdoc SHALL produce HTML documentation from docstrings</li> </ul>"},{"location":"specs/ci-cd-tooling/#scenario-git-commit-badge","title":"Scenario: Git commit badge","text":"<ul> <li>WHEN docs are generated</li> <li>THEN each page SHALL include the git commit SHA and branch in a navigation badge</li> </ul>"},{"location":"specs/ci-cd-tooling/#requirement-grpc-client-generation-and-publishing","title":"Requirement: gRPC Client Generation and Publishing","text":"<p>The tooling SHALL generate and publish gRPC client stubs for Python, TypeScript, and .NET from Protobuf definitions. Publishing SHALL use Gitea package registries.</p>"},{"location":"specs/ci-cd-tooling/#scenario-python-client-publish","title":"Scenario: Python client publish","text":"<ul> <li>WHEN <code>GrpcClientGenerator</code> is invoked for a Python target</li> <li>THEN grpc_tools.protoc SHALL generate stubs, a wheel SHALL be built via uv, and the wheel SHALL be published to the Gitea PyPI registry</li> </ul>"},{"location":"specs/ci-cd-tooling/#scenario-typescript-client-publish","title":"Scenario: TypeScript client publish","text":"<ul> <li>WHEN <code>GrpcClientGenerator</code> is invoked for a TypeScript target</li> <li>THEN buf SHALL generate ES stubs, an npm package SHALL be built, and it SHALL be published to the Gitea npm registry</li> </ul>"},{"location":"specs/ci-cd-tooling/#scenario-net-client-publish","title":"Scenario: .NET client publish","text":"<ul> <li>WHEN <code>GrpcClientGenerator</code> is invoked for a .NET target</li> <li>THEN dotnet pack SHALL create a NuGet package from the client project, and it SHALL be pushed to the Gitea NuGet registry</li> </ul>"},{"location":"specs/ci-cd-tooling/#requirement-reusable-cicd-workflows","title":"Requirement: Reusable CI/CD Workflows","text":"<p>Common CI/CD workflows for Docker builds, Helm deployments, package publishing, doc generation, and linting SHALL be provided as reusable GitHub Actions workflows.</p>"},{"location":"specs/ci-cd-tooling/#scenario-docker-build-workflow","title":"Scenario: Docker build workflow","text":"<ul> <li>WHEN a downstream project calls the Docker build workflow</li> <li>THEN a multi-architecture Docker image SHALL be built and pushed to the Harbor registry</li> </ul>"},{"location":"specs/ci-cd-tooling/#scenario-helm-deploy-workflow","title":"Scenario: Helm deploy workflow","text":"<ul> <li>WHEN a downstream project calls the deploy workflow</li> <li>THEN the Helm chart SHALL be installed or upgraded in the target Kubernetes namespace</li> </ul>"},{"location":"specs/ci-cd-tooling/#requirement-centralized-versioning","title":"Requirement: Centralized Versioning","text":"<p>A single <code>versions.env</code> file SHALL define <code>API_VERSION</code> and <code>LIBRARY_VERSION</code> for all downstream projects. The tooling SHALL read this file to tag packages and Docker images.</p>"},{"location":"specs/ci-cd-tooling/#scenario-version-propagation","title":"Scenario: Version propagation","text":"<ul> <li>WHEN <code>versions.env</code> is updated to a new library version</li> <li>THEN all generated packages, Docker images, and documentation SHALL carry the new version</li> </ul>"},{"location":"specs/ci-cd-tooling/#requirement-docfx-custom-templates","title":"Requirement: DocFX Custom Templates","text":"<p>Shared DocFX templates SHALL provide consistent branding with Bootstrap 5, dark/light theme support, search, and breadcrumbs.</p>"},{"location":"specs/ci-cd-tooling/#scenario-template-application","title":"Scenario: Template application","text":"<ul> <li>WHEN a project uses the custom DocFX templates</li> <li>THEN its generated docs SHALL have the shared Virtufin visual identity</li> </ul>"},{"location":"specs/ci-cd-tooling/#requirement-local-development-utilities","title":"Requirement: Local Development Utilities","text":"<p>Scripts SHALL be provided for running Dapr applications locally, launching gRPC UI for debugging, and listing gRPC services via reflection.</p>"},{"location":"specs/ci-cd-tooling/#scenario-local-dapr-run","title":"Scenario: Local Dapr run","text":"<ul> <li>WHEN <code>run_dapr_app</code> is called with an app ID and port</li> <li>THEN the Dapr sidecar SHALL start, the app SHALL be launched, and health checks SHALL gate readiness before declaring success</li> </ul>"},{"location":"specs/client-testing/","title":"Client Testing","text":""},{"location":"specs/client-testing/#purpose","title":"Purpose","text":"<p>Standardized client test requirements across all 3 repos (api, websocketmanager, workmanager) and all 3 languages (Python, TypeScript, .NET). Every repo SHALL have the same set of client tests in every language, ensuring SDK parity and preventing regressions.</p>"},{"location":"specs/client-testing/#workflow","title":"Workflow","text":""},{"location":"specs/client-testing/#how-test-parity-is-maintained","title":"How test parity is maintained","text":"<pre><code>     proto file \u2500\u2500\u25ba generator \u2500\u2500\u25ba checker \u2500\u2500\u25ba CI gate\n</code></pre> <ol> <li>Generator (<code>virtufin-common/scripts/generate-client-tests.py</code>): Reads a <code>.proto</code> file and emits test files in Python, TypeScript, and .NET with test-case ID comments. Run after proto changes.</li> <li>Checker (<code>virtufin-common/scripts/check-client-test-parity.sh</code>): Scans all 3 repos for ID comments and reports gaps against this spec. Run locally or in CI.</li> <li>CI (<code>virtufin-common/.github/workflows/test-parity-common.yaml</code>): Weekly + on-demand parity gate.</li> </ol>"},{"location":"specs/client-testing/#running-the-generator","title":"Running the generator","text":"<pre><code># The repos are NOT a monorepo, so the script is fetched\n# directly from the virtufin-common Gitea repo at runtime.\ncd virtufin-&lt;service&gt;\npython3 &lt;(curl -L https://git.haenerconsulting.com/virtufin/virtufin-common/raw/branch/master/scripts/generate-client-tests.py) \\\n  src/Virtufin.&lt;Service&gt;.Protos/proto/&lt;service&gt;.proto \\\n  --prefix &lt;X&gt;\n</code></pre> <p>The generator is idempotent. Run it whenever the <code>.proto</code> file changes.</p>"},{"location":"specs/client-testing/#running-the-checker","title":"Running the checker","text":"<pre><code>cd virtufin  # directory containing all service repos\nVIRTUFIN_ROOT=. bash virtufin-common/scripts/check-client-test-parity.sh\n</code></pre> <p>Exits 0 if all IDs are present, 1 if gaps exist.</p>"},{"location":"specs/client-testing/#generated-vs-hand-written-tests","title":"Generated vs hand-written tests","text":"<ul> <li>Generated (<code>tests/python/test_generated.py</code>, <code>tests/typescript/generated.test.ts</code>, <code>tests/...Client.Tests/GeneratedTests.cs</code>): Structural coverage from <code>.proto</code>. Do not edit \u2014 regenerated on proto changes.</li> <li>Hand-written (<code>tests/python/test_*.py</code>, <code>tests/typescript/*.test.ts</code>, <code>tests/...Client.Tests/*Tests.cs</code>): Business logic, patterns, integration. ID-commented for parity.</li> </ul> <p>Both carry test-case IDs. Generated tests provide baseline coverage; hand-written extend it.</p>"},{"location":"specs/client-testing/#test-id-namespaces","title":"Test ID namespaces","text":"<p>The spec test IDs (e.g. <code>A11</code>, <code>W18</code>, <code>M30</code>) are only for hand-written tests. They correspond to the rows in the per-service tables below. The generator emits its own test IDs in a separate namespace so that generated structural/stub tests don't collide with the spec IDs:</p> Service Hand-written prefix Generated prefix virtufin-api <code>A</code> <code>GA</code> virtufin-websocketmanager <code>W</code> <code>GW</code> virtufin-workmanager <code>M</code> <code>GM</code> <p>The generator defaults <code>--gen-prefix</code> to <code>G</code> + <code>--prefix</code> (so <code>--prefix A</code> \u2192 <code>--gen-prefix GA</code>). Override with <code>--gen-prefix &lt;X&gt;</code> if needed.</p> <p>The parity checker (<code>check-client-test-parity.sh</code>) only validates the hand-written IDs against the spec \u2014 generated test files are scanned but their <code>GA*</code> / <code>GW*</code> / <code>GM*</code> IDs don't match the <code>\\b(A|W|M)[0-9]+\\b</code> pattern, so they're correctly excluded.</p>"},{"location":"specs/client-testing/#virtufin-api-client-test-cases","title":"virtufin-api Client Test Cases","text":"<p>The previously specified A1-A10 and A12-A14 (constructor/package/method/integration tests) are deferred. They described an aspirational set that was never implemented in hand-written code; the generator now provides structural coverage via the <code>GA</code>-prefix test IDs in <code>test_generated.py</code> / <code>generated.test.ts</code> / <code>GeneratedTests.cs</code> (see \"Test ID namespaces\" above). Only A11 is currently hand-written.</p> # Category Test Case Python TypeScript .NET A11 Integration Connectivity check \u2713 -- --"},{"location":"specs/client-testing/#virtufin-websocketmanager-client-test-cases","title":"virtufin-websocketmanager Client Test Cases","text":"# Category Test Case Python TypeScript .NET W1 Constructor Default host/port \u2713 \u2713 \u2713 W2 Constructor Custom host/port \u2713 -- -- W3 Constructor All expected methods present \u2713 \u2713 \u2713 W4 Package Proto types importable \u2713 \u2713 \u2713 W5 Package gRPC client stub accessible \u2713 \u2713 \u2713 W6 Package Transport dependency available -- \u2713 \u2713 W7 Package ConnectRequest schema -- \u2713 \u2713 W8 Package ConnectResponse schema -- \u2713 \u2713 W9 Package ListRequest schema -- \u2713 \u2713 W10 Package ListResponse schema -- \u2713 \u2713 W11 Package DisconnectRequest schema -- \u2713 \u2713 W12 Package SendRequest schema -- \u2713 \u2713 W13 Package SendRequest fields (id, message, timeout) -- -- \u2713 W14 Package SendRawRequest schema -- \u2713 \u2713 W15 Package StartPublishRequest schema -- \u2713 \u2713 W16 Package StopPublishRequest schema -- \u2713 \u2713 W17 Package WebSocketConnection schema -- \u2713 \u2713 W18 Method Connect delegates to stub \u2713 \u2713 -- W19 Method List delegates to stub \u2713 \u2713 -- W20 Method Disconnect delegates to stub \u2713 \u2713 -- W21 Method Send delegates to stub \u2713 \u2713 -- W22 Method Send with default timeout \u2713 -- -- W23 Method SendRaw delegates to stub \u2713 \u2713 -- W24 Method StartPublish delegates to stub \u2713 \u2713 -- W25 Method StopPublish delegates to stub \u2713 \u2713 -- W26 Method Close releases channel \u2713 -- -- W27 Error Connect error propagates \u2713 \u2713 \u2713 W28 Error Send error propagates \u2713 \u2713 -- W29 Error List error propagates \u2713 \u2713 -- W30 Lifecycle GrpcChannel construction -- -- \u2713 W31 Lifecycle Transport creates successfully -- \u2713 -- W32 Lifecycle Package exports index entry -- \u2713 -- W33 Method SetTag delegates to stub \u2713 \u2713 -- W34 Method GetTag delegates to stub \u2713 \u2713 -- W35 Method SetTags delegates to stub \u2713 \u2713 -- W36 Method GetTags delegates to stub \u2713 \u2713 --"},{"location":"specs/client-testing/#virtufin-workmanager-client-test-cases","title":"virtufin-workmanager Client Test Cases","text":"# Category Test Case Python TypeScript .NET M1 Constructor Default host/port \u2713 \u2713 \u2713 M2 Constructor Custom host/port \u2713 -- -- M3 Constructor All expected methods present \u2713 \u2713 \u2713 M4 Package Proto types importable \u2713 \u2713 \u2713 M5 Package Proto enums accessible -- \u2713 \u2713 M6 Package gRPC client stub accessible \u2713 \u2713 \u2713 M7 Package Transport dependency available -- \u2713 \u2713 M8 Package CreateWorkerRequest schema -- \u2713 \u2713 M9 Package ListWorkersRequest schema -- \u2713 \u2713 M10 Package DeleteWorkerRequest schema -- \u2713 \u2713 M11 Package StartWorkerRequest schema -- \u2713 \u2713 M12 Package StopWorkerRequest schema -- \u2713 \u2713 M13 Package RecoverWorkersRequest schema -- \u2713 \u2713 M14 Package LoadCodeRequest has an Id property -- \u2713 \u2713 M15 Package LoadCodeRequest has a CodeSource property -- \u2713 \u2713 M16 Package WorkerInfo schema -- \u2713 \u2713 M17 Package CodeSource schema -- \u2713 \u2713 M18 Package WorkerStatus enum -- \u2713 \u2713 M19 Method CreateWorker with URL \u2713 \u2713 -- M20 Method CreateWorker with content \u2713 -- -- M21 Method CreateWorker without connect raises \u2713 -- -- M22 Method ListWorkers delegates to stub \u2713 \u2713 -- M23 Method DeleteWorker delegates to stub \u2713 \u2713 -- M24 Method StartWorker delegates to stub \u2713 \u2713 -- M25 Method StopWorker delegates to stub \u2713 \u2713 -- M26 Method RecoverWorkers delegates to stub \u2713 \u2713 -- M27 Method GetWorkerHistory delegates to stub \u2713 \u2713 -- M28 Obsolete ~~LoadCodeFromContent delegates to stub~~ \u2014 <code>LoadCode</code> consolidates both variants behind one RPC (see M3); no separate <code>LoadCodeFromContent</code> stub method exists to delegate to. -- -- -- M29 Obsolete ~~LoadCodeFromUrl delegates to stub~~ \u2014 same consolidation as M28. -- -- -- M30 Error CreateWorker error propagates \u2713 \u2713 \u2713 M31 Error ListWorkers error propagates -- \u2713 -- M32 Error DeleteWorker error propagates -- \u2713 -- M33 Lifecycle GrpcChannel construction -- -- \u2713 M34 Lifecycle Transport creates successfully -- \u2713 -- M35 Lifecycle Package exports index entry -- \u2713 -- M36 Obsolete ~~SetEnvironmentVariable delegates to stub~~ \u2014 the custom worker environment-variable RPC was removed entirely (process-wide state, unsafe across co-loaded workers); config now flows through the CloudEvent payload instead. -- -- -- M37 Obsolete ~~GetEnvironmentVariable delegates to stub~~ \u2014 same removal as M36. -- -- -- M38 Obsolete ~~SetEnvironmentVariables delegates to stub~~ \u2014 same removal as M36. -- -- -- M39 Obsolete ~~GetEnvironmentVariables delegates to stub~~ \u2014 same removal as M36. -- -- --"},{"location":"specs/client-testing/#rules-for-adding-tests","title":"Rules for Adding Tests","text":"<ol> <li>Every test case listed above SHALL exist for every language marked \u2713.</li> <li>When adding a new test to ANY language, a corresponding row SHALL be added to this spec.</li> <li>The CI parity checker SHALL fail if gaps are detected.</li> <li>Test naming SHALL follow these conventions so the checker can match them:</li> </ol> Language Pattern Example Python <code>test_&lt;snake_case_name&gt;</code> <code>test_connect_delegates_to_stub</code> TypeScript <code>it(\"&lt;human readable name&gt;\")</code> <code>it(\"connect delegates to gRPC client\")</code> .NET <code>CamelCaseName</code> <code>ConnectDelegatesToStub</code> <p>Each test case ID (e.g., <code>W18</code>, <code>M19</code>) MUST appear as a comment in ALL language implementations to enable automated matching:</p> <p><pre><code># W18\ndef test_connect_delegates_to_stub(self, client, mock_stub):\n    ...\n</code></pre> <pre><code>// W18\nit(\"connect delegates to gRPC client\", async () =&gt; {\n    ...\n});\n</code></pre> <pre><code>// W18\n[Fact]\npublic void ConnectDelegatesToStub()\n{\n    ...\n}\n</code></pre></p>"},{"location":"specs/core-data-types/","title":"Core Data Types","text":""},{"location":"specs/core-data-types/#purpose","title":"Purpose","text":"<p>Core .NET library providing the Virtufin data model: Entity-Attribute-Value framework, Variant discriminated union, FlatBuffers serialization, and reactive observables for NATS, WebSocket, and Valkey data sources.</p>"},{"location":"specs/core-data-types/#requirements","title":"Requirements","text":""},{"location":"specs/core-data-types/#requirement-entity-attribute-value-model","title":"Requirement: Entity-Attribute-Value Model","text":"<p>The library SHALL provide a registry-based EAV framework where entities own named attributes whose values are provided by pluggable value providers.</p>"},{"location":"specs/core-data-types/#scenario-entity-lifecycle","title":"Scenario: Entity lifecycle","text":"<ul> <li>WHEN an entity is created with a unique identifier</li> <li>THEN it SHALL be registerable in a global registry and support adding attributes of any type</li> </ul>"},{"location":"specs/core-data-types/#scenario-value-observation","title":"Scenario: Value observation","text":"<ul> <li>WHEN an attribute value changes</li> <li>THEN value providers SHALL notify subscribers reactively</li> </ul>"},{"location":"specs/core-data-types/#requirement-variant-discriminated-union","title":"Requirement: Variant Discriminated Union","text":"<p>The library SHALL provide a sealed <code>Variant</code> type representing a value that is exactly one of: None, Bool, Byte, Int16, Int32, Int64, Float, Double, String, a typed array of any of the preceding primitive kinds, a nested array of <code>Variant</code> (<code>ArrayVariant</code>), or Dictionary. It SHALL support FlatBuffers serialization and implicit conversions from C# primitives.</p>"},{"location":"specs/core-data-types/#scenario-type-safe-construction","title":"Scenario: Type-safe construction","text":"<ul> <li>WHEN a Variant is constructed from a C# primitive (int, string, bool, etc.)</li> <li>THEN the type SHALL be determined at construction and retrievable via pattern matching</li> </ul>"},{"location":"specs/core-data-types/#scenario-flatbuffers-serialization","title":"Scenario: FlatBuffers serialization","text":"<ul> <li>WHEN a Variant is serialized to FlatBuffers binary</li> <li>THEN it SHALL deserialize to an equivalent Variant with the same value and type</li> </ul>"},{"location":"specs/core-data-types/#scenario-implicit-conversions","title":"Scenario: Implicit conversions","text":"<ul> <li>WHEN a Variant is used where a <code>string</code> or <code>int</code> is expected</li> <li>THEN the implicit conversion SHALL succeed if the underlying type matches, or throw if it does not</li> </ul>"},{"location":"specs/core-data-types/#requirement-reactive-observables","title":"Requirement: Reactive Observables","text":"<p>The library SHALL provide <code>IObservable&lt;T&gt;</code> implementations for NATS, WebSocket, and Valkey data sources. Observables SHALL auto-reconnect on connection loss.</p>"},{"location":"specs/core-data-types/#scenario-nats-subscription","title":"Scenario: NATS subscription","text":"<ul> <li>WHEN a NATS observable is subscribed to with a subject pattern</li> <li>THEN each message published on matching subjects SHALL be delivered to all subscribers</li> </ul>"},{"location":"specs/core-data-types/#scenario-connection-lifecycle","title":"Scenario: Connection lifecycle","text":"<ul> <li>WHEN the underlying connection to a data source is lost</li> <li>THEN the observable SHALL reconnect automatically and resume message delivery</li> </ul>"},{"location":"specs/core-data-types/#requirement-financial-domain-types","title":"Requirement: Financial Domain Types","text":"<p>The library SHALL provide financial domain primitives including <code>Currency</code> (the full ISO 4217 currency code set, not limited to any subset) and <code>Cash</code>. Operations on mismatched currencies SHALL be rejected.</p> <p>Note (2026-07-26): <code>Cash</code> currently has no arithmetic operators at all (<code>+</code>, <code>-</code>, equality) \u2014 the mismatch-rejection behavior described by \"Operations on mismatched currencies SHALL be rejected\" has no implementation yet to test against. This requirement describes the intended contract, not current behavior.</p>"},{"location":"specs/core-data-types/#scenario-cash-arithmetic","title":"Scenario: Cash arithmetic","text":"<ul> <li>WHEN two Cash values with the same currency are added</li> <li>THEN the result SHALL be a Cash with the summed amount and same currency</li> </ul>"},{"location":"specs/core-data-types/#scenario-currency-mismatch","title":"Scenario: Currency mismatch","text":"<ul> <li>WHEN two Cash values with different currencies are combined</li> <li>THEN the operation SHALL reject with a clear error</li> </ul>"},{"location":"specs/core-data-types/#requirement-flatbuffers-wire-format","title":"Requirement: FlatBuffers Wire Format","text":"<p>The library SHALL define a FlatBuffers schema for the Variant type and generate serialization code at build time. All language SDKs SHOULD use this schema for cross-language data interchange.</p>"},{"location":"specs/core-data-types/#scenario-cross-language-compatibility","title":"Scenario: Cross-language compatibility","text":"<ul> <li>WHEN a Variant is serialized by a C# producer</li> <li>THEN a Python or TypeScript consumer using the same FlatBuffers schema SHALL be able to deserialize it correctly</li> </ul>"},{"location":"specs/core-data-types/#scenario-build-time-code-generation","title":"Scenario: Build-time code generation","text":"<ul> <li>WHEN the project is built</li> <li>THEN the FlatBuffers compiler (<code>flatc</code>) SHALL generate C# serialization code before compilation begins</li> </ul>"},{"location":"specs/cross-cutting/","title":"Cross-cutting","text":""},{"location":"specs/cross-cutting/#purpose","title":"Purpose","text":"<p>Shared infrastructure, security, observability, and resilience requirements that apply across all Virtufin services. Every service SHALL conform to these requirements.</p> <p>Topic and state-key naming conventions live in pubsub-topics/spec.md; scenario registry conventions live in scenarios/spec.md. This spec covers the access patterns (API-mediated access, ownership) and the non-naming cross-cutting concerns.</p>"},{"location":"specs/cross-cutting/#requirements","title":"Requirements","text":""},{"location":"specs/cross-cutting/#requirement-api-mediated-pubsub-and-state","title":"Requirement: API-Mediated Pub/Sub and State","text":"<p>Services MUST NOT call <code>DaprClient.PublishEventAsync</code>, <code>DaprClient.GetStateAsync</code>, <code>DaprClient.SaveStateAsync</code>, <code>DaprClient.DeleteStateAsync</code>, <code>GetBulkStateAsync</code>, or any other Dapr pubsub or state API directly. Services MUST use the virtufin-api's <code>Pubsub</code> and <code>State</code> gRPC services for all pubsub and state operations.</p> <p>The virtufin-api is the only service that talks to Dapr pubsub/state on behalf of other services. This gives the API a single place for access control, observability, and state ownership. It does NOT enforce topic naming -- see Pub/Sub Topics spec \u00a7No Topic or Scenario-State Validation Anywhere; topic naming is a convention, not an API-enforced contract.</p> <p>Scenario registry management (create / pause / archive / delete) uses the same generic <code>State.SaveState</code> / <code>State.GetState</code> / <code>State.DeleteState</code> / <code>State.QueryState</code> RPCs above, not dedicated scenario RPCs -- per scenarios/spec.md, \"No new RPCs SHALL be added to virtufin-api for scenario management\"; virtufin-api stays pure infrastructure with no domain logic.</p> <p>No inline topic-shape validation (clarification): Services that publish directly to Dapr for any reason (e.g., the WebSocketManager's per-frame user-data publish path, which is high-volume and low-value-for-mediation) SHALL NOT validate the topic name against the patterns in Pub/Sub Topics spec \u00a7Topic Patterns before calling <code>DaprClient.PublishEventAsync</code>. Topic-pattern shape (universe, scenario ID, venue, entity, event) is Virtufin domain vocabulary; a direct-publish service is pure infrastructure and SHALL NOT encode or enforce it. Any topic Dapr/NATS itself accepts as legal SHALL be passed through unvalidated. This was previously a MUST (see <code>pubsub-topics/migration.md</code> for the superseded rule); reversed because inline pattern-matching duplicated domain knowledge into a service (<code>virtufin-websocketmanager</code>) whose charter is explicitly \"no business logic.\"</p>"},{"location":"specs/cross-cutting/#scenario-service-publishes-an-event","title":"Scenario: Service publishes an event","text":"<ul> <li>WHEN a service needs to publish an event</li> <li>THEN it SHALL call <code>Pubsub.PublishEvent</code> on the virtufin-api (gRPC)</li> <li>AND it SHALL NOT call <code>DaprClient.PublishEventAsync</code> directly</li> </ul>"},{"location":"specs/cross-cutting/#scenario-service-publishes-a-high-volume-per-frame-event-eg-websocketmanager-market-data","title":"Scenario: Service publishes a high-volume per-frame event (e.g., WebSocketManager market data)","text":"<ul> <li>WHEN a service publishes per-frame user-data events directly to Dapr (high volume, low mediation value)</li> <li>THEN it SHALL NOT validate the topic name against Pub/Sub Topics spec \u00a7Topic Patterns -- that is domain vocabulary, not this service's concern</li> <li>AND any topic Dapr/NATS itself accepts as legal SHALL be passed through to <code>DaprClient.PublishEventAsync</code> unvalidated</li> </ul>"},{"location":"specs/cross-cutting/#scenario-service-reads-or-writes-state","title":"Scenario: Service reads or writes state","text":"<ul> <li>WHEN a service needs to read or write persistent state</li> <li>THEN it SHALL call the API's <code>State</code> gRPC service (SaveState, GetState, GetAllState, RegisterKeys, DeleteState)</li> <li>AND it SHALL NOT call any Dapr state API directly</li> </ul>"},{"location":"specs/cross-cutting/#scenario-dapr-sidecar-usage","title":"Scenario: Dapr sidecar usage","text":"<ul> <li>WHEN a service uses Dapr for service-invocation, mTLS, distributed tracing, or metrics</li> <li>THEN the Dapr sidecar is the right path; pubsub and state go through the API</li> <li>The sidecar stays even after the API-mediates pubsub/state; the two concerns are independent</li> </ul>"},{"location":"specs/cross-cutting/#requirement-topic-naming-delegated-to-pubsub-topics-spec","title":"Requirement: Topic Naming (delegated to pubsub-topics spec)","text":"<p>Topic patterns, CloudEvents envelope contract, scenario ID conventions, NATS permissions, state-key taxonomy, and migration rules are defined in pubsub-topics/spec.md. This spec does NOT duplicate those rules; services SHALL conform to the patterns in that spec.</p>"},{"location":"specs/cross-cutting/#scenario-service-publishes-a-domain-event","title":"Scenario: Service publishes a domain event","text":"<ul> <li>WHEN a service publishes an event under any of the namespaces defined in pubsub-topics/spec.md (Tier 0 infrastructure, market data world, scenario world)</li> <li>THEN it SHOULD call <code>Pubsub.PublishEvent</code> with the topic matching the pattern for that namespace</li> <li>AND the API does NOT validate or reject the topic -- see Pub/Sub Topics spec \u00a7No Topic or Scenario-State Validation Anywhere</li> </ul>"},{"location":"specs/cross-cutting/#scenario-service-publishes-a-scenario-scoped-event","title":"Scenario: Service publishes a scenario-scoped event","text":"<ul> <li>WHEN a service publishes to <code>sc.&lt;scenarioId&gt;.&lt;domain&gt;.*</code></li> <li>THEN the API does NOT validate that <code>scenarioId</code> is registered, active, or otherwise -- per scenarios/spec.md, <code>virtufin-api</code> carries no domain logic, and no service validates scenario state before publish</li> </ul>"},{"location":"specs/cross-cutting/#scenario-subscriber-consumes-another-services-events","title":"Scenario: Subscriber consumes another service's events","text":"<ul> <li>WHEN a service needs to consume another service's events</li> <li>THEN it SHALL call <code>Pubsub.Subscribe</code> with the topic string duplicated locally</li> <li>AND the local constant SHALL carry a comment cross-linking to the publisher's <code>Configuration/Topics.cs</code> source of truth (or scenario registry entry for scenario-scoped topics)</li> </ul>"},{"location":"specs/cross-cutting/#scenario-topic-name-is-a-public-contract","title":"Scenario: Topic name is a public contract","text":"<ul> <li>WHEN a publisher changes its topic name</li> <li>THEN it is a breaking change for every subscriber; the constant is part of the service's public contract</li> </ul>"},{"location":"specs/cross-cutting/#requirement-per-service-state-service-names","title":"Requirement: Per-service State Service Names","text":"<p>Each service uses its own state service entry (the entry in the API's <code>services.json</code> matching the service's name). Callers use the regular <code>State.*</code> RPCs with the <code>service</code> field set to the per-service name (<code>websocketmanager</code>, <code>workmanager</code>). There are no <code>State.*System*</code> RPC variants.</p>"},{"location":"specs/cross-cutting/#scenario-service-reads-or-writes-its-own-state","title":"Scenario: Service reads or writes its own state","text":"<ul> <li>WHEN a service needs to read or write its own persistent state</li> <li>THEN it SHALL call the regular <code>State.*</code> RPCs with <code>service: \"&lt;service-name&gt;\"</code></li> <li>AND it SHALL NOT call any <code>State.*System*</code> variant (none exist)</li> </ul>"},{"location":"specs/cross-cutting/#requirement-lifecycle-events-as-cloudevents-v10","title":"Requirement: Lifecycle Events as CloudEvents v1.0","text":"<p>Services that publish lifecycle events MUST format them as CloudEvents v1.0 envelopes. The CloudEvents attributes (<code>ce-id</code>, <code>ce-source</code>, <code>ce-type</code>, <code>ce-time</code>, <code>ce-specversion</code>, <code>ce-datacontenttype</code>) MUST be carried in the request <code>metadata</code> field, prefixed with <code>ce-</code>. The CloudEvents <code>data</code> field MUST be the request's <code>data</code> field.</p>"},{"location":"specs/cross-cutting/#scenario-connection-lifecycle-event","title":"Scenario: Connection lifecycle event","text":"<ul> <li>WHEN a service publishes a connection lifecycle event (e.g., a WebSocket connection is established, disconnects, or fails)</li> <li>THEN it SHALL set <code>ce-type</code> to <code>com.virtufin.&lt;service&gt;.lifecycle.&lt;state&gt;</code></li> <li>AND <code>ce-source</code> to its own service URN (e.g., <code>urn:com.virtufin.websocketmanager</code>)</li> <li>AND the data payload to a JSON object with <code>connection_id</code>, <code>url</code>, <code>instance_id</code> (and any state-specific fields)</li> </ul>"},{"location":"specs/cross-cutting/#scenario-worker-lifecycle-event","title":"Scenario: Worker lifecycle event","text":"<ul> <li>WHEN a service publishes a worker lifecycle event (created, started, stopped, error)</li> <li>THEN it SHALL set <code>ce-type</code> to <code>com.virtufin.workmanager.lifecycle.&lt;state&gt;</code></li> <li>AND <code>ce-source</code> to <code>urn:com.virtufin.workmanager</code></li> <li>AND the data payload to a JSON object with <code>worker_id</code>, <code>group</code>, <code>topic</code> (and <code>error_type</code> / <code>error_message</code> for error events)</li> </ul>"},{"location":"specs/cross-cutting/#requirement-api-endpoint-configuration","title":"Requirement: API Endpoint Configuration","text":"<p>Services that call the virtufin-api MUST accept the API's gRPC endpoint via configuration. The endpoint SHALL be configurable via environment variables (<code>VIRTUFIN_API_HOST</code>, <code>VIRTUFIN_API_GRPC_PORT</code>) with sensible defaults (<code>localhost:5002</code>).</p>"},{"location":"specs/cross-cutting/#scenario-production-deployment","title":"Scenario: Production deployment","text":"<ul> <li>WHEN a service is deployed to a cluster</li> <li>THEN <code>VIRTUFIN_API_HOST</code> and <code>VIRTUFIN_API_GRPC_PORT</code> SHALL be set to the cluster-internal address of the virtufin-api service</li> </ul>"},{"location":"specs/cross-cutting/#scenario-local-development","title":"Scenario: Local development","text":"<ul> <li>WHEN a service runs locally</li> <li>THEN the default <code>localhost:5002</code> is used unless overridden</li> </ul>"},{"location":"specs/cross-cutting/#requirement-observability","title":"Requirement: Observability","text":"<p>Every service SHALL emit OpenTelemetry traces and metrics. Health checks SHALL gate readiness until the service is fully initialized.</p>"},{"location":"specs/cross-cutting/#scenario-distributed-tracing","title":"Scenario: Distributed tracing","text":"<ul> <li>WHEN a request is processed across multiple services</li> <li>THEN each span SHALL be exported to the configured OpenTelemetry collector with correlation context</li> </ul>"},{"location":"specs/cross-cutting/#scenario-liveness-and-readiness","title":"Scenario: Liveness and readiness","text":"<ul> <li>WHEN a service starts up and before it recovers its persistent state</li> <li>THEN the readiness health check SHALL fail, preventing traffic routing</li> </ul>"},{"location":"specs/cross-cutting/#scenario-startup-recovery","title":"Scenario: Startup recovery","text":"<ul> <li>WHEN a service restarts after a crash</li> <li>THEN it SHALL restore its persisted state before marking itself healthy</li> </ul>"},{"location":"specs/cross-cutting/#requirement-resilience","title":"Requirement: Resilience","text":"<p>All Dapr operations and external calls SHALL use retry with exponential backoff and circuit breaking.</p>"},{"location":"specs/cross-cutting/#scenario-transient-dapr-failure","title":"Scenario: Transient Dapr failure","text":"<ul> <li>WHEN a Dapr API call fails with a transient error</li> <li>THEN the operation SHALL be retried up to 3 times with exponential backoff before failing</li> </ul>"},{"location":"specs/cross-cutting/#scenario-persistent-failure","title":"Scenario: Persistent failure","text":"<ul> <li>WHEN Dapr API calls consistently fail beyond a threshold</li> <li>THEN the circuit breaker SHALL open, failing fast for subsequent calls until the break duration elapses</li> </ul>"},{"location":"specs/cross-cutting/#requirement-security","title":"Requirement: Security","text":"<p>All service-to-service communication SHALL be encrypted. User-supplied code execution SHALL be sandboxed. URL-based code fetching SHALL prevent SSRF.</p>"},{"location":"specs/cross-cutting/#scenario-service-to-service-communication","title":"Scenario: Service-to-service communication","text":"<ul> <li>WHEN one service communicates with another</li> <li>THEN Dapr mTLS SHALL encrypt the connection</li> </ul>"},{"location":"specs/cross-cutting/#scenario-ssrf-prevention","title":"Scenario: SSRF prevention","text":"<ul> <li>WHEN fetching code from a user-supplied URL</li> <li>THEN the service SHALL reject URLs targeting private IP ranges (loopback, RFC1918, link-local) unless the host is in an explicit allowlist</li> </ul>"},{"location":"specs/cross-cutting/#requirement-native-aot-compilation","title":"Requirement: Native AOT Compilation","text":"<p>Services targeting AOT-capable runtimes SHALL compile to native code. All JSON serialization SHALL use source-generated contexts.</p>"},{"location":"specs/cross-cutting/#scenario-build-and-deployment","title":"Scenario: Build and deployment","text":"<ul> <li>WHEN a service is built for production</li> <li>THEN it SHALL compile to a self-contained native binary with trimmed dependencies</li> </ul>"},{"location":"specs/cross-cutting/#requirement-configuration","title":"Requirement: Configuration","text":"<p>Every service SHALL support configuration via command-line arguments, environment variables, and configuration files. Ports SHALL be configurable. Dapr component names SHALL be configurable.</p>"},{"location":"specs/cross-cutting/#scenario-port-override","title":"Scenario: Port override","text":"<ul> <li>WHEN the <code>HttpPort</code> environment variable or <code>--http-port</code> argument is set</li> <li>THEN the service SHALL listen on that port instead of the default</li> </ul>"},{"location":"specs/cross-cutting/#scenario-dapr-component-override","title":"Scenario: Dapr component override","text":"<ul> <li>WHEN <code>pubsubName</code> is configured</li> <li>THEN the service SHALL use the named Dapr pubsub component for all pub/sub operations</li> </ul>"},{"location":"specs/cross-cutting/#requirement-containerization","title":"Requirement: Containerization","text":"<p>Every service SHALL be containerizable as a minimal Docker image based on the chiseled runtime image.</p>"},{"location":"specs/cross-cutting/#scenario-docker-build","title":"Scenario: Docker build","text":"<ul> <li>WHEN a service's Dockerfile is built</li> <li>THEN the resulting image SHALL contain only the native AOT binary and its runtime dependencies on a <code>runtime-deps</code> base</li> </ul>"},{"location":"specs/deployment/","title":"Deployment","text":""},{"location":"specs/deployment/#purpose","title":"Purpose","text":"<p>Helm chart and deployment scripts for deploying the Virtufin platform (API Gateway, WorkManager, WebSocket Manager) on Kubernetes with Dapr sidecars, configurable per-service versioning, Dapr Components, health probes, and private registry support.</p>"},{"location":"specs/deployment/#requirements","title":"Requirements","text":""},{"location":"specs/deployment/#requirement-helm-chart","title":"Requirement: Helm Chart","text":"<p>The platform SHALL be deployable via a single Helm v2 chart that covers the API Gateway, WorkManager, and WebSocket Manager with Dapr sidecars. Each service SHALL be independently enableable.</p>"},{"location":"specs/deployment/#scenario-full-stack-install","title":"Scenario: Full stack install","text":"<ul> <li>WHEN <code>helm install virtufin .</code> is run</li> <li>THEN all three services, their Dapr sidecars, Dapr Components, and ConfigMaps SHALL be created in the target namespace</li> </ul>"},{"location":"specs/deployment/#scenario-single-service-install","title":"Scenario: Single-service install","text":"<ul> <li>WHEN the chart is installed with all but one service disabled</li> <li>THEN only the enabled service and its Dapr sidecar SHALL be deployed</li> </ul>"},{"location":"specs/deployment/#requirement-per-service-versioning","title":"Requirement: Per-Service Versioning","text":"<p>Each service's Docker image tag SHALL be independently configurable. Services SHALL support replicas, resource limits, and environment variables.</p>"},{"location":"specs/deployment/#scenario-canary-deployment","title":"Scenario: Canary deployment","text":"<ul> <li>WHEN a specific service's image tag is overridden</li> <li>THEN only that deployment SHALL roll out the new image; other services SHALL remain unchanged</li> </ul>"},{"location":"specs/deployment/#requirement-dapr-component-management","title":"Requirement: Dapr Component Management","text":"<p>The Helm chart SHALL create <code>dapr.io/v1alpha1 Component</code> resources for the state store and pub/sub broker. These SHALL reference Redis/Valkey with configurable host, port, password, redelivery count, and dead-letter topic.</p>"},{"location":"specs/deployment/#scenario-production-redis","title":"Scenario: Production Redis","text":"<ul> <li>WHEN deployed with a custom state store host and password</li> <li>THEN the Dapr state store component SHALL connect to the specified Redis with authentication</li> </ul>"},{"location":"specs/deployment/#scenario-pubsub-redelivery","title":"Scenario: Pub/Sub redelivery","text":"<ul> <li>WHEN a message cannot be processed</li> <li>THEN the pubsub component SHALL redeliver up to the configured maximum count, with a configurable redelivery interval, before routing to the dead-letter topic</li> </ul>"},{"location":"specs/deployment/#requirement-dapr-configuration","title":"Requirement: Dapr Configuration","text":"<p>The Helm chart SHALL create a <code>dapr.io/v1alpha1 Configuration</code> resource with tracing, metrics, and mTLS settings.</p>"},{"location":"specs/deployment/#scenario-mtls-enforcement","title":"Scenario: mTLS enforcement","text":"<ul> <li>WHEN mTLS is enabled in Dapr config</li> <li>THEN all inter-service communication SHALL be encrypted and authenticated</li> </ul>"},{"location":"specs/deployment/#scenario-tracing-sampling","title":"Scenario: Tracing sampling","text":"<ul> <li>WHEN tracing is configured with a sampling rate</li> <li>THEN the Dapr sidecar SHALL sample traces at that rate</li> </ul>"},{"location":"specs/deployment/#requirement-service-discovery-configuration","title":"Requirement: Service Discovery Configuration","text":"<p>The API Gateway's <code>services.json</code> SHALL be generated as a ConfigMap from Helm values, listing each enabled backend service with its gRPC host, port, Dapr app ID, and pubsub/state component names.</p>"},{"location":"specs/deployment/#scenario-service-registration","title":"Scenario: Service registration","text":"<ul> <li>WHEN a new backend service is added via Helm values</li> <li>THEN it SHALL appear in the generated <code>services.json</code> ConfigMap without manual intervention</li> </ul>"},{"location":"specs/deployment/#requirement-health-probes","title":"Requirement: Health Probes","text":"<p>Every deployment SHALL configure Kubernetes liveness and readiness probes on the service's HTTP health endpoint. Dapr sidecars SHALL use the service health check path.</p>"},{"location":"specs/deployment/#scenario-readiness-gate","title":"Scenario: Readiness gate","text":"<ul> <li>WHEN a service is starting up and has not completed recovery</li> <li>THEN the readiness probe SHALL fail, preventing the Service from routing traffic to that pod</li> </ul>"},{"location":"specs/deployment/#requirement-private-registry","title":"Requirement: Private Registry","text":"<p>All container images SHALL be pulled from a private Harbor registry using a Kubernetes image pull secret.</p>"},{"location":"specs/deployment/#scenario-image-pull","title":"Scenario: Image pull","text":"<ul> <li>WHEN Kubernetes schedules a pod</li> <li>THEN it SHALL authenticate to the private registry using the configured image pull secret</li> </ul>"},{"location":"specs/deployment/#requirement-single-service-deployundeploy","title":"Requirement: Single-Service Deploy/Undeploy","text":"<p>Deployment scripts SHALL support deploying or undeploying individual services without affecting the rest of the release.</p>"},{"location":"specs/deployment/#scenario-remove-a-single-service","title":"Scenario: Remove a single service","text":"<ul> <li>WHEN the undeploy script is run targeting a specific service</li> <li>THEN only that deployment and service SHALL be removed while other services continue running</li> </ul>"},{"location":"specs/deployment/#scenario-deploy-a-single-service","title":"Scenario: Deploy a single service","text":"<ul> <li>WHEN the deploy script is run targeting a specific service</li> <li>THEN only that deployment SHALL be installed or upgraded without redeploying other services</li> </ul>"},{"location":"specs/multi-sdk/","title":"Multi-SDK","text":""},{"location":"specs/multi-sdk/#purpose","title":"Purpose","text":"<p>First-class client SDKs for C#/.NET, Python, and TypeScript, generated from service Protobuf definitions and distributed via private Gitea package registries with idiomatic async APIs and consistent error handling.</p>"},{"location":"specs/multi-sdk/#requirements","title":"Requirements","text":""},{"location":"specs/multi-sdk/#requirement-single-source-proto-contracts","title":"Requirement: Single-Source Proto Contracts","text":"<p>Every service with a public API SHALL define its interface in a Protocol Buffers <code>.proto</code> file. Client SDKs for all languages SHALL be generated from this single source.</p>"},{"location":"specs/multi-sdk/#scenario-proto-as-source-of-truth","title":"Scenario: Proto as source of truth","text":"<ul> <li>WHEN a service adds or modifies an RPC method</li> <li>THEN the change SHALL be made in the <code>.proto</code> file, with SDKs regenerated from it</li> </ul>"},{"location":"specs/multi-sdk/#requirement-first-class-net-client","title":"Requirement: First-Class .NET Client","text":"<p>Each service SHALL provide a NuGet package with a typed gRPC client. The client SHALL support using the service's generated stubs directly.</p>"},{"location":"specs/multi-sdk/#scenario-service-invocation","title":"Scenario: Service invocation","text":"<ul> <li>WHEN a .NET consumer references the client NuGet package</li> <li>THEN it SHALL be able to call all gRPC methods strongly typed, without manual proto compilation</li> </ul>"},{"location":"specs/multi-sdk/#requirement-first-class-python-client","title":"Requirement: First-Class Python Client","text":"<p>Each service SHALL provide a PyPI package (<code>virtufin-&lt;service&gt;</code>) with async gRPC client wrappers. Generated stubs SHALL use <code>grpcio</code>.</p>"},{"location":"specs/multi-sdk/#scenario-async-service-invocation","title":"Scenario: Async service invocation","text":"<ul> <li>WHEN a Python consumer imports the client package and calls an async method</li> <li>THEN the call SHALL be made over gRPC asynchronously</li> </ul>"},{"location":"specs/multi-sdk/#requirement-first-class-typescript-client","title":"Requirement: First-Class TypeScript Client","text":"<p>Each service SHALL provide an npm package (<code>virtufin-&lt;service&gt;</code>) with gRPC-Web or ConnectRPC client support. Packages SHALL use unscoped naming.</p>"},{"location":"specs/multi-sdk/#scenario-browser-grpc-web-call","title":"Scenario: Browser gRPC-Web call","text":"<ul> <li>WHEN a TypeScript consumer in a browser environment calls a service method</li> <li>THEN the call SHALL use gRPC-Web transport with ConnectRPC</li> </ul>"},{"location":"specs/multi-sdk/#requirement-idiomatic-error-handling","title":"Requirement: Idiomatic Error Handling","text":"<p>Client SDKs SHALL translate gRPC status codes to language-idiomatic exceptions or error types.</p>"},{"location":"specs/multi-sdk/#scenario-not-found-error","title":"Scenario: Not found error","text":"<ul> <li>WHEN the server returns <code>NOT_FOUND</code></li> <li>THEN the Python client SHALL raise a typed exception with the gRPC status details</li> <li>THEN the TypeScript client SHALL return a discriminated error</li> <li>THEN the C# client SHALL throw an <code>RpcException</code></li> </ul>"},{"location":"specs/multi-sdk/#requirement-gitea-package-distribution","title":"Requirement: Gitea Package Distribution","text":"<p>All SDK packages SHALL be published to the private Gitea package registry. Build pipelines SHALL handle versioning from a shared <code>versions.env</code>.</p>"},{"location":"specs/multi-sdk/#scenario-package-publishing","title":"Scenario: Package publishing","text":"<ul> <li>WHEN a service is released</li> <li>THEN its NuGet package SHALL be published to the Gitea NuGet registry</li> <li>THEN its PyPI package SHALL be published to the Gitea PyPI registry</li> <li>THEN its npm package SHALL be published to the Gitea npm registry</li> </ul>"},{"location":"specs/multi-sdk/#requirement-version-compatibility","title":"Requirement: Version Compatibility","text":"<p>Client SDK versions SHALL match the API version of the service they connect to. Breaking proto changes SHALL bump the library major version.</p>"},{"location":"specs/multi-sdk/#scenario-api-version-mismatch","title":"Scenario: API version mismatch","text":"<ul> <li>WHEN a client SDK v1 connects to a service with an incompatible proto schema</li> <li>THEN the error SHALL be clearly identifiable as a version mismatch</li> </ul>"},{"location":"specs/multi-sdk/#requirement-request-reply-over-pubsub","title":"Requirement: Request-Reply over PubSub","text":"<p>Python and C# client SDKs SHALL support the request-reply pattern over Dapr pub/sub using correlation IDs. The <code>publish_with_result_async</code> (Python) and <code>PublishWithResultAsync</code> (C#) methods SHALL publish to a topic with a generated <code>correlationid</code> in metadata, subscribe to a reply topic, and return the first response whose <code>correlationid</code> metadata matches.</p>"},{"location":"specs/multi-sdk/#scenario-successful-request-reply","title":"Scenario: Successful request-reply","text":"<ul> <li>WHEN a client publishes a request with correlation ID X and a responder publishes a response on the reply topic with the same correlation ID</li> <li>THEN the client SHALL receive the matching response within the specified timeout</li> </ul>"},{"location":"specs/multi-sdk/#scenario-timeout","title":"Scenario: Timeout","text":"<ul> <li>WHEN no matching response arrives within the timeout period</li> <li>THEN the client SHALL raise a TimeoutError (Python) or TimeoutException (C#)</li> </ul>"},{"location":"specs/pubsub-topics/","title":"Pub/Sub Topics","text":""},{"location":"specs/pubsub-topics/#purpose","title":"Purpose","text":"<p>Canonical naming scheme for Dapr pub/sub topics and state keys across all Virtufin services. Defines the routing taxonomy, CloudEvents envelope contract, state-key taxonomy, and scenario-id axis. Subsumes the prior <code>cross-cutting</code> \"Per-service Pubsub Topics\" requirement.</p> <p>Related specs: - Scenarios \u2014 scenario registry, triplet definition, <code>LIVE</code> bootstrap. - Cross-cutting \u2014 API-mediated pub/sub (envelope contract travels through the API gRPC). - Worker Management \u2014 worker + connection lifecycle events (<code>workmanager.lifecycle</code>, <code>websocketmanager.lifecycle</code>). - Proto-to-Client Mapping \u2014 code-generation conventions for the gRPC types referenced here.</p>"},{"location":"specs/pubsub-topics/#architecture","title":"Architecture","text":"<p>Two orthogonal routing axes plus a scenario-agnostic infrastructure axis:</p> Axis Pattern Scope Example Market data world <code>act.exchange.&lt;venue&gt;.&lt;entity&gt;.&lt;event&gt;[.&lt;id&gt;]</code> or <code>hyp.&lt;name&gt;.exchange.&lt;venue&gt;.&lt;entity&gt;.&lt;event&gt;[.&lt;id&gt;]</code> data-universe-bound; shared across scenarios <code>act.exchange.binance.orderbook.update.BTCUSDT</code> Scenario world <code>sc.&lt;scenarioId&gt;.&lt;domain&gt;.&lt;entity&gt;.&lt;event&gt;[.&lt;id&gt;]</code> triplet-bound (market + portfolio + strategy) <code>sc.LIVE.trading.order.filled.&lt;order_id&gt;</code> Tier 0 / infrastructure <code>&lt;service&gt;.&lt;category&gt;</code> scenario-agnostic <code>websocketmanager.lifecycle</code>, <code>state.change</code> <p>Rationale: - Market data is world-scoped because it depends on the data universe (live vs hypothetical replay) and is shareable across scenarios. Two paper runs sharing the same Binance feed do not double-publish ticks. - Trading / position / risk / pnl are scenario-scoped because each depends on the full triplet. P&amp;L in particular is meaningless without knowing all three legs. - Tier 0 is scenario-agnostic because infra lifecycle (workers up, connections reconnecting) is a deployment concern, not a trading concern.</p> <p><code>act</code> and <code>hyp.&lt;name&gt;</code> are both top-level world markers. <code>act</code> is the single actual reality (<code>act.exchange.*</code>); a hypothetical world carries a <code>&lt;name&gt;</code> segment identifying which hypothetical reality is in play (<code>hyp.BACKTEST.exchange.*</code>). Bare <code>hyp</code> (no name) is never a valid topic segment \u2014 it denotes the category of all hypothetical worlds, not a specific one (see Scenarios spec \u00a7Named Hypothetical Worlds). The data universe (<code>ALL</code>, <code>EQ_EUROPE</code>, <code>EQ_2008_2009</code>, \u2026) is not encoded in the topic \u2014 it travels in the <code>market.universe</code> CloudEvent envelope extension (see below) and on the producing WebSocketManager connection's <code>universe</code> tag. The scenario triplet (market + portfolio + strategy legs) lives in the scenario registry.</p>"},{"location":"specs/pubsub-topics/#requirements","title":"Requirements","text":""},{"location":"specs/pubsub-topics/#requirement-topic-patterns","title":"Requirement: Topic Patterns","text":"<p>Services SHALL publish to and subscribe from topics matching one of the following patterns, to keep the taxonomy consistent and routable across the platform. This is a naming convention, not an enforced contract: no service (publisher, subscriber, or <code>virtufin-api</code> itself) validates or rejects topics against these patterns before publish -- see cross-cutting spec for why no enforcement point owns this check.</p>"},{"location":"specs/pubsub-topics/#scenario-tier-0-infrastructure-topic","title":"Scenario: Tier 0 / infrastructure topic","text":"<ul> <li>WHEN a service publishes a lifecycle / infra event</li> <li>THEN the topic SHALL match <code>&lt;service&gt;.&lt;category&gt;</code> (e.g. <code>websocketmanager.lifecycle</code>, <code>workmanager.lifecycle</code>)</li> <li>AND the <code>&lt;category&gt;</code> SHALL be a domain noun (<code>lifecycle</code>, <code>infra</code>, <code>audit</code>) \u2014 not a state name</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-market-data-topic-real","title":"Scenario: Market data topic (real)","text":"<ul> <li>WHEN a service publishes real (live) market data</li> <li>THEN the topic SHALL match <code>act.exchange.&lt;venue&gt;.&lt;entity&gt;.&lt;event&gt;[.&lt;entity_id&gt;]</code></li> <li>AND the data universe SHALL be carried in the <code>market.universe</code> envelope extension (<code>ALL</code> for unrestricted, <code>EQ_EUROPE</code> for a live subset) \u2014 it is not a topic segment</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-market-data-topic-hypothetical","title":"Scenario: Market data topic (hypothetical)","text":"<ul> <li>WHEN a service publishes hypothetical (replay / backtest) market data</li> <li>THEN the topic SHALL match <code>hyp.&lt;name&gt;.exchange.&lt;venue&gt;.&lt;entity&gt;.&lt;event&gt;[.&lt;entity_id&gt;]</code></li> <li>AND <code>&lt;name&gt;</code> SHALL identify the specific hypothetical world (see Scenarios spec \u00a7Named Hypothetical Worlds) \u2014 bare <code>hyp</code> (no name) SHALL NOT appear in a topic</li> <li>AND the data universe SHALL be carried in the <code>market.universe</code> envelope extension, independent of <code>&lt;name&gt;</code> \u2014 the same universe MAY appear under different hypothetical worlds and vice versa</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-trading-topic","title":"Scenario: Trading topic","text":"<ul> <li>WHEN a service publishes a trading-domain event (order lifecycle, trade lifecycle)</li> <li>THEN the topic SHALL match <code>sc.&lt;scenarioId&gt;.trading.&lt;order|trade&gt;.&lt;event&gt;.&lt;id&gt;</code></li> <li>AND <code>&lt;scenarioId&gt;</code> SHALL be either the reserved <code>LIVE</code> or a registered opaque scenario ID</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-position-topic","title":"Scenario: Position topic","text":"<ul> <li>WHEN a service publishes a position-domain event</li> <li>THEN the topic SHALL match <code>sc.&lt;scenarioId&gt;.position.&lt;symbol&gt;.&lt;event&gt;</code> or <code>sc.&lt;scenarioId&gt;.position.portfolio.&lt;event&gt;</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-risk-topic","title":"Scenario: Risk topic","text":"<ul> <li>WHEN a service publishes a risk-domain event</li> <li>THEN the topic SHALL match <code>sc.&lt;scenarioId&gt;.risk.&lt;metric&gt;.&lt;event&gt;</code> (consolidated; per-symbol routing via <code>subject</code>)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-pl-topic","title":"Scenario: P&amp;L topic","text":"<ul> <li>WHEN a service publishes a P&amp;L-domain event</li> <li>THEN the topic SHALL match <code>sc.&lt;scenarioId&gt;.pnl.&lt;metric&gt;.&lt;event&gt;</code> (consolidated; per-symbol routing via <code>subject</code>)</li> </ul>"},{"location":"specs/pubsub-topics/#requirement-no-topic-or-scenario-state-validation-anywhere","title":"Requirement: No Topic or Scenario-State Validation Anywhere","text":"<p>No service \u2014 publisher, subscriber, <code>virtufin-api</code>'s <code>Pubsub.PublishEvent</code> handler, or any shared helper \u2014 SHALL validate a topic's shape against the patterns above, or a scenario ID's registration/lifecycle status, before publish. This was previously a per-service inline requirement (added by the 2026-07-09 pubsub-pattern-fixes change); reversed because it duplicated Virtufin domain vocabulary (universes, scenario IDs, lifecycle status) into every publishing service's own code, including services whose charter is explicitly \"pure infrastructure, no domain logic\" (WebSocketManager, WorkManager). <code>virtufin-websocketmanager</code>'s <code>TopicValidator</code> was the one place this was actually implemented; it has been removed in full.</p>"},{"location":"specs/pubsub-topics/#scenario-malformed-or-unregistered-topic-is-published-anyway","title":"Scenario: Malformed or unregistered topic is published anyway","text":"<ul> <li>WHEN a service publishes to a topic that matches no pattern in this spec, or to an <code>sc.&lt;scenarioId&gt;.*</code> topic whose <code>scenarioId</code> is not registered (or is <code>paused</code>/<code>archived</code>/<code>deleted</code>)</li> <li>THEN the publish SHALL proceed; Dapr/NATS is the only enforcement point, accepting whatever it considers a legal topic string</li> <li>AND detecting and correcting a bad topic or stale scenario reference is a monitoring/observability concern (dead-letter, consumer-side logging), not a publish-time rejection</li> </ul>"},{"location":"specs/pubsub-topics/#requirement-cloudevents-envelope","title":"Requirement: CloudEvents Envelope","text":"<p>Every event SHALL be a CloudEvents v1.0 envelope. The CE base attributes (<code>id</code>, <code>source</code>, <code>type</code>, <code>time</code>, <code>subject</code> when applicable, <code>specversion</code>, <code>datacontenttype</code>) SHALL be carried as <code>ce-</code> prefixed metadata; <code>data</code> SHALL be the JSON payload.</p>"},{"location":"specs/pubsub-topics/#scenario-ce-type-format","title":"Scenario: ce-type format","text":"<ul> <li>WHEN a service publishes an event</li> <li>THEN <code>ce-type</code> SHALL match one of:</li> <li>Tier 0: <code>com.virtufin.&lt;service&gt;.&lt;category&gt;.&lt;event&gt;</code> (e.g. <code>com.virtufin.websocketmanager.lifecycle.connected</code>)</li> <li>Market data: <code>com.virtufin.exchange.&lt;venue&gt;.&lt;entity&gt;.&lt;event&gt;</code> (e.g. <code>com.virtufin.exchange.binance.orderbook.updated</code>)</li> <li>Trading: <code>com.virtufin.trading.&lt;order|trade&gt;.&lt;event&gt;</code> (e.g. <code>com.virtufin.trading.order.filled</code>)</li> <li>Position: <code>com.virtufin.position.&lt;event&gt;</code> (e.g. <code>com.virtufin.position.flipped</code>)</li> <li>Risk: <code>com.virtufin.risk.&lt;event&gt;</code> (e.g. <code>com.virtufin.risk.exposure.updated</code>)</li> <li>P&amp;L: <code>com.virtufin.pnl.&lt;event&gt;</code> (e.g. <code>com.virtufin.pnl.realized.updated</code>)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-ce-source-format","title":"Scenario: ce-source format","text":"<ul> <li>WHEN a service publishes an event</li> <li>THEN <code>ce-source</code> SHALL be the publishing service's URN (e.g. <code>urn:com.virtufin.websocketmanager</code>, <code>urn:com.virtufin.trading-engine</code>)</li> <li>AND the URN SHALL be stable per service (not per instance)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-subject-attribute-per-entity-events","title":"Scenario: subject attribute (per-entity events)","text":"<ul> <li>WHEN a service publishes an event referring to a specific entity (order, trade, position, symbol)</li> <li>THEN <code>ce-subject</code> SHALL be set to <code>&lt;entity_type&gt;/&lt;entity_id&gt;</code> (e.g. <code>order/&lt;order_id&gt;</code>, <code>position/BTCUSDT</code>, <code>currency_pair/BTCUSDT</code>)</li> <li>AND consolidated topics (risk, pnl, portfolio) SHALL also use <code>subject</code> to enable per-entity routing without topic proliferation</li> </ul>"},{"location":"specs/pubsub-topics/#requirement-common-envelope-extensions","title":"Requirement: Common Envelope Extensions","text":"<p>Every Tier 1 event SHALL carry the following CloudEvents extensions. Tier 0 events MAY carry <code>correlationid</code> only.</p> Extension Type Required Notes <code>correlationid</code> UUID Tier 1 yes; Tier 0 no Ties fills back to originating order across services <code>scenarioid</code> string Tier 1 yes Opaque registry key; <code>LIVE</code> for production <code>runid</code> UUID Tier 1 yes when parallel backtests Disambiguates concurrent runs sharing a scenario <code>exchange</code> string Tier 1 yes (when applicable) Exchange name (<code>binance</code>, <code>coinbase</code>, \u2026); distinct from <code>ce-source</code> <code>clocktype</code> <code>wall</code> | <code>historical</code> Tier 1 yes <code>wall</code> for prod + paper; <code>historical</code> for backtest replay <code>eventtime</code> RFC3339 Tier 1 yes (= <code>ce-time</code>) Logical event time matching <code>clocktype</code> <code>walltime</code> RFC3339 Tier 1 yes Always real clock at publish \u2014 for tracing / logging <code>market.world</code> <code>act</code> | <code>hyp.&lt;name&gt;</code> Tier 1 yes Triplet leg (world) for market <code>market.universe</code> string Tier 1 yes Triplet leg (universe) for market; present for both <code>act</code> and <code>hyp.&lt;name&gt;</code> (use <code>\"ALL\"</code> for unrestricted) <code>portfolio.world</code> <code>act</code> | <code>hyp.&lt;name&gt;</code> Tier 1 yes Triplet leg for portfolio <code>strategy.world</code> <code>act</code> | <code>hyp.&lt;name&gt;</code> Tier 1 yes Triplet leg for strategy"},{"location":"specs/pubsub-topics/#scenario-envelope-on-a-production-fill-event","title":"Scenario: Envelope on a production fill event","text":"<ul> <li>WHEN a service publishes <code>sc.LIVE.trading.order.filled.&lt;order_id&gt;</code></li> <li>THEN the CloudEvent extensions SHALL include <code>scenarioid=LIVE</code>, <code>runid=&lt;uuid&gt;</code>, <code>correlationid=&lt;uuid&gt;</code>, <code>exchange=binance</code>, <code>clocktype=wall</code>, <code>eventtime=&lt;rfc3339&gt;</code>, <code>walltime=&lt;rfc3339&gt;</code>, <code>market.world=act</code>, <code>market.universe=ALL</code>, <code>portfolio.world=act</code>, <code>strategy.world=act</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-envelope-on-a-backtest-fill-event","title":"Scenario: Envelope on a backtest fill event","text":"<ul> <li>WHEN a backtest engine publishes <code>sc.S7F3A9B.trading.order.filled.&lt;order_id&gt;</code></li> <li>THEN the envelope SHALL include <code>scenarioid=S7F3A9B</code>, <code>clocktype=historical</code>, <code>eventtime=&lt;historical rfc3339&gt;</code> (the replayed tick's timestamp), <code>walltime=&lt;real rfc3339&gt;</code> (now), <code>market.world=hyp.BACKTEST</code>, <code>market.universe=EQ_2008_2009</code>, <code>portfolio.world=hyp.BACKTEST</code>, <code>strategy.world=hyp.BACKTEST</code></li> <li>AND <code>runid</code> SHALL differ for concurrent backtests against the same scenario</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-clocktype-rationale","title":"Scenario: clocktype rationale","text":"<ul> <li>WHEN a strategy computes a time-series indicator (e.g. 20-period moving average) from a backtest stream</li> <li>THEN it SHALL use <code>eventtime</code> (which is <code>historical</code> for backtests) to maintain correct indicator semantics</li> <li>AND it SHALL use <code>walltime</code> only for tracing, logging, and alerting</li> </ul>"},{"location":"specs/pubsub-topics/#requirement-trade-lifecycle-events","title":"Requirement: Trade Lifecycle Events","text":"<p>The trading domain SHALL model the lifecycle: <code>submitted \u2192 accepted \u2192 (partially_filled \u2192)* filled \u2192 cleared \u2192 settled</code>. For Binance CEX, <code>cleared</code> and <code>settled</code> collapse to near-instantaneous but the events remain distinct for future DeFi / TradFi integration.</p>"},{"location":"specs/pubsub-topics/#scenario-order-submitted","title":"Scenario: Order submitted","text":"<ul> <li>WHEN a service submits an order to an exchange</li> <li>THEN it SHALL publish <code>sc.&lt;scenarioId&gt;.trading.order.submitted.&lt;order_id&gt;</code> with <code>ce-type = com.virtufin.trading.order.submitted</code></li> <li>AND the data payload SHALL include <code>order_id</code>, <code>side</code> (<code>buy</code>|<code>sell</code>), <code>type</code> (<code>market</code>|<code>limit</code>), <code>qty</code>, <code>price?</code>, <code>venue</code>, <code>submitted_at</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-order-accepted","title":"Scenario: Order accepted","text":"<ul> <li>WHEN the venue acknowledges the order</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.trading.order.accepted.&lt;order_id&gt;</code> with <code>venue_order_id</code>, <code>accepted_at</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-order-rejected","title":"Scenario: Order rejected","text":"<ul> <li>WHEN an order is rejected by pre-trade risk or the venue</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.trading.order.rejected.&lt;order_id&gt;</code> with <code>reject_reason</code>, <code>reject_source</code> (<code>risk</code>|<code>venue</code>)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-order-amended-mutation-not-transition","title":"Scenario: Order amended (mutation, not transition)","text":"<ul> <li>WHEN an order's price or quantity is changed in-place</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.trading.order.amended.&lt;order_id&gt;</code> with <code>new_price?</code>, <code>new_qty?</code>, <code>amended_at</code></li> <li>AND the order's status SHALL remain unchanged (<code>pending</code> or <code>partially_filled</code>)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-order-cancelled","title":"Scenario: Order cancelled","text":"<ul> <li>WHEN an order is cancelled by the user or system</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.trading.order.cancelled.&lt;order_id&gt;</code> with <code>cancelled_at</code>, <code>cancelled_by</code> (<code>user</code>|<code>system</code>|<code>venue</code>)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-order-expired","title":"Scenario: Order expired","text":"<ul> <li>WHEN an order's time-in-force is reached without a fill</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.trading.order.expired.&lt;order_id&gt;</code> with <code>expired_at</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-order-filled-workhorse-event","title":"Scenario: Order filled (workhorse event)","text":"<ul> <li>WHEN a partial or full fill is received from the venue (Binance WebSocket fill callback)</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.trading.order.filled.&lt;order_id&gt;</code> with <code>fill_id</code>, <code>qty_filled</code>, <code>price</code>, <code>fee</code>, <code>liquidity</code> (<code>maker</code>|<code>taker</code>), <code>remaining_qty</code>, <code>filled_at</code>, <code>realized_pnl?</code> (optional, included if computed at fill time)</li> <li>AND for partial fills, <code>remaining_qty</code> SHALL be the post-fill remaining quantity (decreasing across events)</li> <li>AND when <code>remaining_qty == 0</code>, the order is implicitly fully filled (no separate <code>order.completed</code> event)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-trade-cleared","title":"Scenario: Trade cleared","text":"<ul> <li>WHEN an order is fully filled and counterparty obligations are netted</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.trading.trade.cleared.&lt;trade_id&gt;</code> with <code>trade_id</code>, <code>order_id</code>, <code>cleared_at</code></li> <li>AND this event SHALL fire exactly once per trade (not per fill)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-trade-settled","title":"Scenario: Trade settled","text":"<ul> <li>WHEN asset transfer for a trade is final</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.trading.trade.settled.&lt;trade_id&gt;</code> with <code>trade_id</code>, <code>order_id</code>, <code>settled_at</code>, <code>tx_hash?</code> (null for CEX, populated for on-chain settlement)</li> <li>AND for Binance CEX, <code>cleared</code> and <code>settled</code> SHALL be emitted back-to-back by the publisher</li> <li>AND for DeFi / TradFi integration, the gap between <code>cleared</code> and <code>settled</code> may span minutes to days</li> </ul>"},{"location":"specs/pubsub-topics/#requirement-position-lifecycle-events","title":"Requirement: Position Lifecycle Events","text":"<p>Position state SHALL be derived from signed size plus a liquidation sub-state. <code>position.flipped</code> SHALL be a first-class atomic event because the risk engine needs direction-change awareness without a flat interval.</p>"},{"location":"specs/pubsub-topics/#scenario-position-opened","title":"Scenario: Position opened","text":"<ul> <li>WHEN a new position is established (size transitions from <code>0</code> to non-zero)</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.position.&lt;symbol&gt;.opened</code> with <code>side</code> (<code>long</code>|<code>short</code>), <code>qty</code>, <code>avg_price</code>, <code>opened_at</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-position-increased","title":"Scenario: Position increased","text":"<ul> <li>WHEN an existing position grows in size (same side)</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.position.&lt;symbol&gt;.increased</code> with <code>new_qty</code>, <code>new_avg_price</code>, <code>delta_qty</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-position-decreased","title":"Scenario: Position decreased","text":"<ul> <li>WHEN an existing position shrinks in size (same side, partial close)</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.position.&lt;symbol&gt;.decreased</code> with <code>new_qty</code>, <code>realized_pnl</code>, <code>delta_qty</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-position-flipped-atomic-direction-change","title":"Scenario: Position flipped (atomic direction change)","text":"<ul> <li>WHEN a position changes from <code>long</code> to <code>short</code> or vice versa in a single fill sequence</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.position.&lt;symbol&gt;.flipped</code> with <code>from_side</code>, <code>to_side</code>, <code>qty</code>, <code>price</code>, <code>order_id</code>, <code>realized_pnl</code></li> <li>AND this event SHALL be atomic \u2014 no intermediate <code>position.closed</code> + <code>position.opened</code> sequence</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-position-closed","title":"Scenario: Position closed","text":"<ul> <li>WHEN a position returns to flat (size = 0) via user close</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.position.&lt;symbol&gt;.closed</code> with <code>realized_pnl</code>, <code>closed_at</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-position-liquidated-forced","title":"Scenario: Position liquidated (forced)","text":"<ul> <li>WHEN a position is force-closed by the venue</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.position.&lt;symbol&gt;.liquidated</code> with <code>liquidation_price</code>, <code>realized_pnl</code>, <code>liquidated_at</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-liquidation-warning-signal-not-transition","title":"Scenario: Liquidation warning (signal, not transition)","text":"<ul> <li>WHEN margin ratio crosses a warning threshold</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.position.&lt;symbol&gt;.liquidation.warning</code> with <code>margin_ratio</code>, <code>threshold</code></li> <li>AND the position's primary state SHALL NOT change; only the <code>liquidation_status</code> sub-state becomes <code>warning</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-position-adjustment-corporate-action","title":"Scenario: Position adjustment (corporate action)","text":"<ul> <li>WHEN a corporate action (split, dividend, symbol change) mutates a position's size or avg_price</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.position.&lt;symbol&gt;.adjustment</code> with <code>adjustment_type</code> (<code>split</code>|<code>dividend</code>|<code>symbol_change</code>), <code>new_qty</code>, <code>new_avg_price</code>, <code>corporate_action_id</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-portfolio-snapshot","title":"Scenario: Portfolio snapshot","text":"<ul> <li>WHEN a full portfolio snapshot is published (consolidated)</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.position.portfolio.snapshot</code> with <code>total_value</code>, <code>total_pnl</code>, <code>position_count</code></li> </ul>"},{"location":"specs/pubsub-topics/#requirement-market-data-events","title":"Requirement: Market Data Events","text":"<p>Market data events SHALL live under the world-scoped pattern (<code>act.exchange.&lt;venue&gt;.*</code> or <code>hyp.&lt;name&gt;.exchange.&lt;venue&gt;.*</code>) \u2014 <code>hyp</code> carries one more segment than <code>act</code> (the <code>&lt;name&gt;</code> slot); neither carries the data universe, which travels in the <code>market.universe</code> envelope extension. Per-currency-pair cardinality SHALL be used for <code>orderbook.*</code>, <code>trade.tape</code>, <code>ticker.update</code>, <code>funding.rate</code>, <code>mark.price</code>, <code>ohlcv.candle.*</code>. Consolidated (no per-pair segment) for <code>instrument.info</code>.</p>"},{"location":"specs/pubsub-topics/#scenario-order-book-snapshot","title":"Scenario: Order book snapshot","text":"<ul> <li>WHEN the venue publishes a full order book snapshot at depth N</li> <li>THEN the service SHALL publish <code>act\\|hyp.&lt;name&gt;.exchange.&lt;venue&gt;.orderbook.snapshot.&lt;currency_pair&gt;</code> (act omits <code>&lt;name&gt;</code>) with <code>bids[]</code>, <code>asks[]</code>, <code>last_update_id</code>, <code>snapshot_at</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-order-book-update-delta","title":"Scenario: Order book update (delta)","text":"<ul> <li>WHEN the venue publishes an L2/L3 order book delta</li> <li>THEN the service SHALL publish <code>act\\|hyp.&lt;name&gt;.exchange.&lt;venue&gt;.orderbook.update.&lt;currency_pair&gt;</code> (act omits <code>&lt;name&gt;</code>) with <code>bids_delta[]</code>, <code>asks_delta[]</code>, <code>first_update_id</code>, <code>last_update_id</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-public-trade-tape","title":"Scenario: Public trade tape","text":"<ul> <li>WHEN a public trade occurs on the venue</li> <li>THEN the service SHALL publish <code>act\\|hyp.&lt;name&gt;.exchange.&lt;venue&gt;.trade.tape.&lt;currency_pair&gt;</code> (act omits <code>&lt;name&gt;</code>) with <code>trade_id</code>, <code>price</code>, <code>qty</code>, <code>side</code>, <code>trade_time</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-ticker-update","title":"Scenario: Ticker update","text":"<ul> <li>WHEN the venue publishes 24h rolling stats</li> <li>THEN the service SHALL publish <code>act\\|hyp.&lt;name&gt;.exchange.&lt;venue&gt;.ticker.update.&lt;currency_pair&gt;</code> (act omits <code>&lt;name&gt;</code>) with <code>last_price</code>, <code>volume_24h</code>, <code>change_24h_pct</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-funding-rate","title":"Scenario: Funding rate","text":"<ul> <li>WHEN the venue publishes a perpetual funding rate tick</li> <li>THEN the service SHALL publish <code>act\\|hyp.&lt;name&gt;.exchange.&lt;venue&gt;.funding.rate.&lt;currency_pair&gt;</code> (act omits <code>&lt;name&gt;</code>) with <code>funding_rate</code>, <code>next_funding_time</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-mark-price","title":"Scenario: Mark price","text":"<ul> <li>WHEN the venue publishes a mark price update (derivatives)</li> <li>THEN the service SHALL publish <code>act\\|hyp.&lt;name&gt;.exchange.&lt;venue&gt;.mark.price.&lt;currency_pair&gt;</code> (act omits <code>&lt;name&gt;</code>) with <code>mark_price</code>, <code>index_price</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-ohlcv-candle","title":"Scenario: OHLCV candle","text":"<ul> <li>WHEN a candle interval closes</li> <li>THEN the service SHALL publish <code>act\\|hyp.&lt;name&gt;.exchange.&lt;venue&gt;.ohlcv.candle.&lt;interval&gt;.&lt;currency_pair&gt;</code> (act omits <code>&lt;name&gt;</code>) with <code>open</code>, <code>high</code>, <code>low</code>, <code>close</code>, <code>volume</code>, <code>candle_close_time</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-instrument-info","title":"Scenario: Instrument info","text":"<ul> <li>WHEN instrument metadata changes (lot size, tick size, status)</li> <li>THEN the service SHALL publish <code>act\\|hyp.&lt;name&gt;.exchange.&lt;venue&gt;.instrument.info</code> (act omits <code>&lt;name&gt;</code>) with <code>symbol</code>, <code>status</code>, <code>base_asset</code>, <code>quote_asset</code>, <code>lot_size</code>, <code>tick_size</code></li> </ul>"},{"location":"specs/pubsub-topics/#requirement-risk-events","title":"Requirement: Risk Events","text":"<p>Risk events SHALL be consolidated (one topic per metric). Per-symbol routing SHALL use <code>ce-subject</code>.</p>"},{"location":"specs/pubsub-topics/#scenario-exposure-update","title":"Scenario: Exposure update","text":"<ul> <li>WHEN risk greeks (delta / gamma / vega / theta) are recomputed</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.risk.exposure.update</code> with <code>greeks: { delta, gamma, vega, theta }</code>, <code>underlyings[]</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-margin-update","title":"Scenario: Margin update","text":"<ul> <li>WHEN margin used or available changes</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.risk.margin.update</code> with <code>margin_used</code>, <code>margin_available</code>, <code>margin_ratio</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-drawdown-alert","title":"Scenario: Drawdown alert","text":"<ul> <li>WHEN drawdown crosses a configured threshold</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.risk.drawdown.alert</code> with <code>drawdown_pct</code>, <code>threshold_pct</code>, <code>peak_value</code>, <code>current_value</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-limit-breach","title":"Scenario: Limit breach","text":"<ul> <li>WHEN a configured risk limit is breached</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.risk.limit.breach</code> with <code>limit_name</code>, <code>limit_value</code>, <code>actual_value</code>, <code>severity</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-scenario-pl-stress-test","title":"Scenario: Scenario P&amp;L (stress test)","text":"<ul> <li>WHEN a stress / scenario P&amp;L computation completes</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.risk.scenario.pnl</code> with <code>scenario_name</code>, <code>pnl</code>, <code>scenario_inputs</code></li> </ul>"},{"location":"specs/pubsub-topics/#requirement-pl-events","title":"Requirement: P&amp;L Events","text":"<p>P&amp;L events SHALL be consolidated. The full triplet context is carried in the envelope (not the topic) because P&amp;L is meaningless without it.</p>"},{"location":"specs/pubsub-topics/#scenario-realized-pl-update","title":"Scenario: Realized P&amp;L update","text":"<ul> <li>WHEN realized P&amp;L is recomputed (e.g. on fill or position close)</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.pnl.realized.update</code> with <code>realized_pnl</code>, <code>by_symbol: { &lt;symbol&gt;: &lt;pnl&gt; }</code>, <code>period</code> (<code>daily</code>|<code>session</code>|<code>cumulative</code>)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-unrealized-pl-update","title":"Scenario: Unrealized P&amp;L update","text":"<ul> <li>WHEN unrealized P&amp;L is recomputed (mark-to-market)</li> <li>THEN the service SHALL publish <code>sc.&lt;scenarioId&gt;.pnl.unrealized.update</code> with <code>unrealized_pnl</code>, <code>by_symbol: { &lt;symbol&gt;: &lt;pnl&gt; }</code>, <code>mark_prices: { &lt;symbol&gt;: &lt;price&gt; }</code></li> </ul>"},{"location":"specs/pubsub-topics/#requirement-tier-0-infrastructure-events","title":"Requirement: Tier 0 / Infrastructure Events","text":"<p>Tier 0 events SHALL be scenario-agnostic and SHALL follow the <code>&lt;service&gt;.&lt;category&gt;</code> pattern. Access control SHALL move to NATS account / subject permissions (no <code>public</code> / <code>internal</code> segment in the topic).</p>"},{"location":"specs/pubsub-topics/#scenario-worker-lifecycle-event","title":"Scenario: Worker lifecycle event","text":"<ul> <li>WHEN a worker changes state (created, started, online, degraded, stopping, stopped, failed)</li> <li>THEN the WorkManager SHALL publish to <code>workmanager.lifecycle</code> with <code>ce-type = com.virtufin.workmanager.lifecycle.&lt;state&gt;</code></li> <li>AND the event SHALL carry <code>worker_id</code>, <code>group?</code>, <code>topic</code>, and state-specific fields (<code>error_type?</code>, <code>error_message?</code>)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-connection-lifecycle-event","title":"Scenario: Connection lifecycle event","text":"<ul> <li>WHEN a WebSocket connection changes state (registered, connecting, connected, degraded, reconnecting, closed, failed)</li> <li>THEN the WebSocketManager SHALL publish to <code>websocketmanager.lifecycle</code> with <code>ce-type = com.virtufin.websocketmanager.lifecycle.&lt;state&gt;</code></li> <li>AND the event SHALL carry <code>connection_id</code>, <code>url</code>, <code>instance_id</code>, and state-specific fields</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-infra-events-connection-worker-detail","title":"Scenario: Infra events (connection / worker detail)","text":"<ul> <li>WHEN finer-grained infra events are needed (heartbeats, grouplock changes)</li> <li>THEN the service SHALL publish to <code>&lt;service&gt;.infra.&lt;sub_category&gt;</code> (e.g. <code>websocketmanager.infra.connection.heartbeat</code>, <code>workmanager.infra.worker.heartbeat</code>, <code>workmanager.infra.grouplock</code>)</li> </ul>"},{"location":"specs/pubsub-topics/#requirement-cardinality-rule","title":"Requirement: Cardinality Rule","text":"<p>Services SHALL prefer consolidated topics (one topic, per-entity routing via <code>ce-subject</code>) over per-entity topics (<code>.&lt;entity_id&gt;</code> segment). Per-entity topics SHALL be used only when subscribers need broker-level per-entity subscription or routing.</p>"},{"location":"specs/pubsub-topics/#scenario-per-entity-topic-justified","title":"Scenario: Per-entity topic justified","text":"<ul> <li>WHEN subscribers need to subscribe at the broker level to a single entity (e.g., fills for one specific order, order book for one specific pair)</li> <li>THEN the topic SHALL carry the entity ID segment (<code>trading.order.filled.&lt;order_id&gt;</code>, <code>exchange.binance.orderbook.update.&lt;currency_pair&gt;</code>)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-consolidated-topic-preferred","title":"Scenario: Consolidated topic preferred","text":"<ul> <li>WHEN subscribers typically want aggregate views (P&amp;L totals, risk limits, portfolio snapshots)</li> <li>THEN the topic SHALL be consolidated and <code>ce-subject</code> SHALL carry the entity reference (<code>risk.exposure.update</code> with <code>subject=position/BTCUSDT</code>)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-act-and-hyp-carry-no-universe-segment","title":"Scenario: act and hyp carry no universe segment","text":"<ul> <li>WHEN a topic uses the <code>act.exchange.&lt;venue&gt;.*</code> or <code>hyp.&lt;name&gt;.exchange.&lt;venue&gt;.*</code> pattern</li> <li>THEN the <code>hyp</code> form SHALL have exactly one more segment than the <code>act</code> form (the <code>&lt;name&gt;</code> slot)</li> <li>AND the data universe SHALL NOT appear in either topic \u2014 it travels in the <code>market.universe</code> envelope extension, not as a topic segment</li> <li>AND brokers and wildcard subscriptions SHALL NOT assume segment-count parity between the two forms \u2014 subscribe to <code>act.exchange.*</code> and <code>hyp.*.exchange.*</code> as two separate patterns rather than one shared positional pattern</li> </ul>"},{"location":"specs/pubsub-topics/#requirement-nats-permissions-replacing-pubprv","title":"Requirement: NATS Permissions (replacing pub/prv)","text":"<p>Access control SHALL be enforced via NATS account / subject permissions, not via topic naming. <code>pub</code> / <code>prv</code> segments are NOT part of any topic pattern.</p>"},{"location":"specs/pubsub-topics/#scenario-market-data-permissions","title":"Scenario: Market data permissions","text":"<ul> <li>WHEN NATS permissions are configured</li> <li>THEN <code>act.exchange.*</code> SHALL allow publishers = exchange connectors, subscribers = all trading / risk / backtest services</li> <li>AND <code>hyp.*.exchange.*</code> SHALL allow publishers = backtest engines, subscribers = strategy services (no production services) \u2014 note the extra <code>*</code> for the <code>&lt;name&gt;</code> segment versus <code>act</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-scenario-event-permissions","title":"Scenario: Scenario event permissions","text":"<ul> <li>WHEN NATS permissions are configured</li> <li>THEN <code>sc.*.trading.*</code> SHALL allow publishers = trading / strategy services, subscribers = risk / position / pnl / dashboards</li> <li>AND <code>sc.*.position.*</code> SHALL allow publishers = trading services, subscribers = risk / pnl</li> <li>AND <code>sc.*.risk.*</code> and <code>sc.*.pnl.*</code> SHALL allow publishers = risk / pnl services, subscribers = dashboards / alerting</li> <li>AND <code>sc.LIVE.*</code> writes SHALL require elevated permissions (production role)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-tier-0-permissions","title":"Scenario: Tier 0 permissions","text":"<ul> <li>WHEN NATS permissions are configured</li> <li>THEN <code>&lt;service&gt;.lifecycle</code> and <code>&lt;service&gt;.infra.*</code> SHALL allow publishers = owning service, subscribers = all services</li> </ul>"},{"location":"specs/pubsub-topics/#requirement-state-keys","title":"Requirement: State Keys","text":"<p>State keys SHALL share the topic taxonomy. After the <code>&lt;app-name&gt;||</code> prefix:</p> Namespace Pattern Example Tier 0 / infra <code>&lt;service&gt;.&lt;entity&gt;[.&lt;id&gt;]</code> <code>workmanager.worker.&lt;worker_id&gt;</code> Market data cache <code>act\\|hyp.&lt;name&gt;.exchange.&lt;venue&gt;.&lt;entity&gt;[.&lt;id&gt;]</code> <code>act.exchange.binance.orderbook.BTCUSDT</code> (act omits <code>&lt;name&gt;</code>) Scenario state <code>sc.&lt;scenarioId&gt;.&lt;domain&gt;.&lt;entity&gt;[.&lt;id&gt;]</code> <code>sc.LIVE.trading.order.&lt;order_id&gt;</code> Scenario registry <code>scenario.&lt;scenarioId&gt;</code> <code>scenario.LIVE</code>"},{"location":"specs/pubsub-topics/#scenario-order-state-key","title":"Scenario: Order state key","text":"<ul> <li>WHEN a service writes order state</li> <li>THEN the key SHALL be <code>sc.&lt;scenarioId&gt;.trading.order.&lt;order_id&gt;</code></li> <li>AND the value SHALL be JSON <code>{ status, side, type, qty, remaining_qty, avg_price, venue, created_at, updated_at }</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-trade-state-key","title":"Scenario: Trade state key","text":"<ul> <li>WHEN a service writes trade state</li> <li>THEN the key SHALL be <code>sc.&lt;scenarioId&gt;.trading.trade.&lt;trade_id&gt;</code></li> <li>AND the value SHALL be JSON <code>{ status, order_id, cleared_at?, settled_at?, tx_hash? }</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-position-state-key","title":"Scenario: Position state key","text":"<ul> <li>WHEN a service writes position state</li> <li>THEN the key SHALL be <code>sc.&lt;scenarioId&gt;.position.&lt;symbol&gt;</code></li> <li>AND the value SHALL be JSON <code>{ size, avg_price, realized_pnl, liquidation_status, updated_at }</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-portfolio-state-key","title":"Scenario: Portfolio state key","text":"<ul> <li>WHEN a service writes portfolio state</li> <li>THEN the key SHALL be <code>sc.&lt;scenarioId&gt;.position.portfolio</code></li> <li>AND the value SHALL be JSON <code>{ total_value, total_pnl, position_count, updated_at }</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-worker-state-key","title":"Scenario: Worker state key","text":"<ul> <li>WHEN the WorkManager writes worker state</li> <li>THEN the key SHALL be <code>workmanager.worker.&lt;worker_id&gt;</code></li> <li>AND the value SHALL be JSON <code>{ status, last_heartbeat, started_at, ... }</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-connection-state-key","title":"Scenario: Connection state key","text":"<ul> <li>WHEN the WebSocketManager writes connection state</li> <li>THEN the key SHALL be <code>websocketmanager.connection.&lt;connection_id&gt;</code></li> <li>AND the value SHALL be JSON <code>{ status, last_heartbeat, connected_at, ... }</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-order-book-state-key","title":"Scenario: Order book state key","text":"<ul> <li>WHEN the WebSocketManager writes per-pair order book cache</li> <li>THEN the key SHALL be <code>act\\|hyp.&lt;name&gt;.exchange.&lt;venue&gt;.orderbook.&lt;currency_pair&gt;</code> (act omits <code>&lt;name&gt;</code>)</li> <li>AND the value SHALL be JSON <code>{ bids, asks, last_update_id }</code> (consolidated, single source of truth per venue + pair)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-scenario-registry-key","title":"Scenario: Scenario registry key","text":"<ul> <li>WHEN a service writes scenario metadata</li> <li>THEN the key SHALL be <code>scenario.&lt;scenarioId&gt;</code></li> <li>AND the value SHALL be the scenario triplet definition (see Scenarios spec)</li> </ul>"},{"location":"specs/pubsub-topics/#requirement-source-of-truth-rule","title":"Requirement: Source-of-Truth Rule","text":"<p>Each piece of derived or owned state SHALL have exactly one writer.</p>"},{"location":"specs/pubsub-topics/#scenario-single-publisher-per-status-field","title":"Scenario: Single publisher per status field","text":"<ul> <li>WHEN two publishers attempt to write the same status field on the same entity (e.g. both writing <code>order.status</code>)</li> <li>THEN this SHALL be treated as a design bug \u2014 the entity has exactly one owner for status transitions</li> <li>AND consumers SHALL treat the status field as monotonically advancing through the state machine</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-single-owner-per-derived-metric","title":"Scenario: Single owner per derived metric","text":"<ul> <li>WHEN multiple services could compute the same derived metric (e.g. portfolio total value)</li> <li>THEN exactly one aggregator SHALL own the write; other services SHALL subscribe to the topic for read access</li> </ul>"},{"location":"specs/pubsub-topics/#requirement-delete-on-deploy-no-deprecation","title":"Requirement: Delete-on-Deploy (No Deprecation)","text":"<p>Topic and state-key renames SHALL NOT carry a deprecation window. Old names SHALL be deleted on the same commit that introduces new names.</p>"},{"location":"specs/pubsub-topics/#scenario-rename-lifecycle","title":"Scenario: Rename lifecycle","text":"<ul> <li>WHEN a topic or state-key name is changed</li> <li>THEN the change SHALL be a single commit per affected repo</li> <li>AND old names SHALL be removed in the same commit (no parallel publishing under both names)</li> <li>AND the migration document SHALL list old \u2192 new mappings</li> </ul>"},{"location":"specs/pubsub-topics/#requirement-multi-publisher-tier-2","title":"Requirement: Multi-Publisher Tier 2","text":"<p>Subscribers of Tier 2 (scenario-scoped) topics SHALL NOT assume single-source. A scenario topic MAY have multiple publishers (e.g., aggregator + per-leg publisher).</p>"},{"location":"specs/pubsub-topics/#scenario-multiple-publishers-on-same-topic","title":"Scenario: Multiple publishers on same topic","text":"<ul> <li>WHEN two services publish to the same scenario topic (e.g., a strategy publishes <code>order.filled</code> and a replayer also publishes the same event during replay)</li> <li>THEN consumers SHALL handle events from either source</li> <li>AND <code>ce-source</code> SHALL identify the publishing service for filtering / dedup</li> </ul>"},{"location":"specs/pubsub-topics/#requirement-run-id-in-envelope-not-topic","title":"Requirement: Run-ID in Envelope, Not Topic","text":"<p>Backtest replay and parallel scenarios SHALL NOT introduce run-id segments into topics. Run isolation SHALL be achieved via the <code>runid</code> envelope extension.</p>"},{"location":"specs/pubsub-topics/#scenario-parallel-backtests-share-topic","title":"Scenario: Parallel backtests share topic","text":"<ul> <li>WHEN two backtests run concurrently against the same scenario (<code>sc.S7F3A9B</code>)</li> <li>THEN both SHALL publish to <code>sc.S7F3A9B.trading.*</code></li> <li>AND each event SHALL carry a distinct <code>runid</code> extension</li> <li>AND consumers that want a single backtest's events SHALL filter by <code>runid</code></li> </ul>"},{"location":"specs/pubsub-topics/#scenario-run-id-dropped-from-topic","title":"Scenario: Run-id dropped from topic","text":"<ul> <li>WHEN a service migrates from <code>binance-depth.&lt;run_id&gt;</code> to the new scheme</li> <li>THEN the topic SHALL become <code>act.exchange.binance.orderbook.update.&lt;currency_pair&gt;</code> (no run-id segment)</li> <li>AND the run-id SHALL travel in the <code>runid</code> envelope extension (or be omitted for production traffic)</li> </ul>"},{"location":"specs/pubsub-topics/#requirement-subscription-implementation-pattern","title":"Requirement: Subscription Implementation Pattern","text":"<p>Dapr-based subscribe handlers in any Virtufin service SHALL open one Dapr subscription per topic per service process and fan out the resulting event stream in-process to every interested consumer within that process. Per-call Dapr subscriptions (one per gRPC call or one per caller) are PROHIBITED because they cause the same event to be silently load-balanced across callers instead of broadcast to each.</p>"},{"location":"specs/pubsub-topics/#scenario-multiple-callers-on-one-topic","title":"Scenario: Multiple callers on one topic","text":"<ul> <li>WHEN two gRPC clients subscribe to the same topic in the same API process</li> <li>THEN each caller SHALL receive every message on that topic (true 1:many broadcast within the process)</li> <li>AND the Dapr sidecar SHALL see exactly one subscription per topic per process (competing consumers between API replicas is the only competing-consumer layer)</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-new-caller-arrives","title":"Scenario: New caller arrives","text":"<ul> <li>WHEN a third gRPC client subscribes to a topic that already has two subscribers in the same process</li> <li>THEN the existing Dapr subscription SHALL be reused; the new caller's in-process handler SHALL be added to the fan-out list; no new Dapr subscription SHALL be opened</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-last-caller-leaves","title":"Scenario: Last caller leaves","text":"<ul> <li>WHEN the last gRPC client unsubscribes from a topic</li> <li>THEN the in-process fan-out registration SHALL be removed; the Dapr subscription SHALL be disposed; subsequent callers SHALL open a new Dapr subscription on demand</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-in-process-exception-on-a-subscriber","title":"Scenario: In-process exception on a subscriber","text":"<ul> <li>WHEN an in-process subscriber's handler throws an exception while processing a fanned-out message</li> <li>THEN the broker SHALL continue delivering to the remaining subscribers (per-subscriber fault isolation)</li> <li>AND the failed subscriber SHALL be logged but SHALL NOT cause the message to be re-delivered to the surviving subscribers</li> </ul>"},{"location":"specs/pubsub-topics/#requirement-retry-policy-on-dapr-subscribe-exceptions","title":"Requirement: Retry Policy on Dapr Subscribe Exceptions","text":"<p>Services that consume pub/sub messages SHALL guarantee at-least-once processing with a bounded retry count, followed by dead-letter publication on exhaustion \u2014 messages SHALL NOT be silently lost on an uncaught processing exception. Where this guarantee is enforced depends on the consumption model:</p> <ul> <li>A service holding its own direct Dapr subscription (the classic HTTP subscribe model) SHALL return <code>TopicResponseAction.Retry</code> on uncaught exceptions so Dapr's <code>maxRedeliveryCount</code> and dead-letter topic configuration take effect. <code>Drop</code> SHALL be reserved for explicit business-logic skips (e.g., \"no local worker registered for this topic\"); it SHALL NOT be used to silently discard a message on an uncaught exception.</li> <li>A service consuming pub/sub via the streaming <code>Pubsub.Subscribe</code> gRPC API (see the \"Subscription Implementation Pattern\" requirement above) is one of potentially several in-process fan-out consumers per Dapr subscription; the Gateway acks Dapr's broker before any consumer processes the message, so broker-level redelivery is not available to it \u2014 retrying at the broker would double-process the fan-out's other, already-succeeded consumers. Such a consumer SHALL implement its own bounded-retry-then-dead-letter mechanism at the application level: retry the failed operation up to a configured attempt count with backoff, and on exhaustion publish the original message to the configured dead-letter topic (default <code>&lt;topic&gt;-dead</code>) rather than discarding it.</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-worker-throws-on-every-message","title":"Scenario: Worker throws on every message","text":"<ul> <li>WHEN a service holding its own direct Dapr subscription throws on every message it receives</li> <li>THEN Dapr SHALL re-deliver the message up to <code>maxRedeliveryCount</code> (default 3) times</li> <li>AND after the redelivery cap is reached the message SHALL be published to the configured dead-letter topic (default <code>&lt;topic&gt;-dead</code>)</li> <li>AND the handler's process SHALL NOT terminate</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-explicit-business-logic-skip","title":"Scenario: Explicit business-logic skip","text":"<ul> <li>WHEN a handler determines the message is not relevant (e.g., no local worker on the topic) and explicitly chooses to skip it</li> <li>THEN the handler MAY return <code>TopicResponseAction.Drop</code> and the message SHALL be acked with no redelivery</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-handler-times-out","title":"Scenario: Handler times out","text":"<ul> <li>WHEN a handler does not return within <code>MessageHandlingPolicy</code> timeout (default 30 s)</li> <li>THEN Dapr SHALL re-deliver the message (<code>Retry</code> policy)</li> <li>AND the timed-out handler's process SHALL remain responsive</li> </ul>"},{"location":"specs/pubsub-topics/#scenario-streaming-api-consumer-throws-on-every-message","title":"Scenario: Streaming-API consumer throws on every message","text":"<ul> <li>WHEN a consumer of the streaming <code>Pubsub.Subscribe</code> API throws processing every message on a topic</li> <li>THEN the consumer SHALL retry the operation up to its configured attempt count with backoff</li> <li>AND after attempts are exhausted the consumer SHALL publish the original message to the configured dead-letter topic (default <code>&lt;topic&gt;-dead</code>)</li> <li>AND the consumer's process SHALL NOT terminate and SHALL continue processing subsequent messages</li> </ul>"},{"location":"specs/scenarios/","title":"Scenarios","text":""},{"location":"specs/scenarios/#purpose","title":"Purpose","text":"<p>The scenario registry defines the triplet (market + portfolio + strategy legs) for every scenario ID that appears in Virtufin trading / position / risk / P&amp;L topics. The registry is the source of truth for what a scenario means; topics carry the opaque scenario ID, and the triplet lives here.</p> <p>Related specs: - Pub/Sub Topics \u2014 the <code>sc.&lt;scenarioId&gt;.*</code> topic pattern and the envelope extensions (<code>scenarioid</code>, <code>market.world</code>, <code>market.universe</code>, <code>portfolio.world</code>, <code>strategy.world</code>) that this spec defines the values for.</p>"},{"location":"specs/scenarios/#requirements","title":"Requirements","text":""},{"location":"specs/scenarios/#requirement-scenario-triplet","title":"Requirement: Scenario Triplet","text":"<p>Every scenario SHALL be defined by exactly three legs:</p> Leg Values Notes <code>market</code> <code>{ world: \"act\" \\| \"hyp.&lt;name&gt;\", universe: string }</code> <code>universe</code> names the data universe and SHALL be present for both <code>act</code> and <code>hyp.&lt;name&gt;</code> (symmetric \u2014 e.g. <code>act EQ_EUROPE</code>, <code>act ALL</code>, <code>hyp.STRESS EQ_2008_2009</code>); use <code>\"ALL\"</code> for \"no restriction\". <code>&lt;name&gt;</code> identifies a specific hypothetical reality (see Requirement: Named Hypothetical Worlds) \u2014 bare <code>hyp</code> is never a valid value here. <code>portfolio</code> <code>{ world: \"act\" \\| \"hyp.&lt;name&gt;\" }</code> Which portfolio state backs this scenario <code>strategy</code> <code>{ world: \"act\" \\| \"hyp.&lt;name&gt;\", id?: string }</code> Which strategy code drives this scenario <p>Any combination of legs is permitted (e.g., all-act, all-hyp, act-market + hyp-portfolio + hyp-strategy for shadow mode) \u2014 legs need not share the same <code>hyp.&lt;name&gt;</code> world.</p>"},{"location":"specs/scenarios/#scenario-production-scenario","title":"Scenario: Production scenario","text":"<ul> <li>WHEN <code>scenario.LIVE</code> is bootstrapped at service start</li> <li>THEN the triplet SHALL be <code>{ market: { world: \"act\", universe: \"ALL\" }, portfolio: { world: \"act\" }, strategy: { world: \"act\" } }</code></li> </ul>"},{"location":"specs/scenarios/#scenario-paper-trading-scenario","title":"Scenario: Paper trading scenario","text":"<ul> <li>WHEN a paper-trading scenario is registered</li> <li>THEN the triplet MAY be <code>{ market: { world: \"act\", universe: \"ALL\" }, portfolio: { world: \"hyp.PAPER\" }, strategy: { world: \"hyp.PAPER\", id: \"strategy_v1\" } }</code></li> <li>AND the strategy's <code>id</code> SHALL name the strategy code under test</li> </ul>"},{"location":"specs/scenarios/#scenario-backtest-scenario","title":"Scenario: Backtest scenario","text":"<ul> <li>WHEN a backtest scenario is registered</li> <li>THEN the triplet MAY be <code>{ market: { world: \"hyp.BACKTEST\", universe: \"EQ_2008_2009\" }, portfolio: { world: \"hyp.BACKTEST\" }, strategy: { world: \"hyp.BACKTEST\", id: \"strategy_v10\" } }</code></li> <li>AND the market <code>universe</code> SHALL name the data universe being replayed</li> <li>AND the world name (<code>BACKTEST</code> here) identifies the hypothetical reality independently of which universe or strategy it's paired with \u2014 a different backtest campaign would use a different name (e.g. <code>hyp.BACKTEST_2022</code>), not the same name with a different universe</li> </ul>"},{"location":"specs/scenarios/#scenario-live-scenario-scoped-to-a-market-subset","title":"Scenario: Live scenario scoped to a market subset","text":"<ul> <li>WHEN a scenario needs live data restricted to a named subset (e.g. European equities only)</li> <li>THEN the triplet MAY be <code>{ market: { world: \"act\", universe: \"EQ_EUROPE\" }, portfolio: { world: \"act\" }, strategy: { world: \"act\" } }</code></li> <li>AND <code>act</code> triplets SHALL name a <code>universe</code> exactly like <code>hyp.&lt;name&gt;</code> ones do \u2014 <code>act</code> carries no implicit \"everything\" meaning on its own; use <code>universe: \"ALL\"</code> to say so explicitly</li> </ul>"},{"location":"specs/scenarios/#scenario-shadow-mode-scenario","title":"Scenario: Shadow mode scenario","text":"<ul> <li>WHEN a shadow-mode scenario is registered (real market, real portfolio, experimental strategy)</li> <li>THEN the triplet MAY be <code>{ market: { world: \"act\", universe: \"ALL\" }, portfolio: { world: \"act\" }, strategy: { world: \"hyp.SHADOW\", id: \"strategy_v_candidate\" } }</code></li> </ul>"},{"location":"specs/scenarios/#requirement-scenario-lifecycle","title":"Requirement: Scenario Lifecycle","text":"<p>Scenarios SHALL have one of four statuses: <code>active</code>, <code>paused</code>, <code>archived</code>, <code>deleted</code>.</p>"},{"location":"specs/scenarios/#scenario-scenario-states","title":"Scenario: Scenario states","text":"<ul> <li>WHEN a scenario is first registered</li> <li> <p>THEN its status SHALL be <code>active</code></p> </li> <li> <p>WHEN a scenario is <code>paused</code></p> </li> <li> <p>THEN it SHALL NOT receive new events but retained state SHALL be queryable</p> </li> <li> <p>WHEN a scenario is <code>archived</code></p> </li> <li> <p>THEN it SHALL be read-only; the registry entry and all <code>sc.&lt;scenarioId&gt;.*</code> state keys SHALL be retained</p> </li> <li> <p>WHEN a scenario is <code>deleted</code></p> </li> <li>THEN the registry entry and ALL <code>sc.&lt;scenarioId&gt;.*</code> state keys SHALL be purged irreversibly</li> <li>AND the deletion SHALL be confirmed via an explicit second confirmation (no accidental drops)</li> </ul>"},{"location":"specs/scenarios/#requirement-scenario-id-constraints","title":"Requirement: Scenario ID Constraints","text":"<p>Scenario IDs SHALL be constrained as follows.</p>"},{"location":"specs/scenarios/#scenario-live-is-reserved","title":"Scenario: LIVE is reserved","text":"<ul> <li>WHEN a service attempts to register a scenario with <code>scenarioId = \"LIVE\"</code></li> <li>THEN the registration SHALL be rejected (LIVE is reserved and pre-seeded at bootstrap)</li> <li>AND <code>sc.LIVE.*</code> publishes SHALL be permitted without explicit registration</li> </ul>"},{"location":"specs/scenarios/#scenario-scenario-id-format","title":"Scenario: Scenario ID format","text":"<ul> <li>WHEN a service registers a scenario</li> <li>THEN the <code>scenarioId</code> SHALL match the regex <code>^[A-Z0-9_]{1,32}$</code> (uppercase alphanumerics + underscore, max 32 chars)</li> <li>AND the ID SHALL be opaque to routing logic \u2014 semantic meaning lives in the triplet, not the ID string</li> </ul>"},{"location":"specs/scenarios/#scenario-concurrent-runs","title":"Scenario: Concurrent runs","text":"<ul> <li>WHEN two backtests run concurrently against the same scenario</li> <li>THEN they SHALL publish to the same <code>sc.&lt;scenarioId&gt;.*</code> topics</li> <li>AND their events SHALL be disambiguated by the <code>runid</code> envelope extension</li> <li>AND the registry SHALL NOT track individual runs (run lifecycle is per-publisher, not per-registry)</li> </ul>"},{"location":"specs/scenarios/#requirement-scenario-registry-storage","title":"Requirement: Scenario Registry Storage","text":"<p>The scenario registry SHALL live in the Dapr <code>statestore</code> (Valkey) state store under the <code>scenario.&lt;scenarioId&gt;</code> key space.</p>"},{"location":"specs/scenarios/#scenario-registry-key-shape","title":"Scenario: Registry key shape","text":"<ul> <li>WHEN a scenario is registered</li> <li>THEN the registry entry SHALL be stored at key <code>scenario.&lt;scenarioId&gt;</code></li> <li>AND the value SHALL be JSON:   <pre><code>{\n  \"scenarioName\": \"&lt;human-readable&gt;\",\n  \"status\": \"active\" | \"paused\" | \"archived\" | \"deleted\",\n  \"market\":    { \"world\": \"act\" | \"hyp.&lt;name&gt;\", \"universe\": \"string\" },\n  \"portfolio\": { \"world\": \"act\" | \"hyp.&lt;name&gt;\" },\n  \"strategy\":  { \"world\": \"act\" | \"hyp.&lt;name&gt;\", \"id\": \"string?\" },\n  \"createdAt\": \"&lt;rfc3339&gt;\",\n  \"updatedAt\": \"&lt;rfc3339&gt;\"\n}\n</code></pre></li> </ul>"},{"location":"specs/scenarios/#scenario-scenario-index","title":"Scenario: Scenario index","text":"<ul> <li>WHEN a scenario is registered</li> <li>THEN the <code>scenarioId</code> SHALL be added to <code>scenario.index</code> (a Valkey set)</li> <li>AND <code>scenario.index</code> SHALL be the canonical list of registered scenarios</li> </ul>"},{"location":"specs/scenarios/#scenario-live-bootstrap","title":"Scenario: LIVE bootstrap","text":"<ul> <li>WHEN a service starts</li> <li>THEN it SHALL ensure <code>scenario.LIVE</code> exists in the registry</li> <li>AND if absent, it SHALL seed <code>scenario.LIVE</code> with the production triplet (<code>act/ALL</code>, <code>act</code>, <code>act</code>)</li> <li>AND if present, it SHALL NOT modify the entry (LIVE is reserved; updates require elevated role)</li> </ul>"},{"location":"specs/scenarios/#requirement-scenario-lookup","title":"Requirement: Scenario Lookup","text":"<p>Consumers SHALL resolve a scenario ID to its triplet via the registry, not by any other means.</p>"},{"location":"specs/scenarios/#scenario-resolve-scenario-id-to-triplet","title":"Scenario: Resolve scenario ID to triplet","text":"<ul> <li>WHEN a consumer needs the triplet for a <code>scenarioId</code></li> <li>THEN it SHALL read <code>scenario.&lt;scenarioId&gt;</code> from the state store</li> <li>AND cache the result in-process (TTL \u2264 60 s; refresh on cache miss)</li> </ul>"},{"location":"specs/scenarios/#requirement-scenario-lifecycle-via-existing-state-rpcs","title":"Requirement: Scenario Lifecycle via Existing State RPCs","text":"<p>Scenario CRUD SHALL use the existing virtufin-api <code>State.SaveState</code> / <code>State.GetState</code> / <code>State.DeleteState</code> / <code>State.QueryState</code> RPCs. No new RPCs SHALL be added to <code>virtufin-api</code> for scenario management \u2014 <code>virtufin-api</code> is pure infrastructure and SHALL NOT carry domain logic.</p>"},{"location":"specs/scenarios/#scenario-register-scenario","title":"Scenario: Register scenario","text":"<ul> <li>WHEN a service registers a scenario</li> <li>THEN it SHALL call <code>State.SaveState(scenario.&lt;scenarioId&gt;, triplet_json)</code></li> <li>AND add the <code>scenarioId</code> to the <code>scenario.index</code> Valkey set</li> <li>AND reject <code>scenarioId = LIVE</code> (reserved)</li> </ul>"},{"location":"specs/scenarios/#scenario-read-scenario-triplet","title":"Scenario: Read scenario triplet","text":"<ul> <li>WHEN a service needs the triplet for a <code>scenarioId</code></li> <li>THEN it SHALL call <code>State.GetState(scenario.&lt;scenarioId&gt;)</code></li> <li>AND cache the result in-process (TTL \u2264 60 s)</li> </ul>"},{"location":"specs/scenarios/#scenario-status-transition","title":"Scenario: Status transition","text":"<ul> <li>WHEN a scenario's status changes (active / paused / archived)</li> <li>THEN the caller SHALL call <code>State.SaveState(scenario.&lt;scenarioId&gt;, new_status_json)</code></li> <li>AND consumers caching the triplet (Scenario Lookup, TTL \u2264 60 s) SHALL observe the new status on their next refresh -- this does not gate publishes, which are not validated against scenario status (see below)</li> </ul>"},{"location":"specs/scenarios/#scenario-delete-scenario-irreversible","title":"Scenario: Delete scenario (irreversible)","text":"<ul> <li>WHEN a service deletes a scenario</li> <li>THEN it SHALL:</li> <li>Call <code>State.SaveState(scenario.&lt;scenarioId&gt;, status=deleted)</code></li> <li>Call <code>State.SaveState(scenario.index, remove scenarioId)</code></li> <li>Call <code>State.QueryState({\"filter\": {\"PREFIX\": \"sc.&lt;scenarioId&gt;.\"}})</code> + per-key <code>State.DeleteState(...)</code> for all matching keys</li> <li>Call <code>State.DeleteState(scenario.&lt;scenarioId&gt;)</code></li> <li>AND log at WARN level; this operation is irreversible</li> <li>AND step 3 depends on the deployed Dapr state-store component supporting the query API \u2014 if unsupported, the caller SHALL fall back to tracking <code>sc.&lt;scenarioId&gt;.*</code> keys via its own index rather than assuming <code>QueryState</code> succeeds</li> </ul>"},{"location":"specs/scenarios/#requirement-no-pre-publish-scenario-state-validation","title":"Requirement: No Pre-Publish Scenario-State Validation","text":"<p>No service SHALL validate a scenario's registration or lifecycle status before publishing to its <code>sc.&lt;scenarioId&gt;.*</code> topics \u2014 see Pub/Sub Topics spec \u00a7No Topic or Scenario-State Validation Anywhere. <code>scenario.&lt;scenarioId&gt;</code> lookups (see Scenario Lookup above) are for consumers resolving a triplet's meaning, not a pre-publish gate.</p>"},{"location":"specs/scenarios/#scenario-publish-to-an-unregistered-or-non-active-scenario","title":"Scenario: Publish to an unregistered or non-active scenario","text":"<ul> <li>WHEN a service publishes to <code>sc.&lt;scenarioId&gt;.*</code> for a <code>scenarioId</code> that is unregistered, paused, archived, or deleted</li> <li>THEN the publish SHALL proceed unconditionally \u2014 the registry is a lookup for consumers, not a publish-time gate</li> </ul>"},{"location":"specs/scenarios/#requirement-universe-naming","title":"Requirement: Universe Naming","text":"<p>Universes SHALL name data universes for market data, whether <code>act</code> or <code>hyp.&lt;name&gt;</code>. They appear in <code>act.&lt;universe&gt;.exchange.&lt;venue&gt;.*</code> / <code>hyp.&lt;name&gt;.&lt;universe&gt;.exchange.&lt;venue&gt;.*</code> topics AND in the <code>market.universe</code> envelope extension.</p>"},{"location":"specs/scenarios/#scenario-universe-format","title":"Scenario: Universe format","text":"<ul> <li>WHEN a service defines a new data universe</li> <li>THEN the universe SHALL match <code>^[A-Z0-9_]{1,32}$</code> (same regex as scenario IDs)</li> <li>AND the universe SHALL be stable across runs (changing the universe name for the same data set breaks subscribers)</li> </ul>"},{"location":"specs/scenarios/#scenario-universe-vs-scenario-id","title":"Scenario: Universe vs scenario ID","text":"<ul> <li>WHEN a service needs to distinguish \"the data universe\" from \"a scenario running against that universe\"</li> <li>THEN the universe SHALL identify the data universe (in <code>market.universe</code>)</li> <li>AND the scenario ID SHALL identify the scenario (in <code>scenarioid</code>)</li> <li>AND one universe MAY back many scenario IDs (e.g., <code>EQ_2008_2009</code> for backtests with different strategies, or <code>ALL</code> for every LIVE-derived scenario)</li> </ul>"},{"location":"specs/scenarios/#requirement-named-hypothetical-worlds","title":"Requirement: Named Hypothetical Worlds","text":"<p>A hypothetical <code>world</code> value SHALL always name a specific hypothetical reality: <code>hyp.&lt;name&gt;</code>, where <code>&lt;name&gt;</code> matches <code>^[A-Z0-9_]{1,32}$</code> (same format as scenario IDs and universes). Bare <code>\"hyp\"</code> SHALL NOT be assigned as a <code>world</code> value anywhere \u2014 not a triplet leg, not a topic segment, not an envelope extension. Bare <code>hyp</code> denotes the category of all hypothetical worlds (every <code>world</code> starting with <code>hyp.</code>), useful only for documentation and wildcard subscriptions, never a concrete value.</p> <p><code>&lt;name&gt;</code> is independent of <code>universe</code> and of <code>scenarioId</code>: it does not need to be registered anywhere, is not looked up via the scenario registry, and carries no implicit relationship to any specific scenario. One <code>hyp.&lt;name&gt;</code> world MAY pair with many different universes and back many different scenario IDs, exactly as one <code>universe</code> already MAY back many scenario IDs (see Requirement: Universe Naming).</p>"},{"location":"specs/scenarios/#scenario-naming-a-stress-test-world","title":"Scenario: Naming a stress-test world","text":"<ul> <li>WHEN a service defines a hypothetical world for a stress-testing campaign</li> <li>THEN the world SHALL be named e.g. <code>hyp.STRESS</code></li> <li>AND it MAY be paired with any universe, e.g. <code>{ world: \"hyp.STRESS\", universe: \"EQUITY\" }</code> or <code>{ world: \"hyp.STRESS\", universe: \"FX\" }</code> \u2014 both are valid, distinct market legs sharing the same named world</li> </ul>"},{"location":"specs/scenarios/#scenario-bare-hyp-is-rejected-as-a-value","title":"Scenario: Bare hyp is rejected as a value","text":"<ul> <li>WHEN a service attempts to register a scenario or publish a topic with a bare <code>world: \"hyp\"</code> (no name)</li> <li>THEN this SHALL be treated as a spec violation \u2014 <code>hyp</code> alone identifies a category, not a world</li> </ul>"},{"location":"specs/terminal-ui/","title":"Terminal UI","text":""},{"location":"specs/terminal-ui/#purpose","title":"Purpose","text":"<p>The Terminal UI (<code>virtufin-tui</code>) is a Textual-based Python operator dashboard for the Virtufin control plane. It is a pure consumer of the existing <code>virtufin-api</code> gRPC surface \u2014 it runs no service-side code, adds no proto messages, and requires no service changes. It fills the operator role that <code>kubectl</code> plays for Kubernetes: interactive worker / connection / state / event management over a remote <code>virtufin-api</code> endpoint, with live CloudEvent streaming, multi-context switching, and an AI assistant.</p> <p>Related specs: - Cross-cutting \u2014 API-mediated pub/sub rule (the TUI talks only to <code>virtufin-api</code>, never Dapr directly). - Pub/Sub Topics \u2014 the CloudEvent envelope and topic taxonomy the TUI's event log renders. - Scenarios \u2014 the scenario registry triplet the TUI's scenario page edits. - Worker Management \u2014 worker lifecycle, tags, and creation-time config the TUI drives. - WebSocket Proxy \u2014 connection lifecycle, tags, and publishing the TUI drives.</p>"},{"location":"specs/terminal-ui/#requirements","title":"Requirements","text":""},{"location":"specs/terminal-ui/#requirement-multi-context-configuration","title":"Requirement: Multi-Context Configuration","text":"<p>The TUI SHALL read its endpoint configuration from a TOML file under the XDG config directory (<code>~/.config/virtufin/api/contexts.toml</code> on Linux/macOS, <code>%APPDATA%\\virtufin\\api\\contexts.toml</code> on Windows). The file SHALL contain a <code>[[contexts]]</code> array; each entry MUST have <code>name</code>, <code>api_host</code>, <code>api_port</code> fields and MAY have <code>tls</code> (bool), <code>api_key</code>, <code>default_worker_topic</code>, <code>worker_api_host</code>, <code>worker_api_port</code>, <code>description</code>, and <code>event_topics</code> (default <code>[\"state.change\"]</code>).</p> <p>The active context is state, not config: it SHALL be persisted separately at <code>~/.local/state/virtufin/api/state.toml</code> (mode <code>0600</code>). The TUI SHALL keep the catalog-of-contexts vs. active-selection split for every user setting it persists (contexts, LLM providers, themes, event topics). Active-context resolution order SHALL be: CLI <code>--context</code> flag \u2192 <code>$VIRTUFIN_TUI_CONTEXT</code> env var \u2192 persisted state file \u2192 first entry in the file.</p> <p>Environment variables <code>VIRTUFIN_TUI_API_HOST</code> and <code>VIRTUFIN_TUI_API_PORT</code> SHALL override the active context's host/port in memory when set.</p>"},{"location":"specs/terminal-ui/#scenario-first-run-with-no-config-file","title":"Scenario: First-run with no config file","text":"<ul> <li>WHEN the user runs <code>virtufin-tui</code> and no <code>contexts.toml</code> exists</li> <li>THEN the TUI SHALL seed shipped default configs into <code>~/.config/virtufin/</code></li> <li>AND SHALL NOT overwrite existing files (seeded files are mode <code>0600</code>)</li> </ul>"},{"location":"specs/terminal-ui/#scenario-env-var-override","title":"Scenario: Env var override","text":"<ul> <li>WHEN <code>VIRTUFIN_TUI_API_HOST=staging.virtufin.com</code> and <code>VIRTUFIN_TUI_API_PORT=5002</code> are exported</li> <li>THEN the TUI SHALL use those values for the active context, regardless of what is in <code>contexts.toml</code></li> </ul>"},{"location":"specs/terminal-ui/#scenario-switch-context-mid-session","title":"Scenario: Switch context mid-session","text":"<ul> <li>WHEN the user selects a different context from the config page</li> <li>THEN the TUI SHALL tear down the existing gRPC channel, DataPump tasks, and event subscription</li> <li>AND SHALL rebuild the client and restart polling/subscription against the new context</li> </ul>"},{"location":"specs/terminal-ui/#requirement-client-access-via-grpc-reflection","title":"Requirement: Client Access via gRPC Reflection","text":"<p>The TUI SHALL invoke control-plane RPCs without vendored proto stubs, using the API gateway's reflection RPCs (<code>get_file_descriptor_protos_async</code>) to load message descriptors at runtime. Each backend service SHALL get its own <code>DescriptorPool</code> (pools SHALL NOT be shared \u2014 <code>workmanager.proto</code> and <code>websocketmanager.proto</code> both declare <code>package virtufin</code> with identically- named messages). Repeated-message RPCs (<code>ListWorkers</code>, <code>List</code>, etc.) SHALL be invoked via the raw reflection path, since grpc-gateway's JSON transcode does not cover them.</p>"},{"location":"specs/terminal-ui/#scenario-list-workers-via-reflection","title":"Scenario: List workers via reflection","text":"<ul> <li>WHEN the TUI polls the workers table</li> <li>THEN it SHALL call <code>WorkManager.ListWorkers</code> through the reflection pipeline (<code>invoke_raw_async</code>), decoding the response from the dynamically-loaded message class</li> </ul>"},{"location":"specs/terminal-ui/#scenario-one-descriptor-pool-per-service","title":"Scenario: One descriptor pool per service","text":"<ul> <li>WHEN the TUI loads descriptors for <code>workmanager</code> and <code>websocketmanager</code></li> <li>THEN it SHALL build a separate <code>DescriptorPool</code> per service</li> <li>AND SHALL NOT share pools between services with colliding <code>package virtufin</code> symbols</li> </ul>"},{"location":"specs/terminal-ui/#requirement-worker-operations-surface","title":"Requirement: Worker Operations Surface","text":"<p>The TUI SHALL expose worker CRUD, tagging, and lifecycle operations through the keyboard. List/detail/create/tag/start/stop/delete SHALL be backed by the WorkManager RPCs (<code>ListWorkers</code>, <code>GetTags</code>, <code>GetWorkerHistory</code>, <code>CreateWorker</code>, <code>SetTag</code>, <code>StartWorker</code>, <code>StopWorker</code>, <code>DeleteWorker</code>). Destructive actions (delete) SHALL require a confirmation modal. Tag changes SHALL be undoable for 2 seconds after the change.</p>"},{"location":"specs/terminal-ui/#scenario-create-a-worker","title":"Scenario: Create a worker","text":"<ul> <li>WHEN the user invokes the new-worker flow and submits a worker with MIME type, topic, code source, and tags</li> <li>THEN the TUI SHALL call <code>CreateWorker</code> and the new worker SHALL appear in the workers table on the next poll</li> </ul>"},{"location":"specs/terminal-ui/#scenario-set-a-tag-with-undo","title":"Scenario: Set a tag with undo","text":"<ul> <li>WHEN the user sets a tag on a worker</li> <li>THEN the TUI SHALL call <code>SetTag</code>, display an undo hint for 2 seconds, and undoing SHALL restore the prior value via <code>SetTag</code></li> </ul>"},{"location":"specs/terminal-ui/#scenario-delete-with-confirmation","title":"Scenario: Delete with confirmation","text":"<ul> <li>WHEN the user invokes the delete action on a worker</li> <li>THEN the TUI SHALL show a confirmation modal; on confirm it SHALL call <code>DeleteWorker</code>; on cancel no action is taken</li> </ul>"},{"location":"specs/terminal-ui/#scenario-worker-detail","title":"Scenario: Worker detail","text":"<ul> <li>WHEN the user opens a worker's detail popover</li> <li>THEN the TUI SHALL aggregate <code>GetTags</code> + <code>GetWorkerHistory</code> + the creation-time config from the latest <code>ListWorkers</code> pass (config is immutable after creation; there is no dedicated get-config RPC)</li> </ul>"},{"location":"specs/terminal-ui/#requirement-websocket-connection-operations-surface","title":"Requirement: WebSocket Connection Operations Surface","text":"<p>The TUI SHALL expose WebSocket connection management through the keyboard, backed by the WebSocketManager RPCs (<code>List</code>, <code>GetTags</code>, <code>SetTag</code>, <code>Connect</code>, <code>Disconnect</code>, <code>SendRaw</code>, <code>StartPublish</code>, <code>StopPublish</code>). <code>SendRaw</code> SHALL base64-encode the message body (the proto <code>bytes</code> field requires base64 in the JSON mapping). Tag changes SHALL be undoable for 2 seconds.</p>"},{"location":"specs/terminal-ui/#scenario-connect-to-a-new-websocket-url","title":"Scenario: Connect to a new WebSocket URL","text":"<ul> <li>WHEN the user invokes the connect action with a URL and auto-reconnect preference</li> <li>THEN the TUI SHALL call <code>Connect</code> and the new connection SHALL appear in the connections table on the next poll</li> </ul>"},{"location":"specs/terminal-ui/#scenario-send-a-raw-message","title":"Scenario: Send a raw message","text":"<ul> <li>WHEN the user sends a JSON message to a connection</li> <li>THEN the TUI SHALL base64-encode the body and call <code>SendRaw(conn_id, message, content_type)</code></li> </ul>"},{"location":"specs/terminal-ui/#scenario-start-and-stop-publishing","title":"Scenario: Start and stop publishing","text":"<ul> <li>WHEN the user starts or stops publishing on a connection</li> <li>THEN the TUI SHALL call <code>StartPublish(conn_id, topic)</code> or <code>StopPublish(conn_id)</code></li> <li>AND the server-side <code>topic</code> field is the authoritative publishing signal</li> </ul>"},{"location":"specs/terminal-ui/#scenario-disconnect-is-not-delete","title":"Scenario: Disconnect is not delete","text":"<ul> <li>WHEN the user disconnects a connection</li> <li>THEN the TUI SHALL call <code>Disconnect</code> (the socket closes, but the record MAY reappear if auto-reconnect is enabled \u2014 there is no true delete in the websocket service)</li> </ul>"},{"location":"specs/terminal-ui/#requirement-live-cloudevent-streaming","title":"Requirement: Live CloudEvent Streaming","text":"<p>The TUI SHALL subscribe to <code>Pubsub.Subscribe</code> (the sole subscribe RPC) and append incoming CloudEvents to the events log. The subscription SHALL be parameterized by the context's <code>event_topics</code> (user-editable, persisted to TUI state). On subscription failure the TUI SHALL retry with exponential backoff (1s \u2192 30s cap) and SHALL NOT replay missed events on reconnect. The event log SHALL be a bounded append-only buffer (500 events) with a running total counter.</p>"},{"location":"specs/terminal-ui/#scenario-events-flow-into-the-log","title":"Scenario: Events flow into the log","text":"<ul> <li>WHEN the TUI is connected and a CloudEvent is received on a subscribed topic</li> <li>THEN the event SHALL appear at the bottom of the events log, pretty-printed (type/source/id/topic/time + decoded payload)</li> </ul>"},{"location":"specs/terminal-ui/#scenario-change-subscribed-topics","title":"Scenario: Change subscribed topics","text":"<ul> <li>WHEN the user adds or removes topics via the events page</li> <li>THEN the TUI SHALL restart the subscription immediately with the new topic set</li> <li>AND SHALL persist the new topic set to TUI state</li> </ul>"},{"location":"specs/terminal-ui/#scenario-reconnect-on-grpc-failure","title":"Scenario: Reconnect on gRPC failure","text":"<ul> <li>WHEN the subscription drops</li> <li>THEN the TUI SHALL retry with exponential backoff (1s \u2192 30s cap) and surface a backoff status; the event log SHALL be preserved across reconnects</li> </ul>"},{"location":"specs/terminal-ui/#requirement-world-management","title":"Requirement: World Management","text":"<p>The TUI SHALL manage market-data \"worlds\" as named abstractions over WebSocket connections. An atomic world SHALL be a connection tagged <code>world=&lt;name&gt;</code>; a composite world SHALL be a <code>statestore.world.&lt;name&gt;</code> entry listing component worlds (with DFS cycle detection before saving). World definitions SHALL be loadable from per-file TOML configs under <code>~/.config/virtufin/tui/worlds/*.toml</code>, each of <code>type</code> <code>\"websocket\"</code> (with <code>ws_url</code>, optional <code>handshake</code> steps, <code>reconnect</code>) or <code>\"script\"</code> (with <code>script</code>, <code>execution_trigger</code>, <code>execution_function</code>, <code>return_type</code>).</p> <p>The canonical world-open sequence SHALL be: connect \u2192 tag (<code>world</code>/<code>universe</code>/<code>world_source</code>) \u2192 wait for connected \u2192 replay handshake \u2192 start publish \u2192 tag <code>publishing</code> \u2192 persist.</p>"},{"location":"specs/terminal-ui/#scenario-connect-a-world","title":"Scenario: Connect a world","text":"<ul> <li>WHEN the user connects a world config</li> <li>THEN the TUI SHALL run the canonical open sequence and tag the connection with <code>world</code>, <code>universe</code>, and <code>world_source</code></li> </ul>"},{"location":"specs/terminal-ui/#scenario-compose-worlds","title":"Scenario: Compose worlds","text":"<ul> <li>WHEN the user composes two or more worlds into a composite</li> <li>THEN the TUI SHALL persist <code>statestore.world.&lt;name&gt;</code> with its component list</li> <li>AND SHALL reject a composition that would create a cycle</li> </ul>"},{"location":"specs/terminal-ui/#scenario-replay-handshake-on-reconnect","title":"Scenario: Replay handshake on reconnect","text":"<ul> <li>WHEN a world connection transitions from <code>reconnecting</code> to <code>connected</code></li> <li>THEN the TUI SHALL replay the world's handshake (WebSocketManager never resends a handshake, initial or on reconnect)</li> </ul>"},{"location":"specs/terminal-ui/#requirement-strategy-management","title":"Requirement: Strategy Management","text":"<p>The TUI SHALL manage strategies as named abstractions. An atomic strategy SHALL be a worker tagged <code>type=&lt;name&gt;</code> (derived \u2014 no state entry); a composite strategy SHALL be a <code>statestore.strategy.&lt;name&gt;</code> entry listing component strategies (with DFS cycle detection before saving). Creating an atomic strategy SHALL create a worker and tag it with <code>type</code> and <code>source</code>.</p>"},{"location":"specs/terminal-ui/#scenario-create-an-atomic-strategy","title":"Scenario: Create an atomic strategy","text":"<ul> <li>WHEN the user creates an atomic strategy</li> <li>THEN the TUI SHALL create a worker and tag it with the strategy <code>type</code> and <code>source</code></li> </ul>"},{"location":"specs/terminal-ui/#scenario-compose-strategies","title":"Scenario: Compose strategies","text":"<ul> <li>WHEN the user composes strategies</li> <li>THEN the TUI SHALL persist <code>statestore.strategy.&lt;name&gt;</code> with its component list and reject cycles</li> </ul>"},{"location":"specs/terminal-ui/#requirement-scenario-management","title":"Requirement: Scenario Management","text":"<p>The TUI SHALL edit the scenario registry (per the Scenarios spec) via the state store at <code>scenario.&lt;SCENARIO_ID&gt;</code> with the <code>scenario.index</code> set. <code>LIVE</code> SHALL be reserved (bootstrapped once at app startup, non-fatal on failure) and rejected as a user-supplied ID. Scenario IDs SHALL match <code>^[A-Z0-9_]{1,32}$</code>. Deletion SHALL require a double confirmation and SHALL mark <code>deleted</code>, drop the index entry, and purge the registry.</p>"},{"location":"specs/terminal-ui/#scenario-add-a-scenario","title":"Scenario: Add a scenario","text":"<ul> <li>WHEN the user adds a scenario with market/portfolio/strategy legs</li> <li>THEN the TUI SHALL persist the triplet to <code>scenario.&lt;id&gt;</code> and add the ID to <code>scenario.index</code></li> <li>AND SHALL reject <code>LIVE</code> and IDs not matching <code>^[A-Z0-9_]{1,32}$</code></li> </ul>"},{"location":"specs/terminal-ui/#scenario-set-the-active-scenario","title":"Scenario: Set the active scenario","text":"<ul> <li>WHEN the user activates a scenario</li> <li>THEN the TUI SHALL set it active and refresh the trade status bar (which highlights when the active scenario is <code>LIVE</code>)</li> </ul>"},{"location":"specs/terminal-ui/#scenario-delete-a-scenario","title":"Scenario: Delete a scenario","text":"<ul> <li>WHEN the user deletes a scenario</li> <li>THEN the TUI SHALL require a double confirmation, then mark the entry <code>deleted</code>, remove it from <code>scenario.index</code>, and purge the registry entry</li> </ul>"},{"location":"specs/terminal-ui/#requirement-state-and-service-browser","title":"Requirement: State and Service Browser","text":"<p>The TUI SHALL provide a service browser that enumerates registered services and methods via the gateway reflection RPCs (<code>ListServices</code>, <code>ListMethods</code>), renders a JSON input form, and dispatches invocation through the gateway's dynamic method dispatch. The browser SHALL expose raw JSON invocation only \u2014 it SHALL NOT attempt schema-driven form generation for arbitrary methods.</p>"},{"location":"specs/terminal-ui/#scenario-browse-services-and-methods","title":"Scenario: Browse services and methods","text":"<ul> <li>WHEN the user opens the service browser</li> <li>THEN the TUI SHALL list services, then list a selected service's methods, and invoke a method by JSON payload</li> </ul>"},{"location":"specs/terminal-ui/#requirement-worker-registry-and-schema-driven-creation","title":"Requirement: Worker Registry and Schema-Driven Creation","text":"<p>The TUI SHALL support creating workers from a declarative registry at <code>~/.config/virtufin/tui/workers.toml</code>. Each entry SHALL declare <code>name</code>, <code>mime_type</code>, <code>source_url</code> (templated with <code>{version}</code> or <code>{ref}</code>), <code>config_schema</code> (JSON Schema draft 2020-12), <code>versions</code>, and <code>authentication</code>. The new-worker flow SHALL render a form from <code>config_schema</code> (strings, integers, numbers, booleans, and enums; nested objects skipped with a warning). Source authentication SHALL resolve a <code>SourceCredential</code> (scheme <code>basic</code>/<code>bearer</code>) \u2014 <code>\"virtufin\"</code> SHALL resolve the org's server-resolved shared credential (<code>VIRTUFIN_REGISTRY_CREDENTIAL_NAME</code>), otherwise a named personal credential from <code>credentials.toml</code>.</p>"},{"location":"specs/terminal-ui/#scenario-create-a-worker-from-the-registry","title":"Scenario: Create a worker from the registry","text":"<ul> <li>WHEN the user picks a registered worker type</li> <li>THEN the TUI SHALL render a form from <code>config_schema</code>, resolve the source credential, create the worker, and tag it <code>type</code> + <code>source</code></li> </ul>"},{"location":"specs/terminal-ui/#scenario-inline-or-versioned-source","title":"Scenario: Inline or versioned source","text":"<ul> <li>WHEN the user supplies inline code, a relative file, a version dropdown, or a fixed URL as the code source</li> <li>THEN the TUI SHALL build the <code>code_source</code> accordingly (inline content base64-encoded) and pass it to <code>CreateWorker</code></li> </ul>"},{"location":"specs/terminal-ui/#requirement-trigger-management","title":"Requirement: Trigger Management","text":"<p>The TUI SHALL list, schedule, and delete cron/point-in-time triggers via the TriggerService stubs (<code>ListTriggers</code>, <code>ScheduleTrigger</code>, <code>DeleteTrigger</code>). The schedule modal SHALL support both cron and point-in-time modes with name, target topic, schedule/due-time, repeat count, and TTL.</p>"},{"location":"specs/terminal-ui/#scenario-schedule-a-trigger","title":"Scenario: Schedule a trigger","text":"<ul> <li>WHEN the user schedules a trigger</li> <li>THEN the TUI SHALL call <code>ScheduleTrigger</code> with the chosen cron or point-in-time parameters</li> </ul>"},{"location":"specs/terminal-ui/#requirement-ai-assistant","title":"Requirement: AI Assistant","text":"<p>The TUI SHALL ship an AI chat tab that translates natural-language requests into control-plane operations via OpenAI-compatible tool calling. The agent logic SHALL be Textual-free (unit-testable) and SHALL have no privileged path \u2014 every tool SHALL map 1:1 to an existing <code>TuiClient</code>/<code>DataPump</code> call. The tool catalog SHALL include create/delete trigger, set event topics, create/start/stop worker, add/connect/remove world. The agent SHALL run a bounded loop (max 5 iterations) and SHALL never raise from tool execution (errors returned as <code>{\"ok\": false, \"error\": ...}</code> for the model).</p>"},{"location":"specs/terminal-ui/#scenario-provider-resolution","title":"Scenario: Provider resolution","text":"<ul> <li>WHEN the AI tab resolves a provider</li> <li>THEN it SHALL check <code>VIRTUFIN_TUI_AI_PROVIDER</code> env \u2192 persisted default \u2192 first ready provider \u2192 none</li> <li>AND SHALL show a configure state (disabled input) when no provider resolves</li> </ul>"},{"location":"specs/terminal-ui/#scenario-tool-execution-never-raises","title":"Scenario: Tool execution never raises","text":"<ul> <li>WHEN a tool call fails</li> <li>THEN the TUI SHALL surface the error to the model as a structured result rather than raising</li> </ul>"},{"location":"specs/terminal-ui/#requirement-custom-pane-extension","title":"Requirement: Custom Pane Extension","text":"<p>The TUI SHALL support user-defined panes dropped into <code>~/.config/virtufin/tui/panes/</code>. Two kinds SHALL be supported: a script + form pair (<code>&lt;name&gt;.py</code> + sibling <code>&lt;name&gt;.form.json</code>, run as a subprocess on submit) and a live widget pane (<code>&lt;name&gt;.py</code> defining a module-level <code>Pane</code> class with a <code>(stores, client, **kwargs)</code> constructor, mounted as a persistent page). Files matching neither pattern SHALL be skipped with a warning.</p>"},{"location":"specs/terminal-ui/#scenario-discover-panes","title":"Scenario: Discover panes","text":"<ul> <li>WHEN the TUI resolves pane tabs</li> <li>THEN it SHALL scan <code>~/.config/virtufin/tui/panes/</code> and map script+form pairs and live-widget classes to pages</li> </ul>"},{"location":"specs/terminal-ui/#requirement-dashboard-and-pane-layout","title":"Requirement: Dashboard and Pane Layout","text":"<p>The TUI SHALL render a two-level tab layout (top-level tabs, per-tab sub-tabs) driven by an ordered pane spec. Built-in panes SHALL include <code>home</code>, <code>world</code>, <code>instruments</code>, <code>orderbook</code>, <code>portfolio</code>, <code>strategy</code>, <code>scenario</code>, <code>pnl</code>, <code>risk</code>, <code>workers</code>, <code>connections</code>, <code>scripts</code>, <code>triggers</code>, <code>events</code>, <code>config</code>, and <code>ai</code>. The layout SHALL be user-customizable and SHALL migrate newly-introduced default panes into existing user layouts without clobbering user removals (a <code>pane_order_version</code> stamp drives a one-time migration). Pages SHALL be <code>Widget</code> subclasses (not <code>Screen</code>s) mounted into a shared page body.</p>"},{"location":"specs/terminal-ui/#scenario-default-pane-order","title":"Scenario: Default pane order","text":"<ul> <li>WHEN the TUI starts with no pane customization</li> <li>THEN the default order SHALL group trading panes (world, instruments, orderbook, portfolio, strategy, scenario, pnl, risk) under a <code>trading</code> tab and infra panes (workers, connections, scripts, triggers, events) under an <code>infra</code> tab</li> </ul>"},{"location":"specs/terminal-ui/#scenario-pane-migration-preserves-user-edits","title":"Scenario: Pane migration preserves user edits","text":"<ul> <li>WHEN a new default pane is introduced in a later release</li> <li>THEN the migration SHALL insert it at its default position only if the user's <code>pane_order_version</code> is behind the new version</li> <li>AND SHALL NOT re-add a pane the user previously removed</li> </ul>"},{"location":"specs/terminal-ui/#requirement-keyboard-dispatch","title":"Requirement: Keyboard Dispatch","text":"<p>The TUI SHALL expose per-page actions via a single dashboard binding table, with first-match-wins dispatch and footer visibility gated on whether the current page implements the action. Navigation SHALL support <code>right</code>/<code>left</code> to cycle top-level tabs (priority bindings) and <code>1</code>\u2013<code>9</code> to switch sub-tabs. <code>?</code> SHALL show the help overlay; <code>q</code> SHALL quit.</p>"},{"location":"specs/terminal-ui/#scenario-page-conditional-bindings","title":"Scenario: Page-conditional bindings","text":"<ul> <li>WHEN the user presses a bound key on a page that does not implement the action</li> <li>THEN the key SHALL be hidden from the footer help for that page</li> <li>AND SHALL dispatch to the first matching action when present</li> </ul>"},{"location":"specs/terminal-ui/#requirement-distribution-as-a-single-python-package","title":"Requirement: Distribution as a Single Python Package","text":"<p>The TUI SHALL be distributed as a single pip-installable Python package built with hatchling, exposing a <code>virtufin-tui</code> console script entry point that calls <code>virtufin_tui.app:main</code>. It SHALL be runnable without installation via <code>uvx --from virtufin-tui virtufin-tui</code>. The package SHALL depend on <code>textual[syntax]</code>, <code>virtufin-api</code>, <code>grpcio</code>, <code>openai</code>, <code>pydantic</code>, and <code>tomlkit</code>. The version SHALL be pinned in <code>versions.env</code> as <code>LIBRARY_VERSION</code>.</p>"},{"location":"specs/terminal-ui/#scenario-uvx-run","title":"Scenario: uvx run","text":"<ul> <li>WHEN the user runs <code>uvx --from virtufin-tui virtufin-tui</code></li> <li>THEN the TUI SHALL launch against the active context (or the config page if none is configured)</li> </ul>"},{"location":"specs/terminal-ui/#scenario-pinned-version","title":"Scenario: Pinned version","text":"<ul> <li>WHEN CI builds the package</li> <li>THEN it SHALL inject <code>LIBRARY_VERSION</code> from <code>versions.env</code> into <code>pyproject.toml</code> before building</li> </ul>"},{"location":"specs/websocket-proxy/","title":"WebSocket Proxy","text":""},{"location":"specs/websocket-proxy/#purpose","title":"Purpose","text":"<p>Distributed WebSocket proxy service managing outbound connections to external WebSocket servers with auto-reconnection, correlated request-response messaging, pub/sub message routing, and cross-instance ownership with orphan reclamation.</p>"},{"location":"specs/websocket-proxy/#requirements","title":"Requirements","text":""},{"location":"specs/websocket-proxy/#requirement-external-websocket-connection-lifecycle","title":"Requirement: External WebSocket Connection Lifecycle","text":"<p>The WebSocket Manager SHALL allow clients to connect to arbitrary external WebSocket servers, disconnect from them, and list all managed connections. Connections SHALL persist through service restarts.</p>"},{"location":"specs/websocket-proxy/#scenario-connect-to-external-websocket","title":"Scenario: Connect to external WebSocket","text":"<ul> <li>WHEN a client requests a connection to a WebSocket URL</li> <li>THEN the manager SHALL create and maintain a ClientWebSocket connection, persist the connection metadata to the Dapr state store, and return a unique connection ID</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-list-all-connections","title":"Scenario: List all connections","text":"<ul> <li>WHEN a client lists connections</li> <li>THEN the manager SHALL return all active connections with their IDs, URLs, status, and topics</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-disconnect","title":"Scenario: Disconnect","text":"<ul> <li>WHEN a client disconnects a connection</li> <li>THEN the manager SHALL close the WebSocket, cancel its receive loop, and remove connection state from both local and Dapr stores</li> </ul>"},{"location":"specs/websocket-proxy/#requirement-messaging-modes","title":"Requirement: Messaging Modes","text":"<p>The WebSocket Manager SHALL support two send modes: Raw (fire-and-forget) and correlated request-response via a 16-byte UUID binary prefix envelope. Both modes SHALL accept a content_type field that determines the WebSocket frame type (text/* or application/json \u2192 Text, else Binary).</p>"},{"location":"specs/websocket-proxy/#scenario-raw-message-send","title":"Scenario: Raw message send","text":"<ul> <li>WHEN a client sends a raw message (<code>SendRaw</code>) to a connection with a <code>content_type</code></li> <li>THEN the message SHALL be sent as-is to the WebSocket without correlation tracking, using a Text frame if content_type starts with \"text/\" or equals \"application/json\", otherwise a Binary frame</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-correlated-request-response","title":"Scenario: Correlated request-response","text":"<ul> <li>WHEN a client sends a message via <code>Send</code> (correlated mode) with a <code>content_type</code></li> <li>THEN the message SHALL be prefixed with a 16-byte UUID binary envelope <code>[16 bytes UUID][payload bytes]</code>, sent using a Text frame if content_type starts with \"text/\" or equals \"application/json\", otherwise a Binary frame, and the first response whose first 16 bytes match the UUID SHALL be returned, with the UUID prefix stripped</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-correlated-timeout","title":"Scenario: Correlated timeout","text":"<ul> <li>WHEN a correlated request does not receive a matching response within the configurable timeout</li> <li>THEN a timeout error SHALL be returned to the caller</li> </ul>"},{"location":"specs/websocket-proxy/#requirement-auto-reconnection","title":"Requirement: Auto-Reconnection","text":"<p>Connections marked for auto-reconnect SHALL attempt reconnection with exponential backoff when the WebSocket drops.</p>"},{"location":"specs/websocket-proxy/#scenario-connection-drop-with-auto-reconnect","title":"Scenario: Connection drop with auto-reconnect","text":"<ul> <li>WHEN a connection with <code>auto_reconnect=true</code> loses its WebSocket</li> <li>THEN the manager SHALL attempt reconnection up to a configurable maximum with exponential backoff (base delay doubling each attempt, capped at a maximum delay)</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-reconnection-success","title":"Scenario: Reconnection success","text":"<ul> <li>WHEN a reconnection attempt succeeds</li> <li>THEN a new receive loop SHALL start and message delivery SHALL resume</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-reconnection-exhausted","title":"Scenario: Reconnection exhausted","text":"<ul> <li>WHEN all reconnection attempts fail</li> <li>THEN the connection SHALL transition to Disconnected status</li> </ul>"},{"location":"specs/websocket-proxy/#requirement-pubsub-message-routing","title":"Requirement: Pub/Sub Message Routing","text":"<p>Received WebSocket messages SHALL be forwardable to Dapr pub/sub topics. Each message SHALL include connection metadata in the published envelope.</p>"},{"location":"specs/websocket-proxy/#scenario-start-publishing","title":"Scenario: Start publishing","text":"<ul> <li>WHEN a client calls <code>StartPublish</code> on a connection with a topic name</li> <li>THEN all subsequently received WebSocket messages SHALL be published to that Dapr topic</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-message-envelope","title":"Scenario: Message envelope","text":"<ul> <li>WHEN a WebSocket message is published to Dapr</li> <li>THEN the envelope SHALL contain <code>WebSocketId</code>, <code>WebSocketUrl</code>, <code>Timestamp</code>, <code>Payload</code> (bytes), and <code>ContentType</code> (auto-detected from the WebSocket frame type: Text \u2192 \"text/plain\", Binary \u2192 \"application/octet-stream\")</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-stop-publishing","title":"Scenario: Stop publishing","text":"<ul> <li>WHEN a client calls <code>StopPublish</code> on a connection</li> <li>THEN message forwarding to Dapr SHALL stop</li> </ul>"},{"location":"specs/websocket-proxy/#requirement-service-architecture","title":"Requirement: Service Architecture","text":"<p>The WSM SHALL use the virtufin-api gRPC for all pubsub and state operations. The WSM SHALL NOT call <code>DaprClient.PublishEventAsync</code>, <code>GetStateAsync</code>, or any other Dapr pubsub/state API directly. The Dapr sidecar SHALL remain in use for service-invocation mTLS, distributed tracing, and metrics.</p>"},{"location":"specs/websocket-proxy/#scenario-service-publishes-user-data-event","title":"Scenario: Service publishes user-data event","text":"<ul> <li>WHEN the WSM forwards a received WebSocket message to a user-specified topic</li> <li>THEN it SHALL call <code>Pubsub.PublishEvent</code> on the virtufin-api</li> <li>AND it SHALL NOT call <code>DaprClient.PublishEventAsync</code> directly</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-service-persists-connection-state","title":"Scenario: Service persists connection state","text":"<ul> <li>WHEN the WSM saves or retrieves connection metadata</li> <li>THEN it SHALL call the API's <code>State</code> gRPC service</li> <li>AND it SHALL NOT call any Dapr state API directly</li> </ul>"},{"location":"specs/websocket-proxy/#requirement-connection-lifecycle-events","title":"Requirement: Connection Lifecycle Events","text":"<p>The WSM SHALL publish connection lifecycle events to the <code>websocketmanager.lifecycle</code> topic on every state transition. Events SHALL be CloudEvents v1.0 envelopes.</p>"},{"location":"specs/websocket-proxy/#scenario-initial-connect","title":"Scenario: Initial connect","text":"<ul> <li>WHEN a user successfully connects to an upstream WebSocket server</li> <li>THEN the WSM SHALL publish a <code>connection.connected</code> event with <code>connection_id</code>, <code>url</code>, <code>instance_id</code> in the data payload</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-user-initiated-disconnect","title":"Scenario: User-initiated disconnect","text":"<ul> <li>WHEN a user calls <code>Disconnect</code> on a connection</li> <li>THEN the WSM SHALL publish a <code>connection.disconnected</code> event</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-server-initiated-close","title":"Scenario: Server-initiated close","text":"<ul> <li>WHEN the upstream WebSocket server sends a Close frame</li> <li>THEN the WSM SHALL publish <code>connection.disconnected</code> (if <code>auto_reconnect=false</code>) OR <code>connection.reconnecting</code> (if <code>auto_reconnect=true</code>), then <code>connection.connected</code> (on successful reconnect) OR <code>connection.disconnected</code> (on exhausted reconnect)</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-network-error","title":"Scenario: Network error","text":"<ul> <li>WHEN the receive loop catches an unexpected exception</li> <li>THEN the WSM SHALL publish a <code>connection.error</code> event with <code>error_type</code> and <code>error_message</code> in the data payload</li> <li>AND the WSM SHALL follow the same path as server-initiated close (publish <code>connection.reconnecting</code> or <code>connection.disconnected</code>)</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-reconnecting","title":"Scenario: Reconnecting","text":"<ul> <li>WHEN the WSM is attempting to reconnect after a drop</li> <li>THEN the WSM SHALL publish a <code>connection.reconnecting</code> event with <code>connection_id</code>, <code>url</code>, <code>instance_id</code>, and <code>attempt</code> in the data payload</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-event-ordering","title":"Scenario: Event ordering","text":"<ul> <li>WHEN multiple events fire for the same <code>connection_id</code></li> <li>THEN they SHALL be delivered in causal order: <code>connection.error</code> precedes its resulting state transition; <code>connection.reconnecting</code> precedes its outcome (<code>connection.connected</code> or <code>connection.disconnected</code>)</li> </ul>"},{"location":"specs/websocket-proxy/#requirement-distributed-ownership","title":"Requirement: Distributed Ownership","text":"<p>Each connection SHALL be owned by exactly one WebSocket Manager instance. Only the owning instance may manipulate a connection.</p>"},{"location":"specs/websocket-proxy/#scenario-cross-instance-access-triggers-forward-or-reclaim","title":"Scenario: Cross-instance access triggers forward or reclaim","text":"<ul> <li>WHEN a request targets a connection not found locally</li> <li>THEN the handler SHALL read the connection's InstanceId from the state store</li> <li>AND if the owner is a live peer (per heartbeat), SHALL forward via direct gRPC</li> <li>AND if the owner is dead or unreachable, SHALL reclaim the connection (re-stamp InstanceId) and retry locally</li> </ul>"},{"location":"specs/websocket-proxy/#requirement-instance-heartbeats-and-peer-discovery","title":"Requirement: Instance Heartbeats and Peer Discovery","text":"<p>Each WSM instance SHALL periodically publish its identity to the Dapr state store with a TTL. The key SHALL be websocketmanager.heartbeat.{instanceId}. A peer index at websocketmanager.heartbeat.peers (keyed HashSet&lt;string&gt;) SHALL list all alive instance IDs, maintained via optimistic concurrency. This mechanism is implemented in the shared <code>Virtufin.Service</code> library (<code>InstanceHeartbeatService</code>/<code>InstanceHeartbeatOptions</code>), not duplicated per-service.</p>"},{"location":"specs/websocket-proxy/#scenario-heartbeat-publish-and-peer-discovery","title":"Scenario: Heartbeat publish and peer discovery","text":"<ul> <li>WHEN an instance starts</li> <li>THEN it SHALL publish a heartbeat every WEBSOCKETMANAGER_HEARTBEAT_INTERVAL_SECONDS (default 10) with TTL of 3\u00d7 that interval</li> <li>AND every WEBSOCKETMANAGER_DISCOVERY_INTERVAL_SECONDS (default 30) it SHALL read the peer index and each peer's heartbeat</li> <li>AND peers with a heartbeat age below WEBSOCKETMANAGER_STALE_THRESHOLD_SECONDS SHALL be registered in the local PeerRegistry</li> </ul>"},{"location":"specs/websocket-proxy/#requirement-orphaned-connection-reclamation","title":"Requirement: Orphaned Connection Reclamation","text":"<p>When an owning instance dies, the next RPC targeting an orphaned connection SHALL trigger lazy reclamation: the forward fails (gRPC Unavailable), the forwarder re-stamps the state-store record with its own instance ID, and retries the operation locally.</p>"},{"location":"specs/websocket-proxy/#scenario-pod-failure-triggers-lazy-reclamation","title":"Scenario: Pod failure triggers lazy reclamation","text":"<ul> <li>WHEN a WSM replica dies mid-connection</li> <li>AND the next RPC targets the connection</li> <li>THEN the forwarding handler SHALL attempt a direct gRPC call to the dead peer</li> <li>AND on Unavailable, SHALL re-stamp the connection record's InstanceId in the state store</li> <li>AND SHALL retry the operation locally</li> <li>AND the surviving replica SHALL mark the connection as Disconnected and, if auto_reconnect=true, attempt a fresh WebSocket</li> </ul>"},{"location":"specs/websocket-proxy/#requirement-per-connection-routing-via-heartbeat-based-forwarding","title":"Requirement: Per-connection routing via heartbeat-based forwarding","text":"<p>Each connection SHALL be owned by the instance whose IInstanceIdProvider.GetInstanceId() matches the connection record's InstanceId field in the Dapr state store. Connection-specific RPCs (Disconnect, StartPublish, StopPublish, Send, SendRaw) SHALL be routed to the owning instance via direct gRPC forwarding when they land on a different replica.</p>"},{"location":"specs/websocket-proxy/#scenario-disconnect-on-wrong-replica-forwarded-via-grpc","title":"Scenario: Disconnect on wrong replica forwarded via gRPC","text":"<ul> <li>WHEN replica A receives a Disconnect for a connection owned by replica B</li> <li>THEN replica A's handler SHALL catch ConnectionNotFoundException</li> <li>AND SHALL read the connection's InstanceId from the state store</li> <li>AND SHALL resolve replica B's gRPC endpoint from the PeerRegistry</li> <li>AND SHALL forward the Disconnect call via PeerChannelCache&lt;WebSocketManagerClient&gt;</li> <li>AND replica B SHALL execute the disconnect and return the result</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-forward-failure-triggers-reclamation","title":"Scenario: Forward failure triggers reclamation","text":"<ul> <li>WHEN forward to the owner fails with StatusCode.Unavailable</li> <li>THEN the handler SHALL reclaim the connection record</li> <li>AND SHALL retry the operation locally</li> </ul>"},{"location":"specs/websocket-proxy/#requirement-ownership-error-instrumentation","title":"Requirement: Ownership-error instrumentation","text":"<p>The WSM SHALL expose the following OpenTelemetry counters, registered under meter name <code>Virtufin.WebSocketManager</code>:</p> <ul> <li><code>websocketmanager.disconnect_failed_precondition_total{method}</code> \u2014 every <code>RpcException</code> raised with <code>StatusCode.FailedPrecondition</code> from any RPC method.</li> <li><code>websocketmanager.disconnect_ownership_miss_total{reason}</code> \u2014 every cross-instance ownership miss, with reason <code>not_owned</code>, <code>lapsed</code>.</li> <li><code>websocketmanager.disconnect_forwarded_total{method}</code> \u2014 every RPC call forwarded to the owning replica.</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-counter-emitted-on-cross-instance-miss","title":"Scenario: Counter emitted on cross-instance miss","text":"<ul> <li>WHEN an RPC method targets a connection owned by a different instance</li> <li>THEN the WSM SHALL increment <code>websocketmanager.disconnect_ownership_miss_total{reason=not_owned}</code> before raising the exception</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-counter-emitted-on-missed-then-reclaimed","title":"Scenario: Counter emitted on missed-then-reclaimed","text":"<ul> <li>WHEN an RPC method targets a connection whose owner has dropped from <code>knownInstances</code> for longer than <code>INSTANCE_STALE_THRESHOLD_SECONDS</code></li> <li>THEN the WSM SHALL increment <code>websocketmanager.disconnect_ownership_miss_total{reason=lapsed}</code> and the reclaimer SHALL re-stamp the connection to the handling instance</li> </ul>"},{"location":"specs/websocket-proxy/#requirement-connection-record-freshness","title":"Requirement: Connection record freshness","text":"<p>Connection records SHALL carry a Dapr state TTL (<code>CONNECTION_RECORD_TTL_SECONDS</code>, default 300). The TTL SHALL be refreshed on every successful <code>Send</code> or <code>SendRaw</code> call. Records whose TTL elapses with no traffic SHALL be removed by the state store without explicit reclaimer intervention.</p>"},{"location":"specs/websocket-proxy/#scenario-send-refreshes-ttl","title":"Scenario: Send refreshes TTL","text":"<ul> <li>WHEN a <code>Send</code> or <code>SendRaw</code> call succeeds against an open connection</li> <li>THEN the WSM SHALL re-write the connection record with a refreshed TTL</li> </ul>"},{"location":"specs/websocket-proxy/#scenario-idle-connection-expires-from-state","title":"Scenario: Idle connection expires from state","text":"<ul> <li>WHEN a connection record's TTL elapses without any <code>Send</code> traffic</li> <li>THEN the next reclaimer sweep SHALL observe the absence without raising an error</li> </ul>"},{"location":"specs/worker-management/","title":"Worker Management","text":""},{"location":"specs/worker-management/#purpose","title":"Purpose","text":"<p>Event-driven worker management system enabling deployment and execution of workers written in multiple languages (Python, C#, .NET DLL, NativeAOT) that subscribe to Dapr pub/sub topics and process CloudEvents. Multi-instance coordination uses Dapr competing consumers (shared app-id); see the horizontal scalability requirement below.</p> <p>Topic and state-key naming for worker lifecycle events is defined in pubsub-topics/spec.md. The lifecycle topic is <code>workmanager.lifecycle</code>; finer-grained infra events (heartbeats, instance state changes) live under <code>workmanager.infra.*</code>.</p>"},{"location":"specs/worker-management/#requirements","title":"Requirements","text":""},{"location":"specs/worker-management/#requirement-worker-lifecycle-management","title":"Requirement: Worker Lifecycle Management","text":"<p>The WorkManager SHALL support creating, starting, stopping, and deleting workers. Workers SHALL persist their configuration and restore automatically on service restart.</p>"},{"location":"specs/worker-management/#scenario-create-and-start-a-worker","title":"Scenario: Create and start a worker","text":"<ul> <li>WHEN a client creates a worker with a topic subscription and code payload</li> <li>THEN the worker SHALL be persisted to the state store, subscribed to the topic, and set to Running status</li> </ul>"},{"location":"specs/worker-management/#scenario-hot-reload-worker-code","title":"Scenario: Hot-reload worker code","text":"<ul> <li>WHEN a client updates a running worker's code</li> <li>THEN the worker SHALL switch to the new code without restarting the service</li> </ul>"},{"location":"specs/worker-management/#scenario-auto-recovery-on-restart","title":"Scenario: Auto-recovery on restart","text":"<ul> <li>WHEN the WorkManager service restarts</li> <li>THEN all previously persisted workers SHALL be restored to their saved state before the health check passes</li> </ul>"},{"location":"specs/worker-management/#scenario-worker-version-history","title":"Scenario: Worker version history","text":"<ul> <li>WHEN a worker's code is updated</li> <li>THEN the previous code version SHALL be preserved in a versioned history</li> </ul>"},{"location":"specs/worker-management/#requirement-service-architecture","title":"Requirement: Service Architecture","text":"<p>The WorkManager SHALL use the virtufin-api gRPC for all state operations. The WorkManager SHALL NOT call <code>DaprClient.GetStateAsync</code>, <code>SaveStateAsync</code>, <code>DeleteStateAsync</code>, or any other Dapr state API directly. The Dapr sidecar SHALL remain in use for service-invocation mTLS, distributed tracing, and metrics.</p>"},{"location":"specs/worker-management/#scenario-service-persists-worker-state","title":"Scenario: Service persists worker state","text":"<ul> <li>WHEN the WorkManager saves or retrieves worker configuration</li> <li>THEN it SHALL call the API's <code>State</code> gRPC service</li> <li>AND it SHALL NOT call any Dapr state API directly</li> </ul>"},{"location":"specs/worker-management/#scenario-service-subscribes-to-a-worker-topic","title":"Scenario: Service subscribes to a worker topic","text":"<ul> <li>WHEN the WorkManager subscribes to a worker's input topic</li> <li>THEN it SHALL call <code>Pubsub.Subscribe</code> on the virtufin-api</li> <li>AND it SHALL NOT call <code>DaprPublishSubscribeClient.SubscribeAsync</code> directly</li> </ul>"},{"location":"specs/worker-management/#requirement-worker-lifecycle-events","title":"Requirement: Worker Lifecycle Events","text":"<p>The WorkManager SHALL publish worker lifecycle events to the <code>workmanager.lifecycle</code> topic on every state transition. Topic, ce-type, and state-key conventions are defined in pubsub-topics/spec.md. Events SHALL be CloudEvents v1.0 envelopes with <code>ce-type = com.virtufin.workmanager.lifecycle.&lt;state&gt;</code>.</p>"},{"location":"specs/worker-management/#scenario-worker-created","title":"Scenario: Worker created","text":"<ul> <li>WHEN a worker is created</li> <li>THEN the WorkManager SHALL publish a <code>worker.created</code> event with <code>worker_id</code> and <code>topic</code> in the data payload</li> </ul>"},{"location":"specs/worker-management/#scenario-worker-started","title":"Scenario: Worker started","text":"<ul> <li>WHEN a worker is started</li> <li>THEN the WorkManager SHALL publish a <code>worker.started</code> event</li> </ul>"},{"location":"specs/worker-management/#scenario-worker-stopped","title":"Scenario: Worker stopped","text":"<ul> <li>WHEN a worker is stopped</li> <li>THEN the WorkManager SHALL publish a <code>worker.stopped</code> event</li> </ul>"},{"location":"specs/worker-management/#scenario-worker-error","title":"Scenario: Worker error","text":"<ul> <li>WHEN a worker process fails or throws</li> <li>THEN the WorkManager SHALL publish a <code>worker.error</code> event with <code>error_type</code> and <code>error_message</code> in the data payload</li> </ul>"},{"location":"specs/worker-management/#requirement-polyglot-worker-engines","title":"Requirement: Polyglot Worker Engines","text":"<p>The WorkManager SHALL support executing worker code in multiple languages via a pluggable engine architecture. Built-in engines SHALL include Python (subprocess), C# (Roslyn runtime compilation), pre-compiled .NET DLL (in-process, the default <code>DotNetDllEngine</code> via <code>hostfxr</code> + <code>AssemblyLoadContext</code>), and a native DLL engine (in-process, <code>NativeDllEngine</code>, see the Native DLL Engine / Native Worker ABI requirements below).</p>"},{"location":"specs/worker-management/#scenario-python-worker-execution","title":"Scenario: Python worker execution","text":"<ul> <li>WHEN a worker with a Python engine processes a message</li> <li>THEN the worker code SHALL execute in a dedicated Python subprocess with a configurable timeout</li> </ul>"},{"location":"specs/worker-management/#scenario-c-source-worker-execution","title":"Scenario: C# source worker execution","text":"<ul> <li>WHEN a worker with a C# source engine processes a message</li> <li>THEN the code SHALL be compiled at runtime via Roslyn and executed in-process</li> </ul>"},{"location":"specs/worker-management/#scenario-dll-worker-execution-in-process-default","title":"Scenario: DLL worker execution (in-process, default)","text":"<ul> <li>WHEN a worker with MIME type <code>application/x-dotnet-dll</code> processes a message</li> <li>THEN the worker DLL SHALL be loaded into an isolated <code>AssemblyLoadContext</code> and executed in-process in the WorkManager, with sub-microsecond per-call latency</li> </ul>"},{"location":"specs/worker-management/#scenario-engine-extension","title":"Scenario: Engine extension","text":"<ul> <li>WHEN a new engine implementation conforms to the <code>IEngine</code> interface</li> <li>THEN it SHALL be registrable in the engine registry without modifying core WorkManager code</li> </ul>"},{"location":"specs/worker-management/#requirement-event-driven-message-processing","title":"Requirement: Event-Driven Message Processing","text":"<p>Workers SHALL subscribe to Dapr pub/sub topics and process incoming CloudEvents. The output CloudEvent type field SHALL determine the publish topic.</p>"},{"location":"specs/worker-management/#scenario-message-processing","title":"Scenario: Message processing","text":"<ul> <li>WHEN a CloudEvent is published to a topic a worker subscribes to</li> <li>THEN the worker's code SHALL be invoked with the event data and any output events SHALL be published to the topic matching their <code>type</code> field</li> </ul>"},{"location":"specs/worker-management/#scenario-concurrent-message-handling","title":"Scenario: Concurrent message handling","text":"<ul> <li>WHEN multiple messages arrive for the same worker</li> <li>THEN they SHALL be processed concurrently (the engine's <code>ProcessAsync</code> is re-entrant; the worker author is responsible for internal synchronization)</li> </ul>"},{"location":"specs/worker-management/#requirement-cross-instance-coordination","title":"Requirement: Cross-Instance Coordination","text":"<p>For topics with multiple WorkManager instances, Dapr competing consumers SHALL be used as the coordination primitive. When multiple replicas share a Dapr app-id, Dapr's consumer group semantics deliver each published message to exactly one instance \u2014 the instances compete, they do not load-share. Messages delivered to an instance that has no matching worker registered for the topic SHALL be dropped, acked with no redelivery (an explicit business-logic skip per the Pub/Sub Topics spec's \"Retry Policy on Dapr Subscribe Exceptions\" requirement, not a processing failure).</p> <p>This model replaces the prior distributed-lock approach (removed in LIBRARY_VERSION 0.2.5). The lock-based group coordination (<code>GroupLockOptions</code>, <code>LockContentionException</code>, <code>AcquireGroupLockAsync</code>, <code>ReleaseGroupLockAsync</code>, <code>VIRTUFIN_WORKER_GROUP</code>) is no longer supported. Operators MUST NOT deploy multiple WM replicas expecting throughput scaling on a single topic \u2014 the competing-consumer model ensures exactly-once delivery at the process level, not thread-pool parallelism.</p>"},{"location":"specs/worker-management/#scenario-message-lands-on-a-replica-with-no-matching-worker","title":"Scenario: Message lands on a replica with no matching worker","text":"<ul> <li>WHEN Dapr's competing-consumer delivery routes a message to a WorkManager instance that has no worker registered for that topic</li> <li>THEN the message SHALL be dropped and acked with no redelivery (an explicit business-logic skip, not a processing failure)</li> <li>AND no automatic re-routing to a replica that does have a matching worker SHALL occur</li> </ul>"},{"location":"specs/worker-management/#requirement-worker-code-sources","title":"Requirement: Worker Code Sources","text":"<p>Workers SHALL accept code from inline Base64-encoded content or from HTTP(S) URLs. URL-based sources SHALL be validated against SSRF and host allowlist constraints as defined in the cross-cutting security requirements. URL-based sources MAY carry a per-request credential (see Worker Source Authentication) used only for that fetch.</p>"},{"location":"specs/worker-management/#scenario-inline-code-worker","title":"Scenario: Inline code worker","text":"<ul> <li>WHEN a worker is created with a Base64-encoded code string</li> <li>THEN the decoded code SHALL be passed to the engine for execution</li> </ul>"},{"location":"specs/worker-management/#scenario-url-code-worker","title":"Scenario: URL code worker","text":"<ul> <li>WHEN a worker is created with a URL pointing to code</li> <li>THEN the code SHALL be fetched, SHA-256 verified, and passed to the engine</li> </ul>"},{"location":"specs/worker-management/#scenario-blocked-url","title":"Scenario: Blocked URL","text":"<ul> <li>WHEN a worker's code URL targets a private IP or a host not in the allowlist</li> <li>THEN the worker creation SHALL fail with a security rejection</li> </ul>"},{"location":"specs/worker-management/#requirement-python-sandboxing","title":"Requirement: Python Sandboxing","text":"<p>Python worker subprocesses SHALL run with restricted interpreter flags and a package allowlist. All non-allowed imports SHALL be blocked.</p>"},{"location":"specs/worker-management/#scenario-allowed-import","title":"Scenario: Allowed import","text":"<ul> <li>WHEN a Python worker imports a package listed in <code>PYTHON_ALLOWED_PACKAGES</code></li> <li>THEN the import SHALL succeed</li> </ul>"},{"location":"specs/worker-management/#scenario-blocked-import","title":"Scenario: Blocked import","text":"<ul> <li>WHEN a Python worker imports a package not in the allowlist</li> <li>THEN the import SHALL fail with an error</li> </ul>"},{"location":"specs/worker-management/#requirement-worker-contract-iworker","title":"Requirement: Worker Contract (IWorker)","text":"<p>The <code>IWorker</code> interface SHALL define an asynchronous <code>ProcessAsync</code> method returning <code>Task&lt;CloudEvent?&gt;</code>. WorkerBase SHALL accept constructor parameters <code>(responseType, source)</code> instead of abstract property overrides for CloudEvent routing.</p>"},{"location":"specs/worker-management/#scenario-async-worker-process","title":"Scenario: Async worker process","text":"<ul> <li>WHEN the WorkManager invokes a worker's <code>ProcessAsync</code> method</li> <li>THEN the call SHALL be awaited asynchronously without blocking threads</li> </ul>"},{"location":"specs/worker-management/#scenario-constructor-based-configuration","title":"Scenario: Constructor-based configuration","text":"<ul> <li>WHEN a worker extends WorkerBase or CommandWorker</li> <li>THEN it SHALL pass the response CloudEvent type and source URI via the constructor, not via abstract property overrides</li> </ul>"},{"location":"specs/worker-management/#scenario-apiworker-with-pre-configured-apiclient","title":"Scenario: ApiWorker with pre-configured ApiClient","text":"<ul> <li>WHEN a worker extends ApiWorker with <code>apiHost</code>, <code>apiPort</code>, <code>responseEventType</code>, and <code>source</code></li> <li>THEN the base class SHALL create an <code>ApiClient</code> at the configured host/port and provide it to the abstract <code>HandleAsync(CloudEvent, JsonNode, ApiClient)</code> method (ApiWorker's <code>ProcessAsync(CloudEvent, JsonNode)</code> override dispatches to it)</li> </ul>"},{"location":"specs/worker-management/#scenario-apicommandworker-with-pre-configured-apiclient","title":"Scenario: ApiCommandWorker with pre-configured ApiClient","text":"<ul> <li>WHEN a worker extends ApiCommandWorker with <code>apiHost</code>, <code>apiPort</code>, <code>responseEventType</code>, and <code>source</code></li> <li>THEN the base class SHALL create an <code>ApiClient</code> at the configured host/port and make it available during <code>HandleCommandAsync</code></li> </ul>"},{"location":"specs/worker-management/#requirement-commandworker-base-class","title":"Requirement: CommandWorker Base Class","text":"<p>A <code>CommandWorker</code> abstract class SHALL extend <code>WorkerBase</code> to simplify command-based workers. It SHALL parse the CloudEvent data as JSON, extract a <code>\"command\"</code> field, and dispatch to the abstract <code>HandleCommandAsync</code> method. Unknown commands SHALL produce automatic error responses.</p>"},{"location":"specs/worker-management/#scenario-command-dispatch","title":"Scenario: Command dispatch","text":"<ul> <li>WHEN a CloudEvent arrives with <code>{\"command\": \"hello\", ...}</code> as data</li> <li>THEN CommandWorker SHALL parse the JSON, extract <code>\"hello\"</code>, and call <code>HandleCommandAsync(input, \"hello\", node)</code></li> </ul>"},{"location":"specs/worker-management/#scenario-unknown-command","title":"Scenario: Unknown command","text":"<ul> <li>WHEN CommandWorker cannot match the command to any handler</li> <li>THEN it SHALL return an error response with <code>\"Unknown command: &lt;command&gt;\"</code></li> </ul>"},{"location":"specs/worker-management/#scenario-correlation-id-propagation","title":"Scenario: Correlation ID propagation","text":"<ul> <li>WHEN CommandWorker creates a response via <code>Response(input, ...)</code> or <code>Error(input, ...)</code></li> <li>THEN the <code>correlationid</code> from the input CloudEvent SHALL be propagated to the output CloudEvent via the <code>WithCorrelationId</code> extension method</li> </ul>"},{"location":"specs/worker-management/#requirement-correlation-id-propagation","title":"Requirement: Correlation ID Propagation","text":"<p>Worker responses SHALL propagate the <code>correlationid</code> extension attribute from the input CloudEvent to the output CloudEvent. The <code>CloudEvent.WithCorrelationId(input)</code> extension method SHALL handle this as a no-op when absent.</p>"},{"location":"specs/worker-management/#scenario-response-with-correlation-id","title":"Scenario: Response with correlation ID","text":"<ul> <li>WHEN a worker creates a <code>Response()</code> or <code>Error()</code> via WorkerBase helpers</li> <li>THEN the output CloudEvent SHALL carry the same <code>correlationid</code> as the input CloudEvent if present</li> </ul>"},{"location":"specs/worker-management/#scenario-input-without-correlation-id","title":"Scenario: Input without correlation ID","text":"<ul> <li>WHEN a worker processes an input CloudEvent without a <code>correlationid</code> extension</li> <li>THEN the output CloudEvent SHALL NOT have a <code>correlationid</code> extension</li> </ul>"},{"location":"specs/worker-management/#requirement-generic-commandworker-base-class","title":"Requirement: Generic CommandWorker Base Class","text":"<p>The <code>Virtufin.Worker.DevKit</code> package SHALL provide a generic <code>CommandWorker&lt;T&gt; where T : struct, Enum</code> and a generic <code>ApiCommandWorker&lt;T&gt; where T : struct, Enum</code> as the canonical base classes for command-based workers. The wire protocol of the CloudEvent <code>data.command</code> field SHALL be the lowercase identifier of the enum value (e.g. enum <code>WebSocketManagerCommand.create</code> corresponds to the wire string <code>\"create\"</code>). The base class SHALL parse the wire string with <code>Enum.TryParse&lt;T&gt;(command, ignoreCase: true, out _)</code> and dispatch to an abstract <code>HandleAsync(CloudEvent, T, JsonNode)</code> method. An unknown wire command SHALL produce an error response. The non-generic <code>CommandWorker</code> / <code>ApiCommandWorker</code> types are removed (alpha, no backward compatibility).</p>"},{"location":"specs/worker-management/#scenario-generic-dispatch-on-valid-command","title":"Scenario: Generic dispatch on valid command","text":"<ul> <li>WHEN a CloudEvent arrives with <code>{\"command\": \"create\", ...}</code> and the worker is <code>CommandWorker&lt;MyEnum&gt;</code> where <code>MyEnum.create</code> exists</li> <li>THEN the base SHALL parse <code>\"create\"</code> into <code>MyEnum.create</code> and invoke <code>HandleAsync(input, MyEnum.create, node)</code></li> </ul>"},{"location":"specs/worker-management/#scenario-wire-name-identifier-lowercase","title":"Scenario: Wire name == identifier (lowercase)","text":"<ul> <li>WHEN a worker defines an enum value <code>WebSocketManagerCommand.create</code></li> <li>THEN the corresponding wire command string SHALL be the lowercase identifier <code>\"create\"</code></li> </ul>"},{"location":"specs/worker-management/#scenario-unknown-command_1","title":"Scenario: Unknown command","text":"<ul> <li>WHEN the wire command string does not match any enum value</li> <li>THEN the base SHALL return an error response with message <code>\"Unknown command: &lt;command&gt;\"</code></li> </ul>"},{"location":"specs/worker-management/#scenario-missing-command-field","title":"Scenario: Missing command field","text":"<ul> <li>WHEN the CloudEvent data has no <code>\"command\"</code> field</li> <li>THEN the base SHALL return an error response with message <code>\"Missing 'command' field\"</code></li> </ul>"},{"location":"specs/worker-management/#scenario-malformed-json","title":"Scenario: Malformed JSON","text":"<ul> <li>WHEN the CloudEvent data cannot be parsed as JSON or parses to null</li> <li>THEN the base SHALL return an error response with message <code>\"Failed to parse command JSON\"</code> (or wrap the parser exception in <code>\"Internal error: \u2026\"</code>)</li> </ul>"},{"location":"specs/worker-management/#scenario-generic-apicommandworker","title":"Scenario: Generic ApiCommandWorker","text":"<ul> <li>WHEN a worker extends <code>ApiCommandWorker&lt;T&gt;</code> and the <code>api_host</code> field is present in the command JSON</li> <li>THEN the base SHALL create or reconfigure the <code>ApiClient</code> for the parsed host/port (default port 5002) and make it available via the <code>Api</code> property during <code>HandleAsync</code></li> </ul>"},{"location":"specs/worker-management/#scenario-backend-invocation-via-dynamic-gateway","title":"Scenario: Backend invocation via dynamic gateway","text":"<ul> <li>WHEN a worker invokes a backend service via <code>dynamic gateway = Api.Gateway; await gateway.&lt;service&gt;.&lt;method&gt;(requestData);</code></li> <li>THEN the <code>ServiceClient</code> returned by <code>&lt;service&gt;</code> SHALL resolve to the named service and <code>&lt;method&gt;</code> SHALL invoke the corresponding method via the API Gateway</li> </ul>"},{"location":"specs/worker-management/#scenario-new-enum-value-triggers-compiler-warning-on-the-switch","title":"Scenario: New enum value triggers compiler warning on the switch","text":"<ul> <li>WHEN a new value is added to the command enum and the worker's <code>HandleAsync</code> switch expression does not handle it</li> <li>THEN the C# compiler SHALL emit a warning (CS8509 or equivalent) for the unhandled enum value</li> </ul>"},{"location":"specs/worker-management/#requirement-in-process-dotnet-dll-engine","title":"Requirement: In-Process DotNet DLL Engine","text":"<p>The WorkManager SHALL support an in-process <code>DotNetDllEngine</code> (default for MIME type <code>application/x-dotnet-dll</code>) that loads pre-compiled .NET worker DLLs directly into the AOT-compiled WorkManager process. The engine SHALL embed the .NET runtime (<code>CoreCLR</code>) into the host process on first use via the <code>hostfxr</code> C API and SHALL load the worker DLL into a per-worker, collectible <code>AssemblyLoadContext</code>. The engine SHALL be AOT-compatible: the engine code itself is compiled with <code>&lt;IsAotCompatible&gt;true&lt;/IsAotCompatible&gt;</code> and SHALL NOT use reflection on its own types; the only reflection SHALL be on the JIT-loaded worker assembly.</p>"},{"location":"specs/worker-management/#scenario-first-loadcodeasync-initializes-coreclr","title":"Scenario: First LoadCodeAsync initializes CoreCLR","text":"<ul> <li>WHEN the first <code>LoadCodeAsync</code> is called for MIME type <code>application/x-dotnet-dll</code></li> <li>THEN the engine SHALL load <code>libhostfxr</code> via <code>NativeLibrary.Load</code> and call <code>hostfxr_initialize_for_runtime_config</code> to initialize <code>CoreCLR</code> in the host process; the cost SHALL be paid once per WorkManager process</li> </ul>"},{"location":"specs/worker-management/#scenario-subsequent-loadcodeasync-in-the-same-process","title":"Scenario: Subsequent LoadCodeAsync in the same process","text":"<ul> <li>WHEN a second <code>LoadCodeAsync</code> is called after <code>CoreCLR</code> is already initialized</li> <li>THEN the engine SHALL skip the <code>hostfxr</code> initialization and load the new worker DLL directly</li> </ul>"},{"location":"specs/worker-management/#scenario-per-worker-assemblyloadcontext","title":"Scenario: Per-worker AssemblyLoadContext","text":"<ul> <li>WHEN a <code>LoadCodeAsync</code> is called with a worker nupkg</li> <li>THEN the engine SHALL extract the worker DLL (and its sibling dependencies) from the nupkg's <code>lib/&lt;tfm&gt;/</code> entries, create a new collectible <code>AssemblyLoadContext</code>, and load the worker assembly into it</li> </ul>"},{"location":"specs/worker-management/#scenario-devkit-resolution","title":"Scenario: DevKit resolution","text":"<ul> <li>WHEN the JIT-loaded worker assembly references <code>Virtufin.Worker.DevKit</code></li> <li>THEN the engine SHALL resolve the reference to the AOT-compiled <code>Virtufin.Worker.DevKit</code> instance already loaded in the WorkManager process (so the worker and the engine see the same type identity)</li> </ul>"},{"location":"specs/worker-management/#scenario-direct-in-process-dispatch","title":"Scenario: Direct in-process dispatch","text":"<ul> <li>WHEN <code>ProcessAsync</code> is called on the engine with a CloudEvent</li> <li>THEN the engine SHALL call the worker's <code>IWorker.ProcessAsync</code> directly, with no socket, no JSON serialization, and no subprocess scheduling; the per-call latency SHALL be sub-microsecond</li> </ul>"},{"location":"specs/worker-management/#scenario-runtime-discovery-on-target","title":"Scenario: Runtime discovery on target","text":"<ul> <li>WHEN the WorkManager runs on a target that does not have a compatible .NET runtime installed</li> <li>THEN the first <code>LoadCodeAsync</code> SHALL fail with a clear error message that names the missing runtime and the discovery paths (<code>DOTNET_ROOT</code>, system install, side-by-side <code>host/fxr/&lt;version&gt;/</code>)</li> </ul>"},{"location":"specs/worker-management/#scenario-roll-forward-to-a-newer-runtime","title":"Scenario: Roll-forward to a newer runtime","text":"<ul> <li>WHEN the WorkManager is built for .NET 10 and the target has a .NET 11+ runtime installed</li> <li>THEN the engine SHALL use the newer runtime; the generated <code>runtimeconfig.json</code> SHALL declare <code>\"rollForward\": \"Major\"</code></li> </ul>"},{"location":"specs/worker-management/#requirement-native-dll-engine","title":"Requirement: Native DLL Engine","text":"<p>The WorkManager SHALL support a <code>NativeDllEngine</code> for executing workers shipped as a per-architecture native shared library (<code>.so</code> / <code>.dll</code> / <code>.dylib</code>). The engine SHALL run in-process inside the (NativeAOT-compiled) WorkManager and SHALL NOT require a separate worker-host subprocess or a JIT build stage. The engine SHALL be registered for MIME type <code>application/x-native-dll</code>. Per-architecture entries SHALL live under <code>runtimes/&lt;rid&gt;/native/</code> inside the worker zip, matching the NuGet convention for native packages.</p>"},{"location":"specs/worker-management/#scenario-native-worker-execution","title":"Scenario: Native worker execution","text":"<ul> <li>WHEN a worker with MIME type <code>application/x-native-dll</code> processes a   CloudEvent</li> <li>THEN the engine SHALL <code>NativeLibrary.Load</code> the matching   <code>runtimes/&lt;rid&gt;/native/&lt;file&gt;</code> entry from the worker zip, resolve the   <code>entry_point</code> and <code>free_result</code> exports declared in <code>manifest.json</code>,   marshal the input CloudEvent to a FlatBuffer, invoke the native   <code>Process</code> function, and deserialize the returned FlatBuffer into a   <code>WorkerResponse</code>.</li> </ul>"},{"location":"specs/worker-management/#scenario-per-architecture-entry-selection","title":"Scenario: Per-architecture entry selection","text":"<ul> <li>WHEN a worker zip contains entries under   <code>runtimes/&lt;rid&gt;/native/</code> for at least one supported RID   (<code>linux-x64</code>, <code>linux-arm64</code>)</li> <li>THEN the engine SHALL select the entry whose <code>&lt;rid&gt;</code> directory   matches <code>RuntimeInformation.RuntimeIdentifier</code> of the running process   and SHALL load the corresponding <code>lib&lt;library&gt;.so</code>,   <code>&lt;library&gt;.dll</code>, or <code>lib&lt;library&gt;.dylib</code> per the platform (see   Native Worker Library Naming).</li> </ul>"},{"location":"specs/worker-management/#scenario-unsupported-architecture","title":"Scenario: Unsupported architecture","text":"<ul> <li>WHEN the worker zip does not contain a <code>runtimes/&lt;rid&gt;/native/</code>   entry for the running architecture</li> <li>THEN the engine SHALL throw <code>NotSupportedException</code> listing the   supported archs (<code>linux-x64</code>, <code>linux-arm64</code>).</li> </ul>"},{"location":"specs/worker-management/#scenario-missing-manifest","title":"Scenario: Missing manifest","text":"<ul> <li>WHEN the worker zip does not contain <code>manifest.json</code> at the root</li> <li>THEN the engine SHALL refuse to load the worker and throw a clear   error naming the missing file.</li> </ul>"},{"location":"specs/worker-management/#scenario-abi-version-mismatch","title":"Scenario: ABI version mismatch","text":"<ul> <li>WHEN the <code>manifest.json</code> declares an <code>abi_version</code> that is not in   the engine's supported set</li> <li>THEN the engine SHALL refuse to load the worker and throw a clear   error naming the supported versions.</li> </ul>"},{"location":"specs/worker-management/#requirement-native-worker-abi","title":"Requirement: Native Worker ABI","text":"<p>The <code>NativeDllEngine</code> SHALL define and enforce a stable C ABI between the managed engine and the native worker. The ABI SHALL consist of: (a) a FlatBuffers schema for the CloudEvent wire format; (b) two C function exports <code>Process</code> and <code>FreeResult</code>; (c) a <code>VirtufinHost</code> struct carrying host callbacks; (d) a <code>manifest.json</code> declaring the ABI version, the library basename, and the export names. The engine SHALL vendor a <code>virtufin_worker_api.h</code> header for distribution to worker authors.</p>"},{"location":"specs/worker-management/#scenario-abi-struct-layout","title":"Scenario: ABI struct layout","text":"<ul> <li>WHEN a native worker is loaded</li> <li>THEN the engine SHALL pass a <code>VirtufinHost*</code> whose first field is   an opaque <code>engine_ptr</code> (a <code>GCHandle</code> to the engine instance), whose   second field is <code>abi_version</code> (currently <code>1</code>), and whose remaining   fields are C function pointers <code>log</code>, <code>gateway_call</code>, and   <code>free_response</code>.</li> </ul>"},{"location":"specs/worker-management/#scenario-cloudevent-flatbuffer-input","title":"Scenario: CloudEvent FlatBuffer input","text":"<ul> <li>WHEN the engine invokes <code>Process(host, in_buf, in_len, &amp;out_buf, &amp;out_len)</code></li> <li>THEN <code>in_buf</code> SHALL point to a FlatBuffer-encoded <code>CloudEvent</code>   matching the vendored <code>worker_api.fbs</code> schema. The   <code>extensions[correlationid]</code> field SHALL carry the correlation id from   the input CloudEvent when present.</li> </ul>"},{"location":"specs/worker-management/#scenario-workerresponse-flatbuffer-output","title":"Scenario: WorkerResponse FlatBuffer output","text":"<ul> <li>WHEN <code>Process</code> returns <code>0</code></li> <li>THEN <code>*out_buf</code> SHALL point to a FlatBuffer-encoded   <code>WorkerResponse</code> in which exactly one of <code>result_event</code> or   <code>error_message</code> is set. If <code>result_event</code> is set, the engine SHALL   publish it on the reply topic derived from the input CloudEvent. If   <code>error_message</code> is set, the engine SHALL surface a <code>worker.error</code>   lifecycle event and publish an error response if the worker did not   specify one.</li> </ul>"},{"location":"specs/worker-management/#scenario-freeresult-pairing","title":"Scenario: FreeResult pairing","text":"<ul> <li>WHEN <code>Process</code> returns a non-null <code>*out_buf</code></li> <li>THEN the engine SHALL call <code>FreeResult(out_buf)</code> exactly once,   including on the error path. The worker SHALL use the same allocator   for the <code>*out_buf</code> allocation that <code>FreeResult</code> releases.</li> </ul>"},{"location":"specs/worker-management/#scenario-logging-callback","title":"Scenario: Logging callback","text":"<ul> <li>WHEN the native worker calls <code>host-&gt;log(level, message)</code></li> <li>THEN the engine SHALL map <code>level</code> (<code>0=trace</code>, <code>1=debug</code>, <code>2=info</code>,   <code>3=warn</code>, <code>4=error</code>) to the corresponding   <code>Microsoft.Extensions.Logging.LogLevel</code> and forward <code>message</code> to the   engine's structured logger. The lifetime of <code>message</code> SHALL be limited   to the call.</li> </ul>"},{"location":"specs/worker-management/#scenario-gateway-call-indirection","title":"Scenario: Gateway call indirection","text":"<ul> <li>WHEN the native worker calls <code>host-&gt;gateway_call(service, method, request, request_len, &amp;response, &amp;response_len)</code></li> <li>THEN the engine SHALL deserialize <code>request</code> as JSON into a   <code>Dictionary&lt;string, object?&gt;</code>, invoke   <code>ApiClient.InvokeAsync(service, method, dict)</code>, JSON-serialize the   returned dictionary into <code>*response</code> (allocated with   <code>Marshal.AllocCoTaskMem</code>), and return <code>0</code> on success or non-zero on   internal failure. The worker SHALL free <code>*response</code> via   <code>host-&gt;free_response(response)</code>.</li> </ul>"},{"location":"specs/worker-management/#scenario-manifest-format","title":"Scenario: Manifest format","text":"<ul> <li>WHEN the engine parses the worker's <code>manifest.json</code></li> <li>THEN the JSON object SHALL contain <code>abi_version</code> (integer) and   <code>library</code> (non-empty string matching <code>[A-Za-z0-9_.-]+</code>), and MAY   contain <code>entry_point</code> (string, default <code>\"Process\"</code>) and <code>free_result</code>   (string, default <code>\"FreeResult\"</code>). Additional fields SHALL be ignored   by v1. A missing or empty <code>library</code> field SHALL cause the engine to   refuse the worker with a clear error.</li> </ul>"},{"location":"specs/worker-management/#requirement-native-worker-identity-and-lifecycle","title":"Requirement: Native Worker Identity and Lifecycle","text":"<p>The <code>NativeDllEngine</code> SHALL communicate worker identity to the native worker via process-level environment variables set at LoadCodeAsync time, and SHALL release the loaded library and all engine-owned resources on worker stop.</p>"},{"location":"specs/worker-management/#scenario-worker-identity-env-vars","title":"Scenario: Worker identity env vars","text":"<ul> <li>WHEN the engine loads a native worker</li> <li>THEN it SHALL set <code>VIRTUFIN_WORKER_ID</code> to the worker GUID and   <code>VIRTUFIN_WORKER_TOPIC</code> to the worker's input topic. The worker SHALL   read these once at initialization.</li> </ul>"},{"location":"specs/worker-management/#scenario-correlation-id-from-input","title":"Scenario: Correlation id from input","text":"<ul> <li>WHEN the engine marshals the input CloudEvent for a <code>Process</code> call</li> <li>THEN the FlatBuffer's <code>extensions[correlationid]</code> attribute SHALL   carry the input CloudEvent's <code>correlationid</code> extension attribute if   present. Workers SHALL read it from the FlatBuffer, not from env vars.</li> </ul>"},{"location":"specs/worker-management/#scenario-concurrent-process-calls","title":"Scenario: Concurrent Process calls","text":"<ul> <li>WHEN multiple <code>Process</code> invocations are made against the same loaded   library</li> <li>THEN the engine SHALL allow them to proceed concurrently. The native   worker SHALL be reentrant; any per-worker mutable state SHALL be   protected by the worker's own synchronization primitives.</li> </ul>"},{"location":"specs/worker-management/#scenario-reload-under-load","title":"Scenario: Reload under load","text":"<ul> <li>WHEN <code>LoadCodeAsync</code> is called while a <code>ProcessAsync</code> is in flight</li> <li>THEN the engine SHALL block the reload until the in-flight call   completes. The new code SHALL take effect for subsequent calls only.</li> </ul>"},{"location":"specs/worker-management/#scenario-cleanup-on-stop","title":"Scenario: Cleanup on stop","text":"<ul> <li>WHEN the engine is disposed or the worker is stopped</li> <li>THEN the engine SHALL call <code>NativeLibrary.Free</code> on the loaded   library, free the <code>GCHandle</code> for the engine pointer, delete the temp   file holding the DLL, and restore the prior values of   <code>VIRTUFIN_WORKER_ID</code> and <code>VIRTUFIN_WORKER_TOPIC</code>   if they were set.</li> </ul>"},{"location":"specs/worker-management/#requirement-native-worker-caveats","title":"Requirement: Native Worker Caveats","text":"<p>The <code>NativeDllEngine</code> SHALL document its isolation and timeout characteristics in the engine README and the worker author guide.</p>"},{"location":"specs/worker-management/#scenario-in-process-crash-isolation-caveat","title":"Scenario: In-process crash isolation caveat","text":"<ul> <li>WHEN a native worker raises an unrecoverable error (segmentation   fault, illegal instruction, stack overflow from native code)</li> <li>THEN the WorkManager process SHALL terminate because the error   cannot be caught in a way that leaves the .NET runtime in a defined   state. Workers SHALL be authored under the assumption that a crash   terminates the entire WorkManager. This caveat is unique to the   <code>NativeDllEngine</code>; the Python and DotNetDll engines isolate their   workers in subprocesses.</li> </ul>"},{"location":"specs/worker-management/#scenario-unmanaged-timeout-caveat","title":"Scenario: Unmanaged timeout caveat","text":"<ul> <li>WHEN the <code>MessageHandlingTimeoutSeconds</code> elapses while a native   <code>Process</code> call is in flight</li> <li>THEN the <code>CancellationToken</code> on the managed side SHALL fire and the   engine SHALL surface a <code>worker.error</code> lifecycle event, but the native   call SHALL continue to run until it returns or the process crashes.   Workers SHALL be authored to honor the timeout cooperatively (e.g. via   periodic checks of a flag set from a watchdog thread, if at all) or   SHALL be guaranteed to return within the timeout.</li> </ul>"},{"location":"specs/worker-management/#scenario-abi-version-policy","title":"Scenario: ABI version policy","text":"<ul> <li>WHEN the WorkManager ships a new major ABI version</li> <li>THEN the engine SHALL continue to load workers declaring the   previous ABI version for at least one release cycle, and SHALL refuse   workers declaring ABIs older than that.</li> </ul>"},{"location":"specs/worker-management/#requirement-native-worker-library-naming","title":"Requirement: Native Worker Library Naming","text":"<p>The <code>NativeDllEngine</code> SHALL derive the per-platform shared library filename from the manifest's <code>library</code> field by appending an OS-specific prefix and suffix, matching the convention used by NuGet-distributed native packages (<code>runtimes/&lt;rid&gt;/native/</code>). The engine SHALL pick the filename based on <code>RuntimeInformation.IsOSPlatform</code>.</p>"},{"location":"specs/worker-management/#scenario-linux-filename","title":"Scenario: Linux filename","text":"<ul> <li>WHEN the running platform reports <code>OSPlatform.Linux</code></li> <li>THEN the engine SHALL load <code>lib&lt;library&gt;.so</code> from   <code>runtimes/&lt;rid&gt;/native/</code>.</li> </ul>"},{"location":"specs/worker-management/#scenario-windows-filename","title":"Scenario: Windows filename","text":"<ul> <li>WHEN the running platform reports <code>OSPlatform.Windows</code></li> <li>THEN the engine SHALL load <code>&lt;library&gt;.dll</code> from   <code>runtimes/&lt;rid&gt;/native/</code>.</li> </ul>"},{"location":"specs/worker-management/#scenario-macos-filename","title":"Scenario: macOS filename","text":"<ul> <li>WHEN the running platform reports <code>OSPlatform.OSX</code></li> <li>THEN the engine SHALL load <code>lib&lt;library&gt;.dylib</code> from   <code>runtimes/&lt;rid&gt;/native/</code>.</li> </ul>"},{"location":"specs/worker-management/#scenario-library-name-validation","title":"Scenario: Library name validation","text":"<ul> <li>WHEN the manifest's <code>library</code> field is empty or contains   characters outside <code>[A-Za-z0-9_.-]</code></li> <li>THEN the engine SHALL refuse the worker and throw a clear error   naming the offending value.</li> </ul>"},{"location":"specs/worker-management/#requirement-worker-tags-resource-metadata","title":"Requirement: Worker Tags (Resource Metadata)","text":"<p>The WorkManager SHALL let clients attach arbitrary key/value tags to a worker via <code>CreateWorkerRequest.tags</code>, <code>SetTag</code>, and <code>SetTags</code>, retrievable via <code>GetTag</code>/<code>GetTags</code>/<code>ListWorkers</code> (<code>WorkerInfo.tags</code>). Tags SHALL be resource metadata only \u2014 used for filtering (<code>ListWorkersRequest.filters: repeated TagFilter</code>) and identification \u2014 and SHALL NOT be injected into the worker's process environment or otherwise exposed to the worker's code.</p>"},{"location":"specs/worker-management/#scenario-tags-filter-listworkers","title":"Scenario: Tags filter ListWorkers","text":"<ul> <li>WHEN a client calls <code>ListWorkers</code> with one or more   <code>TagFilter{key, value}</code> entries</li> <li>THEN only workers whose tags match every filter SHALL be returned</li> </ul>"},{"location":"specs/worker-management/#scenario-tags-are-not-process-visible","title":"Scenario: Tags are not process-visible","text":"<ul> <li>WHEN a worker is loaded by any engine</li> <li>THEN its tags SHALL NOT be set as environment variables and SHALL NOT   be passed to the worker's <code>ProcessAsync</code> \u2014 a worker has no supported way   to read its own tags</li> </ul>"},{"location":"specs/worker-management/#scenario-settagsettags-update-stored-metadata-only","title":"Scenario: SetTag/SetTags update stored metadata only","text":"<ul> <li>WHEN a client calls <code>SetTag</code> or <code>SetTags</code> on a running worker</li> <li>THEN the WorkManager SHALL update the persisted tag metadata   immediately; no engine reload is required since tags are never applied   to the process</li> </ul>"},{"location":"specs/worker-management/#requirement-worker-creation-time-config","title":"Requirement: Worker Creation-Time Config","text":"<p>The WorkManager SHALL let clients set worker-instance defaults via <code>CreateWorkerRequest.config</code> (<code>map&lt;string, string&gt;</code>), retrievable via <code>WorkerInfo.config</code>. Unlike the removed worker environment-variable feature (which mutated process-wide <code>Environment</code> state and let co-loaded workers silently stomp on each other's values), config SHALL be delivered per-call, per-worker-instance: <code>Worker.ProcessAsync</code> SHALL stamp every <code>config</code> entry onto the incoming CloudEvent as an extension attribute before dispatching to the engine, using the same generic extension mechanism already used for <code>correlationid</code>. Config SHALL be immutable after creation \u2014 there SHALL be no <code>SetConfig</code>/<code>GetConfig</code> RPCs.</p>"},{"location":"specs/worker-management/#scenario-config-delivered-as-cloudevent-extension-attributes","title":"Scenario: Config delivered as CloudEvent extension attributes","text":"<ul> <li>WHEN a worker created with <code>config: {\"url\": \"https://example.com\"}</code>   receives a triggering CloudEvent</li> <li>THEN the CloudEvent SHALL carry a <code>url</code> extension attribute set to   <code>\"https://example.com\"</code> by the time it reaches the worker's engine</li> </ul>"},{"location":"specs/worker-management/#scenario-config-does-not-leak-between-co-loaded-workers","title":"Scenario: Config does not leak between co-loaded workers","text":"<ul> <li>WHEN two workers, each created with a different <code>config</code> value for   the same key, are loaded in the same WorkManager process</li> <li>THEN each worker's incoming CloudEvents SHALL only ever carry that   worker's own <code>config</code> value \u2014 never the other worker's</li> </ul>"},{"location":"specs/worker-management/#scenario-config-is-immutable-after-creation","title":"Scenario: Config is immutable after creation","text":"<ul> <li>WHEN a client wants to change a worker's config</li> <li>THEN the WorkManager SHALL NOT provide an RPC to do so; the worker   MUST be deleted and recreated with the new <code>config</code></li> </ul>"},{"location":"specs/worker-management/#scenario-config-survives-workmanager-restart","title":"Scenario: Config survives WorkManager restart","text":"<ul> <li>WHEN a WorkManager instance restarts and recovers a worker via   <code>RecoverWorkers</code></li> <li>THEN the recovered worker SHALL retain its original <code>config</code></li> </ul>"},{"location":"specs/worker-management/#requirement-worker-manager-horizontal-scalability","title":"Requirement: Worker Manager Horizontal Scalability","text":"<p>The WorkManager SHALL scale horizontally across different topics, not across the same topic. Multiple WorkManager replicas deployed against the same Dapr pubsub component SHALL compete for messages on each topic, not share load. This is a deliberate design choice that prioritizes hot-swap of workers within a topic over throughput scaling for that topic. Operators MUST NOT deploy multiple WM replicas expecting throughput gain on a single topic; they SHALL deploy multiple workers on different topics on the same WM replica instead.</p>"},{"location":"specs/worker-management/#scenario-multiple-wm-replicas-on-the-same-topic","title":"Scenario: Multiple WM replicas on the same topic","text":"<ul> <li>WHEN two WorkManager replicas both run a worker for topic <code>orders</code></li> <li>THEN each message published to <code>orders</code> is delivered to exactly one of the two replicas (competing consumers at the Dapr layer)</li> <li>AND the worker that wins the message processes it; the worker on the other replica receives nothing for that message</li> <li>AND there is NO automatic re-routing \u2014 a message that lands on a replica with no matching local worker is dropped at the WM layer, acked with no redelivery (an explicit business-logic skip, not a processing failure)</li> </ul>"},{"location":"specs/worker-management/#scenario-hot-swap-within-a-topic","title":"Scenario: Hot-swap within a topic","text":"<ul> <li>WHEN a worker on <code>orders</code> stops and a new worker on <code>orders</code> starts on the same WM replica</li> <li>THEN the new worker inherits the subscription via the <code>LatestWorkerId</code> dispatch mechanism</li> <li>AND the previous worker stops receiving messages on <code>orders</code></li> </ul>"},{"location":"specs/worker-management/#scenario-scaling-throughput","title":"Scenario: Scaling throughput","text":"<ul> <li>WHEN operators want to scale throughput for a high-volume topic</li> <li>THEN they SHALL NOT deploy multiple WM replicas expecting load sharing \u2014 that will cause competing and dropped messages</li> <li>AND they SHALL instead run multiple workers on different topics on the same WM replica (the typical Virtufin design), or scale within a topic by optimizing the worker code</li> <li>AND the Pub/Sub Topics spec \u00a7Horizontal Scalability section is the authoritative reference</li> </ul>"},{"location":"specs/worker-management/#scenario-observability-for-competing-consumer-behavior","title":"Scenario: Observability for competing-consumer behavior","text":"<ul> <li>WHEN an operator sees a worker not receiving messages on a topic</li> <li>THEN they SHALL check whether another WM replica is winning the Dapr competition for that topic</li> <li>AND the <code>workmanager.lifecycle</code> topic SHALL include <code>worker_id</code>, <code>replica_id</code>, and <code>topic</code> fields so operators can correlate</li> </ul>"},{"location":"specs/worker-management/#requirement-per-worker-routing-via-heartbeat-based-forwarding","title":"Requirement: Per-worker routing via heartbeat-based forwarding","text":"<p>Each worker SHALL be owned by the instance whose IInstanceIdProvider.GetInstanceId() matches the worker's WorkerConfig.InstanceId field in the Dapr state store (key: workers/{workerId}). Worker lifecycle RPCs (StartWorker, StopWorker, DeleteWorker, LoadCode, SetTag) SHALL be routed to the owning instance via direct gRPC forwarding when they land on a different replica. Peer endpoints SHALL be resolved from the heartbeat-driven PeerRegistry.</p>"},{"location":"specs/worker-management/#scenario-stopworker-reaches-the-owning-replica-via-forward","title":"Scenario: StopWorker reaches the owning replica via forward","text":"<ul> <li>WHEN a StopWorker(id: 21) RPC lands on replica A</li> <li>AND worker 21 was created on replica B</li> <li>THEN replica A's handler SHALL catch WorkerNotFoundException</li> <li>AND SHALL read WorkerConfig.InstanceId from the state store</li> <li>AND SHALL resolve replica B's gRPC endpoint from the PeerRegistry</li> <li>AND SHALL forward the StopWorker call via PeerChannelCache&lt;WorkManagerClient&gt;</li> <li>AND replica B SHALL stop the worker and update its state</li> <li>AND on forward failure (Unavailable), replica A SHALL reclaim the worker (re-stamp InstanceId, re-create locally) and retry</li> </ul>"},{"location":"specs/worker-management/#requirement-worker-source-authentication","title":"Requirement: Worker Source Authentication","text":"<p>URL-based code sources SHALL support an optional per-request credential, carried on the same request as the code source (<code>CreateWorkerRequest</code>/<code>LoadCodeRequest</code>), never persisted beyond the fetch it authorizes. A credential SHALL be one of: a directly-supplied Basic or Bearer secret, or a named reference resolved server-side. SSRF and host-allowlist validation SHALL apply regardless of whether a credential is present or which kind it is.</p>"},{"location":"specs/worker-management/#scenario-directly-supplied-credential","title":"Scenario: Directly-supplied credential","text":"<ul> <li>WHEN a code source URL is fetched with a Basic or Bearer credential attached to the request</li> <li>THEN the fetch SHALL use that credential's <code>Authorization</code> header, and no other</li> </ul>"},{"location":"specs/worker-management/#scenario-named-credential-resolution","title":"Scenario: Named credential resolution","text":"<ul> <li>WHEN a code source URL is fetched with a named credential reference</li> <li>THEN the service SHALL resolve the name to a server-held credential and validate the target host is permitted for that named credential before injecting it</li> </ul>"},{"location":"specs/worker-management/#scenario-unresolvable-named-credential","title":"Scenario: Unresolvable named credential","text":"<ul> <li>WHEN a request references a named credential the service does not recognize, or whose target host is not permitted for that name</li> <li>THEN the fetch SHALL fail with a clear authorization error rather than proceeding unauthenticated</li> </ul>"},{"location":"specs/worker-management/#scenario-no-credential-supplied","title":"Scenario: No credential supplied","text":"<ul> <li>WHEN a code source URL is fetched with no credential on the request</li> <li>THEN the fetch SHALL proceed without an <code>Authorization</code> header</li> </ul>"},{"location":"specs/worker-management/#scenario-credentials-are-not-persisted","title":"Scenario: Credentials are not persisted","text":"<ul> <li>WHEN a code source fetch completes, successfully or not</li> <li>THEN the supplied credential SHALL NOT be logged or retained beyond that fetch</li> </ul>"}]}